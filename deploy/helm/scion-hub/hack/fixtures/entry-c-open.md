<!--
FIXTURE, Entry C, the OPEN arm. Not a real VALIDATION.md.

It exists so the obligation evaluator has a subject whose answer is known
before it is asked. This is the false arm of the committed true/false pair
every registry entry is required to carry: an evaluator tested only against
the world it will meet is an evaluator whose other branch has never executed.
-->

#### 7.2 The IAM verification: UNRUN, with the refusals that stopped it

The verification could not be performed. It is UNRUN. Measured against a
principal that received 403 from both the Cloud SQL and the GKE APIs, with no
kubectl context available and no local Docker fallback.

`database.auth` is required and has no default, because a default nobody
verified is a parameter nobody chose.

This section is padded to clear the evaluator's minimum-length denominator on
purpose. A fixture shorter than the floor would take the CANNOT-EVALUATE branch
and this arm would go green while testing something else entirely - which is
the same defect the floor was added to catch, reproduced in the fixture.

#### 7.3 The manual smoke test: UNRUN
