{{- $fullName := include "scion-hub.fullname" . -}}
{{- $hubId := include "scion-hub.hubId" . -}}
Scion hub {{ $fullName }} installed in namespace {{ .Release.Namespace }}.

Hub ID: {{ $hubId }}

  This is the value you supplied in hub.hubId, rendered verbatim. The chart never
  generates, defaults or derives it. Supply the same value on every upgrade:
  changing it re-scopes the hub's blob storage prefixes and its secrets and is
  NOT a safe upgrade. Two releases in one cluster must not share a hub ID.

Reach the hub:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ $fullName }} 8080:{{ .Values.service.port }}
  curl -i http://127.0.0.1:8080/readyz

  /readyz is the only readiness signal, and the chart hardcodes it. The hub's
  other health endpoints answer 200 unconditionally and are intercepted by the
  Google front end, so a probe or load balancer health check pointed at one of
  those reports healthy whatever state the hub is in. The path is also matched
  by exact string in both the route table and the authentication exemption, so
  a prefixed variant is at once unrouted and not exempt from authentication.

Agent pods will be created in namespace: {{ include "scion-hub.agentNamespace" . }}
{{- if .Values.runtime.listAllNamespaces }}
  runtime.listAllNamespaces is true, so the hub was granted a ClusterRole rather
  than a namespaced Role.
{{- end }}

{{ if .Values.serviceAccount.create -}}
Workload Identity: bind the Kubernetes service account to your Google service
account before the hub needs Google APIs:
{{ if .Values.serviceAccount.gcpServiceAccount }}
  gcloud iam service-accounts add-iam-policy-binding {{ .Values.serviceAccount.gcpServiceAccount }} \
    --role roles/iam.workloadIdentityUser \
    --member "serviceAccount:PROJECT.svc.id.goog[{{ .Release.Namespace }}/{{ include "scion-hub.serviceAccountName" . }}]"
{{- else }}
  serviceAccount.gcpServiceAccount is not set, so no
  iam.gke.io/gcp-service-account annotation was rendered and the hub has no
  Google identity.
{{- end }}
{{- end }}
{{ if .Values.cloudsql.enabled }}
{{- $icnParts := splitList ":" .Values.cloudsql.instanceConnectionName }}
{{- $csProject := "PROJECT" }}{{- $csRegion := "REGION" }}{{- $csInstance := "INSTANCE" }}
{{- if eq (len $icnParts) 3 }}
{{- $csProject = index $icnParts 0 }}{{- $csRegion = index $icnParts 1 }}{{- $csInstance = index $icnParts 2 }}
{{- end }}
CLOUD SQL

  Instance:  {{ .Values.cloudsql.instanceConnectionName }}
  Proxy:     {{ include "scion-hub.cloudsqlProxyImage" . }}
  Listening: 127.0.0.1:{{ .Values.cloudsql.port }}{{ if .Values.cloudsql.privateIp }} (via the instance's private IP){{ end }}
  Health:    127.0.0.1:{{ .Values.cloudsql.healthCheckPort }}/startup, /readiness, /liveness
{{- if not (eq .Values.database.driver "postgres") }}

  database.driver is {{ .Values.database.driver }}, not postgres, so NOTHING IN THE POD WILL DIAL
  THE PROXY. It is rendered, it will start, it will hold an open connection to
  the instance above, and the hub will never use it - the hub is on
  {{ .Values.database.driver }} and opens no connection to port {{ .Values.cloudsql.port }} at all. That is a
  running cost and an idle instance connection in exchange for nothing. Either
  set cloudsql.enabled: false, or set database.driver: postgres if the instance
  above is the database you meant to use.
{{- else }}

  The hub reaches this instance ONLY through the proxy. The rendered DSN fixes
  the host at 127.0.0.1 and the proxy is what listens there; there is no
  database.host key. Authentication is {{ .Values.database.auth }}.
{{- if eq .Values.database.auth "iam" }}
  Under iam the DSN carries no password - the proxy obtains an OAuth token from
  the pod's Google identity and presents it, so the credential never exists as
  a string the chart could render, log or leak.
{{- else }}
  Under password the credential lives in the settings Secret and reaches the
  hub inside the rendered settings.yaml. It is not on any container's argv, not
  in a ConfigMap and not in a pod annotation; the chart asserts all three.
  Rotating it is a helm upgrade, which rewrites the Secret - and leaves every
  running pod on the OLD credential until you restart them by hand. Read
  ROTATING THE PASSWORD REQUIRES ONE MANUAL STEP below before you rotate: it
  is the command, and the reason the chart cannot do it for you.
{{- end }}

  BEFORE THE POD CAN CONNECT. These are the exact commands for the values you
  supplied, and they are DERIVED FROM GOOGLE'S DOCUMENTATION, NOT FROM A RUN.
  Nothing below has been executed against a real project - see the unrun note
  under WHAT THIS RELEASE DOES NOT YET DO. If one of them is rejected, the
  command is wrong and this chart wants to hear about it.
{{ if .Values.serviceAccount.gcpServiceAccount }}
  1. Let the proxy open the instance.

  gcloud projects add-iam-policy-binding {{ $csProject }} \
    --member "serviceAccount:{{ .Values.serviceAccount.gcpServiceAccount }}" \
    --role roles/cloudsql.client
{{- if eq .Values.database.auth "iam" }}

  2. Let that identity authenticate AS A DATABASE USER. This is a separate role
     from cloudsql.client and omitting it is the most common cause of a proxy
     that connects and a login that is then refused.

  gcloud projects add-iam-policy-binding {{ $csProject }} \
    --member "serviceAccount:{{ .Values.serviceAccount.gcpServiceAccount }}" \
    --role roles/cloudsql.instanceUser

  3. Create the database role. Note the SUFFIX IS OMITTED: Cloud SQL registers
     the IAM principal under the service account email with
     ".gserviceaccount.com" trimmed, because a PostgreSQL role name is limited
     to 63 bytes. The untrimmed spelling is not a role that exists, and the
     resulting error names neither the suffix nor the length.

  gcloud sql users create {{ include "scion-hub.databaseUser" . }} \
    --instance={{ $csInstance }} \
    --project={{ $csProject }} \
    --type=cloud_iam_service_account
{{- else }}

  2. Create the database role and set its password. Use the same value you put
     in database.password; the chart does not create the role and cannot check
     that the two agree.

  gcloud sql users create {{ include "scion-hub.databaseUser" . }} \
    --instance={{ $csInstance }} \
    --project={{ $csProject }} \
    --prompt-for-password

     --prompt-for-password rather than --password=VALUE, deliberately. The
     second form puts the credential in your shell history and in the argv of a
     process on your workstation. This chart refuses to put a secret on a
     container's argv; the same rule is worth keeping one hop upstream.
{{- end }}

  {{ if eq .Values.database.auth "iam" }}4{{ else }}3{{ end }}. Create the database, if it does not exist. The hub does not create it;
     AutoMigrate creates TABLES inside a database that is already there.

  gcloud sql databases create {{ .Values.database.name }} \
    --instance={{ $csInstance }} \
    --project={{ $csProject }}

  {{ if eq .Values.database.auth "iam" }}5{{ else }}4{{ end }}. Grant the role enough to migrate. The hub runs ent AutoMigrate at
     startup, which issues CREATE TABLE and ALTER TABLE, so SELECT is not
     enough. On PostgreSQL 15 and later the public schema no longer grants
     CREATE to PUBLIC, so a role that worked on 14 fails on 15 with
     "permission denied for schema public" - which reads as a missing user
     rather than a missing grant. Connect as a superuser and run:

  GRANT ALL PRIVILEGES ON DATABASE "{{ .Values.database.name }}" TO "{{ include "scion-hub.databaseUser" . }}";
  GRANT ALL ON SCHEMA public TO "{{ include "scion-hub.databaseUser" . }}";

     The double quotes around the role name are not decoration. PostgreSQL
     folds an unquoted identifier to lower case, so any capital letter in the
     name is silently changed and the grant lands on a role that does not
     exist{{ if eq .Values.database.auth "iam" }} - and under iam the name also contains an @, which is not a legal
     unquoted identifier character at all{{ end }}.

     THE MINIMAL grant set is not established here. These two are sufficient by
     documentation and were not narrowed by experiment, because narrowing a
     privilege you cannot test is how you ship an install that fails on its
     second migration rather than its first.
{{- else }}
  serviceAccount.gcpServiceAccount is not set, so no
  iam.gke.io/gcp-service-account annotation was rendered and there is no
  identity here for these commands to bind. The proxy does not fail closed on
  that: it falls back to whatever Application Default Credentials it can find,
  which on GKE is the NODE's service account - an identity you did not choose,
  shared with every other pod on that node, and either over-privileged or
  refused. Set serviceAccount.gcpServiceAccount and re-read this section; the
  commands then print with your values already substituted.
{{- end }}

{{- if eq .Values.database.auth "password" }}

  ROTATING THE PASSWORD REQUIRES ONE MANUAL STEP. THIS IS THE STEP.

  When you change database.password, `helm upgrade` writes the new value into
  the settings Secret and reports success - and every running pod keeps using
  the OLD password until you also run:

    kubectl rollout restart deploy/{{ include "scion-hub.fullname" . }} -n {{ .Release.Namespace }}

  Nothing detects the omission. The upgrade is green, the Secret is correct, and
  the hub is still authenticating with the credential you just retired. If you
  rotated because the old one leaked, it is still live.

  WHY, IN ONE PARAGRAPH, SO THE STEP IS NOT MISTAKEN FOR A BUG TO BE FIXED.
  settings.yaml is a subPath mount and the kubelet never refreshes one, so the
  chart rolls the pods by digesting the settings document into a pod annotation.
  That annotation is readable by anyone who can get deployments - a wider
  audience than the Secret - and the only unknown in a DSN is the password, so
  a digest of the raw document lets that reader confirm a guessed password
  offline. The digest is therefore taken over a projection with the credential
  removed, and a change that touches nothing but the credential no longer moves
  it. Everything else still rolls the pods on its own: host, port, database
  name, pool sizes, config.extra.

  If you would rather not hold the step in your head, rotate the database name
  or the user in the same upgrade and the roll happens automatically - but do
  that because it suits your operations, not as a workaround.
{{- end }}

  THE CONNECTION BUDGET, AND THE PART OF IT WE HAVE NOT MEASURED.

  Cloud SQL enforces a hard max_connections per instance. Exceeding it does not
  degrade: new connections are refused, and the hub's refusal surfaces during
  AutoMigrate or mid-request rather than at startup, so it looks like an
  intermittent application fault. Size it before you scale, not after.

    TOTAL = R * (M + S)

    R = replicaCount                     = {{ .Values.replicaCount }}
    M = database.maxOpenConns            = {{ .Values.database.maxOpenConns }}   (the ent pool, per replica)
    S = every other connection a replica holds
        YOU MUST SUPPLY THIS. It is not a chart value and the chart does not
        guess it, because we have not measured it.

  WE HAVE NOT MEASURED S. No hub has been run against a Cloud SQL instance in
  the course of building this chart - the deploying principal is refused by
  sqladmin.googleapis.com with HTTP 403 - so there is no observation of steady
  state to report, and a number invented here would be indistinguishable from
  one. Measure it on your own instance, at your own load, with one replica:

{{- if .Values.config.existingSecret }}

    SELECT count(*) FROM pg_stat_activity
     WHERE datname = '<the database in your own DSN>'
       AND usename = '<the role in your own DSN>';

  The chart cannot fill those in under config.existingSecret: database.name and
  database.user are refused there precisely because they would go nowhere, and
  the DSN the hub dials is the one in your settings.yaml. M below is the chart's
  default rather than your value, for the same reason.
{{- else }}

    SELECT count(*) FROM pg_stat_activity
     WHERE datname = '{{ .Values.database.name }}' AND usename = '{{ include "scion-hub.databaseUser" . }}';
{{- end }}

  Take that at steady state under representative traffic, subtract M, and the
  remainder is your S. Re-measure after any change to replica count or
  workload; S is not a constant of the software, it is a property of your load.

  THE STRUCTURAL MAXIMUM, WHICH IS A CEILING AND NOT AN OVERHEAD. Read out of
  the hub's source, so it says what the code CAN open, not what a running hub
  DOES open. Do not budget with it as though it were an observation, and do not
  substitute it for S:

    S_max = 2 * max(4, NumCPU) + 2

  per replica, when database.driver is postgres. Its terms:

    - The Postgres event publisher's pool (pkg/hub/events_postgres.go:169) and
      the Postgres command bus's pool (pkg/hub/command_bus.go:113). Both call
      pgxpool.ParseConfig on this same DSN and neither overrides MaxConns -
      applyEventPoolKeepalives sets only idle time and lifetime - and the DSN
      the chart renders carries no pool_max_conns. So each defaults to
      max(4, runtime.NumCPU()) (pgx v5.9.2, pgxpool/pool.go:383).
    - One dedicated LISTEN connection each, outside those pools, held open for
      the process lifetime (events_postgres.go:486, command_bus.go:259). These
      two are not a maximum; they are always open.

  NumCPU IS THE NODE'S CPU COUNT, NOT YOUR CPU LIMIT. The hub does not set
  GOMAXPROCS and does not vendor automaxprocs, so a pod with a 500m limit on a
  64-core node still reads 64. S_max there is 130 per replica, from a container
  you sized at half a core. If you are on large nodes, this term dominates
  everything else in the budget.

  WHY IT IS A CEILING AND NOT A COUNT: pgxpool opens connections on demand and
  closes them when idle, so a quiet hub sits far below this. How far below is
  precisely the thing we did not measure.

  database.maxOpenConns must be at least 2. The hub treats a value of 1 or less
  as unset (pkg/config/hub_config.go:573) because a single-connection pool
  self-deadlocks the moment one query waits on another, so a 1 you set to save
  connections would silently become the hub's own default instead of failing.
  The schema rejects it up front rather than letting that substitution happen.
{{- end }}
{{- if not .Values.cloudsql.nativeSidecar }}

  WARNING: cloudsql.nativeSidecar IS FALSE. THE HUB WILL CRASH-LOOP AT STARTUP.

  The proxy is rendered as a plain container in containers[], and Kubernetes
  gives containers in that list NO START ORDER. The hub therefore races the
  proxy for the loopback port, loses it most of the time, and exits; the
  kubelet restarts it; eventually the proxy is up and a restart succeeds. The
  pod reaches Ready after a handful of CrashLoopBackOff cycles and the events
  say "connection refused", naming nothing that is actually wrong.

  The second half is worse and is not self-correcting. Kubernetes has no
  shutdown order either, so on pod termination the proxy can exit while the hub
  is still draining, and in-flight queries fail against a socket that closed
  under them. There is no backoff that recovers from that.

  This exists ONLY for GKE below 1.29, where restartPolicy on an init container
  is not honoured. On 1.29 or later set cloudsql.nativeSidecar: true and both
  problems disappear - the proxy starts before the hub and is torn down after
  it, by the API, not by luck. Check with: kubectl version
{{- end }}
{{- if .Values.cloudsql.nativeSidecar }}
{{- with (include "scion-hub.nativeSidecarGuard" (dict "major" .Capabilities.KubeVersion.Major "minor" .Capabilities.KubeVersion.Minor "version" .Capabilities.KubeVersion.Version)) }}

  NOTICE: THE 1.29 CHECK BEHIND cloudsql.nativeSidecar DID NOT RUN.

  It is not a failure and it is not an approval. The chart could not read a
  major/minor version out of what this cluster reported, so it declined to
  judge - and it says so here rather than leaving you to assume a silent guard
  is a satisfied one. The manifest carries the same notice above the sidecar.

{{ . | trimPrefix "# " | wrapWith 74 "\n" | indent 2 }}
{{- end }}
{{- end }}
{{- end }}

THE IMAGE

  Running: {{ include "scion-hub.image" . }}

  This must be a hub image built from the root Dockerfile with
  --target hub-gke: non-root at uid 1000, web UI embedded, no baked KUBECONFIG.
  The hub-gke stage arrives with the image-build change that accompanies this
  chart; until then that build reports an unknown target.

  The published artifact named "scion-hub"
  (us-central1-docker.pkg.dev/PROJECT_ID/public-docker/scion-hub, built from
  image-build/hub/Dockerfile) is NOT that image. It runs as root and it is built
  without the embedded web UI, so --enable-web has nothing to serve. The shared
  name is a coincidence. There is no published hub-gke image yet; you build it.

  The chart sets runAsNonRoot on the pod and on the hub container and offers no
  way to turn it off, so a root image fails admission with a clear message
  instead of running. That refusal applies now; the consequence it spares you -
  root-owned files in a workspace share that agents cannot write - arrives with
  the Filestore phase, which this release does not include.
{{ if not .Values.image.digest }}
  Pin the image by digest (image.digest) rather than by tag. A tag can be
  repointed underneath a running cluster; a digest cannot. This release uses a
  tag.
{{- end }}

CONFIGURATION
{{ if .Values.config.existingSecret }}
  From your own Secret: {{ .Values.config.existingSecret }}, key settings.yaml.

  The chart rendered nothing and validated nothing. Hosted mode, the hub ID, the
  preflight keys and schema_version are all yours to get right - see the section
  below on schema_version in particular, which is not optional.

  These values of yours reached nothing. Each one only ever wrote to the
  settings.yaml the chart did not render, so whatever you set them to, the
  running hub takes the key on the right from YOUR file or from its own default:

{{ include "scion-hub.existingSecretTransfers" . }}

  This list is written by hand AND checked against the render, in both
  directions: a value that goes inert without appearing here fails the chart's
  test suite, and an entry here that no longer writes the key it claims fails it
  too. Neither half is worth much without the other, and the check was vacuous
  once already - it read any refusal as coverage, including one fired by a
  different value entirely.

  Values not listed here still work normally - they reach the Deployment, the
  RBAC or the Service. The cloudsql.* values in particular are fully live under
  config.existingSecret: they configure the proxy sidecar, which the chart does
  render. What YOUR settings.yaml must then match is the DSN the proxy expects -
  127.0.0.1 and cloudsql.port - because nothing here writes it for you.

  database.auth is the one value that is half of each, so it is worth a
  sentence. It is LIVE for the proxy - iam is what adds --auto-iam-authn - and
  INERT for the DSN, which is yours. Set it to the mode your own settings.yaml
  actually uses. Setting it to iam while your DSN carries a password gets you a
  proxy authenticating one way and a hub the other, and the failure arrives at
  the first connection rather than at install.

  These also write to settings.yaml and are missing on purpose: they fail the
  render outright when set alongside config.existingSecret, so they cannot go
  quiet on you.

    {{ include "scion-hub.existingSecretRefusals" . }}
{{- else }}
  From the Secret {{ include "scion-hub.settingsSecretName" . }}, key settings.yaml.

    kubectl --namespace {{ .Release.Namespace }} get secret {{ include "scion-hub.settingsSecretName" . }} \
      -o jsonpath='{.data.settings\.yaml}' | base64 -d
{{- end }}

  Mounted read-only at {{ include "scion-hub.scionDir" . }}/settings.yaml.

  Configuration is a file and not environment variables, and that is not a style
  choice. A SCION_SERVER_ name binds only if every underscore-separated segment
  is a plain lowercase word or is listed in camelCaseFields
  (pkg/config/hub_config.go:919), so SCION_SERVER_DATABASE_URL binds and
  SCION_SERVER_DATABASE_MAX_OPEN_CONNS is discarded with no error - and the
  discarded one is still listed to the admin server-config view as an active
  override, so it is reported as applied. The ones that DO bind are worse: they
  are applied after settings.yaml is read and win, so the hub runs a
  configuration this chart never rendered and its guards never saw. Use
  config.extra, which lands in the file.

THE SETTINGS FILE IS READ-ONLY. THIS IS DELIBERATE.

  If you are here because the hub logged a warning about being unable to write
  its settings file, or because a settings change you made through the API did
  not stick: that is this mount, working. Making it writable is the wrong fix,
  and it is the fix everybody reaches for first, so here is the reasoning before
  you spend the evening on it.

  The hub rewrites settings.yaml at runtime. In a Deployment those writes are
  pod-local - the other replicas never see them, and they are lost at pod
  replacement. That alone would only be lossy. What makes it dangerous is
  syncHubSettings, which runs on every boot and re-seeds shared database state
  from the pod-local file for every section whose row is absent or still marked
  as seeded. So a pod that can write this file can promote its own local
  divergence into the shared truth every other replica then reads, at a moment
  nobody chose, with no error anywhere.

  Making the file writable does not fix that. It re-enables it. The proper fix is
  in the hub - the settings file becomes read-only in hosted mode with the
  database as the sole writable store - and it is tracked as ptone/scion#1091.
  When that lands this mount stops being load-bearing. Until then it is the only
  thing preventing the divergence, and the cost of it is a log line.

  Note the directory is NOT read-only. Only the one file is. {{ include "scion-hub.scionDir" . }}
  is the hub's whole state directory - storage/, templates/ and scion-token are
  all created in it - so mounting configuration over the directory breaks the hub
  in ways that have nothing to do with configuration.

  WHAT DOES NOT PERSIST, SPECIFICALLY. "Some settings may not persist" is no use
  to someone who just got an HTTP 200. Until #1091:

    - The GitHub App configuration PUT returns HTTP 200 and does not persist.
      You get a success response and no effect, every time, indefinitely. This
      is the one most likely to cost someone an afternoon.
    - The integration settings writes mostly log a warning and carry on. One of
      them returns HTTP 500 to the caller while the server keeps running.
    - The startup write logs a warning and continues.

  Nothing crashloops, nothing is corrupted, and nothing diverges. Every one of
  these paths is soft by inspection - no log.Fatal, no os.Exit, no panic.

  This is a behaviour change from a writable settings file, and in one respect it
  looks worse: previously these writes succeeded, appeared to work, and were
  silently lost at the next pod replacement. Now they never take effect at all.
  Consistent beats intermittent - a write that always fails visibly is
  debuggable, and a write that works until a rollout is not - but you have to be
  told, which is what this section is for.

  Configure these through your values file and helm upgrade instead. Note the
  separate hazard that a hub which has already run against Postgres keeps some
  settings in database rows that shadow the file entirely; see VALIDATION.md.

SCHEMA_VERSION IS LOAD-BEARING

  The rendered file carries schema_version: "1", and it is not boilerplate.

  The hub auto-migrates a settings file whose format it cannot detect, the
  detector keys on this field, and the migration replaces the file with
  os.Rename. Renaming over a bind-mounted path returns EBUSY. Every other write
  to this file under the mount is soft; that one is a hard failure. Rendering
  schema_version is what stops the migration from ever being attempted, and the
  hub does not guard the path itself in hosted mode - deliberately, on the
  grounds that the chart controls the input. If you supply your own settings.yaml
  through config.existingSecret, it must carry schema_version.

WHAT THIS RELEASE DOES NOT YET DO

  The chart is being built incrementally. It renders the hub's settings.yaml and
  it wires Cloud SQL and the database URL, but it does not yet configure GCS
  credentials beyond the bucket name, Filestore, the session secret, the OAuth
  client secret, Ingress or IAP. Do not treat this as a production install.

  The Cloud SQL wiring is RENDERED BUT UNRUN. No part of it has been applied to
  a cluster or dialled at a real instance: the deploying principal is refused by
  both container.googleapis.com and sqladmin.googleapis.com with HTTP 403, so
  there was no cluster to install into and no instance to connect to. What is
  established is that the chart renders this shape and that the render passes
  the schema and kubeconform. That the proxy authenticates, that the IAM role
  name below is the one Cloud SQL expects, and that the hub completes
  AutoMigrate through it are three further claims and all three are unrun. See
  VALIDATION.md section 7.2, which records them as unrun rather than pending.

  THIS RELEASE CAN TURN THE HUB'S HA PREFLIGHT ON, AND IT CANNOT SATISFY IT, SO
  THE CHART REFUSES TO RENDER THOSE SHAPES. Three of the settings this chart
  writes make isHADeployment (cmd/server_foreground.go:927) true on their own:

    hub.extraEnv setting K_SERVICE                    (:928)
    database.driver: postgres                         (:931)
    storage.provider: gcs with auth.mode: proxy       (:934)

  Any one of them runs validateHostedHAPreflight, whose error aborts startup at
  :151 rather than degrading - log.Fatalf, not a warning. Several of its thirteen
  gates have no source in this chart. Measured by walking the real preflight
  gate by gate over the settings.yaml this chart renders, in the order the hub
  checks them - the list below is checked against that walk, it is not typed
  from memory:

    a durable session secret                session-secret phase
    server.auth.proxy.provider: iap         }
    server.auth.proxy.iap.audience          }
    server.auth.transport present           }  Ingress/IAP phase
    server.auth.transport.mode: iap         }
    server.auth.transport.oidc_audience     }
    server.auth.transport.platform_auth_sa  }

  server.database.url headed this list until the Cloud SQL phase, which landed
  it. The list is shorter because a gate was closed, not because it was re-read.
  It stays yours under config.existingSecret, where the chart renders no
  settings.yaml and so cannot supply it.

  With auth.mode: oauth there is one more, server.auth.mode: proxy (:980), which
  no phase lands - that one is the mode you chose being incompatible with HA
  detection rather than configuration that is missing.

  You would meet them one at a time, each only after fixing the one before it,
  because the preflight returns on the first failure. Rather than let you find
  that out one install at a time, the chart refuses up front and names them all.
  Set acknowledgeHAUnlanded: true to render anyway - for rendering the
  settings.yaml, which works, not for installing, which does not. The chart
  already satisfies the rest of what the preflight demands (the hub ID, the
  postgres driver, gcs storage, the bucket, and now the database URL), which is
  why the refusal starts at the session secret and not earlier.

  The default install is unaffected: sqlite and local storage match none of the
  three routes, isHADeployment is false, and the preflight is skipped.
{{- $haRoutes := include "scion-hub.haRoutes" . }}
{{- if $haRoutes }}

  THESE VALUES ARE ON ONE OF THOSE ROUTES AND THE REFUSAL WAS SUPPRESSED.

  {{ $haRoutes }}, so the hub's isHADeployment test is true for this release.
  The chart did not refuse only because acknowledgeHAUnlanded is set.

  THE RENDER IS NOT A VERDICT ON THE INSTALL. The settings.yaml above is
  correct, and the hub will still abort at the first of the gates listed
  here - the durable session secret - before it serves anything. This release
  closes server.database.url and none of the ones that remain.

  If you started from ci/values-settings.yaml or ci/values-settings-oauth.yaml,
  the flag arrived with the file. Those two fixtures set it so that the
  rendering path can be exercised, and they are also the shapes an operator is
  most likely to copy - so the chart's own examples are the one case where the
  refusal that names these gates never prints. That is why this paragraph does.
{{- end }}

  The hub images published from this repository today run as root, so until a
  hub-gke image exists there is nothing this chart can successfully schedule.
  That is the intended behaviour, not a defect: see THE IMAGE above.

{{ if gt (int .Values.replicaCount) 1 -}}
WARNING: replicaCount is {{ .Values.replicaCount }}.

  Schema migration is safe across replicas, but several hub subsystems hold
  state in the pod: the agent control channel manager, the presence manager, the
  port tunnel manager, the local disk attachment store and the in-process event
  bus. An agent's control channel terminates on exactly one pod, so a web
  terminal, log tail, exec or port-forward request that lands on any other pod
  fails - roughly (N-1)/N of attempts. Load balancer session affinity does not
  fix this: it pins a client to a pod, not to the pod holding the channel.
{{- end }}
