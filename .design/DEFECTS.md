# Defect Ledger — ca-msg-arch

Extracted from `IMPLEMENTATION-STATE.md` by `ca-msg-ledger`, 2026-08-31.
Cross-checked and reconciled by `ca-msg-arch`, 2026-08-31.

**This file is the tracker. The journal is not.** The journal is append-only
narrative and records a defect's status at the moment of writing; only this
table records it now. When a defect changes state, edit the row here in the
same commit as the journal entry that explains why.

Three rows were already stale when extraction finished, because they changed
while it ran (DEF-12, DEF-83, and the two new ids DEF-89/DEF-90 that did not
exist when the sweep started). That is the argument for maintaining this file
rather than re-deriving it.

## Ledger

| ID | Summary | Status | Last mention | Anchor |
|---|---|---|---|---|
| DEF-1 | Participant-level auth on `conv:<id>` — implemented in S4 handler | CLOSED | 5484 | 5441 |
| DEF-2 | Cross-project addressee validation — conditionally closed, wired by C5 [^1] | CLOSED | 16251 | 5442 |
| DEF-3 | Divergence gate independent source of truth — `CheckConversationConsistency` added | CLOSED | 21725 | 5458 |
| DEF-4 | `pkg/hub` test suite OOM — fixed at `b92926dd` | CLOSED | 5462 | 5460 |
| DEF-5 | `conv:<id>` and `#<thread>` have no CLI delivery policy; routing unbuilt | OPEN | 25223 | 5443 |
| DEF-6 | Scheduled sends cannot address a conversation — specced §2.14, not built | OPEN | 18610 | 5444 |
| DEF-7 | `#<thread>` form can never resolve — design answered, build pending §2.6.3 | OPEN | 5447 | 5447 |
| DEF-8 | Agent DMs as two disjoint rows — partial fix in Tranche A, reconciliation unspecced [^2] | OPEN | 21544 | 5448 |
| DEF-9 | Addressee mechanism unwired — `message_addressees` table never written in production | OPEN | 18610 | 5449 |
| DEF-10 | `@<agent>` DMs project-scoped contradicting Q2 — half-struck, remainder bounded | OPEN | 18610 | 5450 |
| DEF-11 | Divergence board CLI mismatch — artifact struck after re-derivation | CLOSED | 18760 | 5445 |
| DEF-12 | Backfill entry point unreachable — PR #1426 merged to main at `66b5cab7f` | CLOSED | 25177 | 5446 |
| DEF-13 | Conversation-reference forms documented — merged at `edd4e4bd` | CLOSED | 5457 | 5451 |
| DEF-14 | DM key membership check — struck, mechanism replaced | CLOSED | 16714 | 5452 |
| DEF-15 | `dm:`-prefixed ThreadID third DM shape — resolved via §2.15 phase 4 at `14b3ba7c` | CLOSED | 5454 | 5453 |
| DEF-16 | Dual-write validation ordering — both handlers validate-before-write on main | CLOSED | 16714 | 5454 |
| DEF-17 | Integration branch CI gate failures — struck, addressed via tranche workflow | CLOSED | 16714 | 5457 |
| DEF-18 | AC-33 cross-project refusal cannot name refused agents — collected but never read | OPEN | 18610 | 5455 |
| DEF-19 | Phase 7 validation `group[]` breakage — fixed, merged in Tranche C | CLOSED | 11366 | 5456 |
| DEF-20 | Phase 4 topic-lookup guard — fixed and verified, Tranche C merged | CLOSED | 15303 | 3927 |
| DEF-21 | Error/absence conflation in topic lookup — fixed, Tranche C merged | CLOSED | 3964 | 3934 |
| DEF-22 | Dangling `conversation_id` — fixed, Tranche C merged | CLOSED | 3947 | 3947 |
| DEF-23 | Two vacuous ACs on topic-scoped sends — fixed, Tranche C merged | CLOSED | 3954 | 1948 |
| DEF-24 | WITHDRAWN — not a defect; stale cut point already fixed on staging [^3] | CLOSED | 1951 | 1951 |
| DEF-25 | Compat-literal test fixtures — fixed, gate re-verified EXIT=0 | CLOSED | 24268 | 841 |
| DEF-26 | Green placeholder test renamed — PR #1340 merged to main at `7c5a64ae9` | CLOSED | 16714 | 792 |
| DEF-27 | Soft-deleted topic shadow conversation — fixed at `25fad0a2`, Tranche C merged | CLOSED | 13409 | 921 |
| DEF-28 | `UpsertConversationByExternalRef` parent_ref erasure — guarded at `f57e07b6` | CLOSED | 5725 | 1041 |
| DEF-29 | `CreateConversation` keyless direct guard — landed, Tranche A merged | CLOSED | 26935 | 1492 |
| DEF-30 | Stored DM keys format mismatch — closed, no migration needed, no exposure | CLOSED | 5702 | 1941 |
| DEF-31 | Topic `default_agent` cross-project routing — PR #1338 merged at `310126977` | CLOSED | 16714 | 1318 |
| DEF-32 | Federated principals unattributable — no `(issuer,subject) → user_id` link table | OPEN | 25476 | 3117 |
| DEF-33 | `hasAgentReplyAfter` casing mismatch fail-open — concrete fix exists but held | OPEN | 18610 | 12389 |
| DEF-34 | `hasAgentReplyAfter` `Channel:"web"` filter — blocked on external #1259 | OPEN | 25224 | 12547 |
| DEF-35 | Pagination constant coupling — latent, folded into DEF-34 pass | OPEN | 13863 | 13863 |
| DEF-36 | Topic mints never populate listing index — landed with #1380 | CLOSED | 26860 | 14857 |
| DEF-37 | Validation-exemption marker gate — C5 merged, PR #1410 merged | CLOSED | 21074 | 2898 |
| DEF-38 | Marker gate stale expectations for `authorizeAgentMessage` — PR #1376 merged | CLOSED | 16714 | 15505 |
| DEF-39 | Fail-open guard in C1 exemption — DEF-39b verified closed, #1379 merged | CLOSED | 16714 | 15527 |
| DEF-40 | Order-dependent cross-project boundary hole — #1379 merged, explicit bool fix | CLOSED | 16714 | 15764 |
| DEF-41 | `validate_compat.go` fabricated `ConversationID` — #1401 landed | CLOSED | 19638 | 2821 |
| DEF-42 | `webChatStore` race condition — PR #1405 merged to main | CLOSED | 20221 | 2899 |
| DEF-43 | Cloud-logs test ordering — verified green on main after #1399 | CLOSED | 18605 | 2965 |
| DEF-44 | Stale `go.sum` in extras — cleared, C7 merged | CLOSED | 18606 | 17549 |
| DEF-45 | Chat-app identity compile error — cleared | CLOSED | 18606 | 17646 |
| DEF-46 | 4 `send_test.go` tests skip in CI — missing `/scion-volumes/` root | OPEN | 18611 | 18409 |
| DEF-47 | Production `/home/scion` fallback — behaviour change question | OPEN | 18611 | 18444 |
| DEF-48 | Validation-after-resolve orphan row on `@agent` path — #1402 landed | CLOSED | 20118 | 2854 |
| DEF-49 | Caller-supplied `conversation_id` membership check — #1403 merged | CLOSED | 19841 | 3097 |
| DEF-50 | Reachability gate comment-blind grep — fixed, PR #1410 merged | CLOSED | 22118 | 3097 |
| DEF-51 | Orphan row on `@email` path — PR #1407 merged | CLOSED | 20165 | 19244 |
| DEF-52 | CI `-race` flag missing — nightly race job, #1408 merged | CLOSED | 21251 | 19362 |
| DEF-53 | B10 guard skips failure case — Tranche G precondition | OPEN | 23773 | 19755 |
| DEF-54 | Server struct `channelRegistry`/`pluginManager` race — PR #1409 merged | CLOSED | 22073 | 19884 |
| DEF-55 | Orphan row via timeout between resolve and send — filed, not staffed | OPEN | 22073 | 20106 |
| DEF-56 | Reachability gate wired to CI — PR #1410 merged | CLOSED | 21453 | 20484 |
| DEF-57 | Broker inbound auth bypass — rescoped to cleanup, PR #1411 merged | CLOSED | 22119 | 20765 |
| DEF-58 | Negative gate needed: `brokerIdentityImpl` vs `UserIdentity`/`AgentIdentity` | OPEN | 26001 | 21316 |
| DEF-59 | `GetAgent` can't resolve slugs — tests pinned, fix not implemented | OPEN | 22654 | 22075 |
| DEF-60 | DEF-57 cleanup removed wrong explanation without leaving right one | FILED-NOT-STAFFED | 22096 | 22096 |
| DEF-61 | `filter.AgentID` stays raw after resolution — trap for DEF-59 fixer | FILED-NOT-STAFFED | 22655 | 22500 |
| DEF-62 | `parseUUID` inconsistency in same filter struct | FILED-NOT-STAFFED | 22655 | 22517 |
| DEF-63 | Divergence metric asymmetry — closed for `handleMessages` | CLOSED | 22623 | 22534 |
| DEF-64 | Read-switch narrows manager view — Tranche G blocker, pinned not fixed | OPEN | 25608 | 22733 |
| DEF-65 | Explicit `thread_id` silently becomes DM-scoped for project-less agents | FILED-NOT-STAFFED | 22890 | 22890 |
| DEF-66 | Consistency checker conflates routing disagreement with consistency failure | FILED-NOT-STAFFED | 23305 | 23091 |
| DEF-67 | S1 nil `webChatStore` short-circuits — `IncFallback` never called | FILED-NOT-STAFFED | 23842 | 23300 |
| DEF-68 | `divergenceCaveats` is runtime-mutable package-level var | FILED-NOT-STAFFED | 23843 | 23319 |
| DEF-69 | Caveat test drops non-string values | FILED-NOT-STAFFED | 23843 | 23332 |
| DEF-70 | PR (B) tests bypass mux — `guarded()` RouteHubAdmin check never exercised | OPEN | 23844 | 23344 |
| DEF-71 | Missing `canManage` positive control — verified closed | CLOSED | 23845 | 23734 |
| DEF-72 | Dual-outcome assertion on 5-part DM key parse cannot fail | FILED-NOT-STAFFED | 23845 | 23845 |
| DEF-73 | `IncFallback` false positives — divergence counter unreliable | FILED-NOT-STAFFED | 23849 | 23847 |
| DEF-74 | DEF-64 pins exercise admin-bypass only — no membership coverage | FILED-NOT-STAFFED | 24236 | 23907 |
| DEF-75 | Delta assertion accepts 0 and 1 regardless of response code | FILED-NOT-STAFFED | 24103 | 24067 |
| DEF-76 | S2 read-switch silently drops messages outside DM model | FILED-NOT-STAFFED | 24197 | 24106 |
| DEF-77 | Nine fragile `delta==0` assertions — switch-ON indistinguishable from OFF | FILED-NOT-STAFFED | 24737 | 24156 |
| DEF-78 | `handleAgentMessagesStream` has no read-switch block — REST/SSE paths diverge | FILED-NOT-STAFFED | 24177 | 24175 |
| DEF-79 | Characterisation test pins `found==false` — narrowing not proven sole cause | OPEN | 26001 | 24208 |
| DEF-80 | Divergence board caveat omits unbackfilled-history fail-open path | OPEN | 25503 | 24281 |
| DEF-81 | Backfill resume timestamp exclusion — PR #1426 merged | CLOSED | 25493 | 24501 |
| DEF-82 | No multi-project backfill coverage | FILED-NOT-STAFFED | 24707 | 24707 |
| DEF-83 | `Init()` fails on pre-existing DB — PR #1437 merged to main [^4] | CLOSED | 28402 | 26239 |
| DEF-84 | `maintenance_executors.go` relies on fetch-all-refs | FILED-NOT-STAFFED | 26654 | 26471 |
| DEF-85 | `RebuildServerExecutor` overwrites live binary with no stash/verify | FILED-NOT-STAFFED | 27540 | 27096 |
| DEF-86 | Rebuild control served by process it restarts — design limitation | FILED-NOT-STAFFED | 27457 | 27100 |
| DEF-87 | `github_pat_` credential in contrib-repo origin URL | FILED-NOT-STAFFED | 27285 | 27285 |
| DEF-88 | Pre-deploy snapshot incompatible with deploy mechanism — resolved via manual SSH | CLOSED | 27531 | 27429 |
| DEF-89 | New webchat topics are created with no `conversation_id`; backfill decays — SQLite fixed, Postgres in progress [^5] | OPEN | §5ke | 28560 |
| DEF-90 | `default-allow-ssh` exposes tcp:22 to `0.0.0.0/0` on ALL default-network instances — gteam remediated, fleet open [^6] | OPEN | 28900 | 28640 |

| DEF-91 | Agent SSH keys accumulate unboundedly in GCE **project** metadata — availability, not security [^7] | FILED-NOT-STAFFED | 29020 | 28960 |
| DEF-104 | Web-chat migration marker insert is not conflict-safe; a concurrent multi-replica cold start silently disables web chat on the losing replica [^20] | FILED-NOT-STAFFED | §5lf | §5lf |
| DEF-105 | `handleBrokerInbound` dispatches to the agent (~:293) before `CreateMessage` (:418), and swallows persist failure by design. Harmless today; once Phase 9b stamps real message/conversation IDs into the delivery text, a persist failure hands an agent identifiers for a row that never existed — dangling, not fabricated, but dereferences to nothing [^21] | FILED — declared gap for 9b; fix is a deliberate persist-before-dispatch reorder, out of scope for 9b | §5ll | §5ll |
| DEF-107 | Caller-supplied `conversation_id` path builds a `ConversationResult` with 2 of 5 fields, so the rendered envelope carries a real conversation id beside empty `kind` and `surface` [^23] | STAFFED — `ca-msg-9e` | §5lp | §5lp |
| DEF-108 | `handleGroupMessage` is a sixth hub send path and was never converted; with the envelope switch ON, `group[]` fan-out delivers the legacy format while all other paths deliver the new one [^24] | STAFFED — `ca-msg-9e`; **gates the gteam deploy** | §5lp | §5lp |
| DEF-109 | Scheduler delivery (`server.go:~2965`) and notification dispatch (`notifications.go:~391`) never stamp `DeliveryText`, so with the switch ON they deliver the legacy envelope into the same agent inbox as every converted path [^25] | STAFFED — `ca-msg-9f`; **holds the gteam deploy** | §5lr | §5lr |
| DEF-110 | Residual unstamped agent-delivery paths after Phase 9f: the two DELIVERY_FAILED error notifications, plus `reconcile.go:328` which dispatches a nil `StructuredMessage` and so has never carried an envelope at all [^26] | FILED-NOT-STAFFED — tracked residual, does **not** hold the deploy | §5ls | §5ls |
| DEF-111 | Backfill coverage gap: the boot warning counts unattributed messages hub-wide, but the remedy it advertises only processes messages in projects returned by `ListProjects`. Messages whose project was hard-deleted are counted forever and reachable never — 5,637 of 24,700 on gteam [^28] | FILED-NOT-STAFFED — addressed by reporting, not closure, in the migration auto-run design | MIGRATION-AUTORUN-DESIGN.md §3 F7, §4.6 | §5lu |
| DEF-112 | Two predicates express "unattributed": the counter tests `conversation_id IS NULL`, the backfill skip-guard tests `!= ""`. Nothing enforces their agreement [^29] | FILED-LATENT — measured 0 divergent rows on gteam; hardening only | MIGRATION-AUTORUN-DESIGN.md §3 F8 | §5lu |
| DEF-113 | `stepMergeOrRekeyEmptyRef` discards every parameter and only increments `EmptyRefSkipped` — correct B14 behaviour under a name, and beside two result counters, that advertise the ACL fabrication B14 forbids [^30] | FILED-NOT-STAFFED — scheduled as phase M8, explicitly not droppable | MIGRATION-AUTORUN-DESIGN.md §3 F10 | §5lv |
| DEF-114 | `groupForMessage` discards `deriveErr` and `Run` records the fixed string "key derivation failed", collapsing four distinct causes into one. 11,593 identical useless entries — the largest single population in the migration cannot be classified [^31] | FILED — scheduled as phase **M0**, blocks the marker policy and every scope decision | MIGRATION-AUTORUN-DESIGN.md §3 F12 | §5lw |
| DEF-115 | Backfill derives the conversation key from `ThreadID` but collects participants from the message's own sender/recipient, with nothing checking the principals are named in that key. Caught by `CheckDMParticipantKey` for `direct`; unguarded for `group` [^32] | FILED-NOT-STAFFED — needs M0's classification to size | MIGRATION-AUTORUN-DESIGN.md §3 F13 | §5lw |
| DEF-116 | `MarkMigrationComplete` accepts a migration name as a bare string; `setMarker`'s switch has no `default`, so an unrecognised name mutates nothing, persists an unchanged document, and returns `nil`. The caller believes a completion marker was written [^33] | FILED — fix requested from ca-mig-1 before M2 merges | MIGRATION-AUTORUN-DESIGN.md §4.2 | §5lx |
| DEF-117 | The `_migrations` document is read into a two-field struct and re-marshalled from that struct, so a read-modify-write by an older binary silently deletes any marker it does not have a field for [^34] | FILED — fix requested from ca-mig-1 before M2 merges | MIGRATION-AUTORUN-DESIGN.md §4.2 | §5lx |
| DEF-118 | `TestAdvisoryLockKeys_AllUnique` asserts `len(all) != expectedTotal` where both are literals in the test file. The assertion its own comment describes — catching a key added to `concurrency.go` but not to the list — cannot fire [^35] | FILED — fix requested from ca-mig-1 before M1 merges | MIGRATION-AUTORUN-DESIGN.md §4.7 | §5lx |
| DEF-119 | `BackfillResult.DeriveFailures` is populated per project, then neither merged in `mergeBackfillResults` nor printed by `printBackfillReport`. The DEF-114 classification is built, unit-tested, and discarded before an operator can see it [^36] | CLOSED — merged at `2912048b8` | MIGRATION-AUTORUN-DESIGN.md §3 F12 | §5ly, §5ma |
| DEF-120 | `HazardAEmailCount` is merged across projects and never printed, while `r.Inferred` is printed under the label `"Inferred (hazard-a)"`. Two production measurements were analysed against a counter that had never once been displayed [^37] | CLOSED — merged at `2912048b8` | MIGRATION-AUTORUN-DESIGN.md §3 F12 | §5ly, §5ma |
| DEF-121 | Message `eac1dd71` carries a DM key containing a literal newline inside a UUID (`…f0c5e96\n7`, length 37). Otherwise well-formed new-format key. Raises whether a *length-preserving* corruption exists elsewhere, which would pass UUID validation and yield a silently wrong ACL [^38] | FILED-NOT-STAFFED — needs a targeted scan, not a migration change | — | §5ly |
| DEF-122 | `resolveGroup` appends an agent-ref resolution failure to `result.Errors` and increments no counter, so `sum(DeriveFailures) + WriteFailures == len(Errors)` is false whenever `NormalizeAgentRef` fails with anything other than `ErrNotFound`/`ErrInvalidInput`. Never fired on gteam data, so every test reconciles [^39] | CLOSED — structural fix merged at `2912048b8`, mutation-verified | MIGRATION-AUTORUN-DESIGN.md §3 F11a | §5lz, §5ma |
| DEF-123 | The DEF-122 classification invariant is enforced by tests that exercise the three *existing* error paths, not by a guard on the source. A sixth call site appending to `result.Errors` directly would compile, pass, and silently undercount — the same decay mode DEF-122 was filed for, one generation on [^40] | FILED-NOT-STAFFED — low; hardening, no live impact | — | §5ma |
| DEF-124 | `stepRekeyOldFormat`'s refusal message reads *"found in neither or both tables"* and the counter is named `Ambiguous`. Those are two opposite conditions — deleted principals (permanent, correct, uninteresting) vs. a UUID existing as both an agent and a user (an integrity fault). Both IDs are also reported jointly, hiding the case where one resolves and the other does not. The `unparseable` counter likewise names no row [^41] | FILED — surfaced by the first live boot | MIGRATION-AUTORUN-DESIGN.md §4.4 | §5mf |
| DEF-125 | `DMMigrationResult.Unparseable` is incremented at five sites for at least three unrelated conditions, including `dm_migration.go:194`, where a **well-formed** key naming a principal that no longer exists is counted as a parse failure — silently, with no error appended and no row named. On the live corpus this produced `unparseable=1` that no one could attribute [^42] | FILED — surfaced by the first live boot | MIGRATION-AUTORUN-DESIGN.md §4.4 | §5mg |
| DEF-92 | Messaging switch getters swallow a malformed `messaging` doc silently — fail-closed but undiagnosable [^8] | **RESOLVED** in `683ba145`+`41f84ece` (tranche-g `45c440bd`) — `sectionState.Malformed` set at ingest; both syntactic malformation and type-incompatible documents logged once per refresh with section, revision and error | §5kc, §5lc, §5lh, §5lj | §5kc, §5lc |
| DEF-93 | Full `pkg/hub` suite takes ~346s with SQLite — exceeds common default tool timeouts, agents keep misreading the kill as a test failure [^9] | OPEN | §5ke | §5kc |
| DEF-94 | `test-full-suite` is `continue-on-error: true` — 3074/4187 `pkg/hub` tests, incl. the whole authz suite, sit behind a job that cannot fail the build [^10] | OPEN | §5ke | §5ke |
| DEF-95 | `internal/fixturegen.TestFixtureCoverage` red on `main` for ≥3 consecutive runs, invisible because of DEF-94 — `access_constraints` has no fixture row [^11] | OPEN | §5ke | §5ke |
| DEF-96 | `PromoteDM` creates no conversation row — same shape as DEF-89 but promotion is an ACL transition, NOT a repeat fix [^12] | OPEN | §5ke | §5ke |
| DEF-97 | Two implementation agents dispatched into one `shared-plain` working tree — near-loss of uncommitted work [^13] | CLOSED | §5ke | §5ke |
| DEF-98 | `tranche-g` calls `createProjectMembersGroupAndPolicy`; `main` renamed it — PR #1432's merge commit does not compile [^14] | OPEN | §5ke | §5ke |
| DEF-99 | `pgWebChatStore` — 1700 lines, 55 methods — has **zero** test coverage; no postgres test harness exists in the repo [^15] | OPEN | §5kf | §5kf |
| DEF-100 | Thread conversations are written with `external_ref=''` but read via derived `thread:<projectID>:<threadID>` — no group thread resolves with the read-switch ON [^16] | **CLOSED** — merged `85f25c1a1`, deployed to gteam, acceptance check 200 (was 409) | §5kp | §5kk |
| DEF-101 | New `DeliveryEnvelope` (conversation object on the receive side) is complete with a legacy adapter but has **zero** production callers — delivery still uses the legacy formatter [^17] | FILED-NOT-STAFFED | §5km | §5km |

## Counts by status

| Status | Count |
|---|---|
| CLOSED | 48 |
| OPEN | 28 |
| FILED-NOT-STAFFED | 22 |
| DEFERRED | 0 |
| UNKNOWN | 0 |
| **Total** | **99** |

## DEF-NN ids mentioned but never defined

None. All 101 ids (DEF-1 through DEF-101) have definitions in the journal.

## Notes and edge cases

[^1]: **DEF-2** (CLOSED): The journal says "CLOSED — but conditionally" (line 5442). The condition was that C5 wire the production caller for cross-project addressee validation. C5 was part of Tranche C, which merged. A later mention at line 16251 says "C5 is the FIRST production caller. Until it wires the call, AC-33/DEF-2 is unenforced in production." This was written before C5 completed; C5 subsequently merged as part of Tranche C. Classified CLOSED based on the journal's explicit marking and the merge of the conditioning tranche, but the conditional language warrants attention.

[^2]: **DEF-8** (OPEN): An extraction agent found Tranche A carrying "DEF-8 x4" commits that merged. However, the deferred-item ledger at line 5448 says "Gates the beta" and requires "spec reconciliation." The four Tranche A commits improved the resolver (e.g., populating ExternalRef) but the core issue — agent DMs exist as two disjoint rows requiring reconciliation — is not resolved. The reconciliation is unspecced. Classified OPEN because the structural defect persists.

[^3]: **DEF-24** (CLOSED): The journal says "WITHDRAWN — not a defect." WITHDRAWN is not in the allowed status vocabulary, so this is mapped to CLOSED. The stale cut point referenced was already fixed on the staging branch.

[^4]: **DEF-83** (CLOSED — reclassified by ca-msg-arch after extraction): The extraction correctly read OPEN from the journal as it stood: the fix was on `tranche-g` and deployed to the test VM, with `def83-main` awaiting merge. ptone merged that work as PR #1437 while extraction was in flight (journal §5jf, line 28402), and the merge was verified rather than assumed — the deleted `CREATE UNIQUE INDEX` is still created by the migration on both drivers, so fresh DBs keep the index. Now closed on main.

[^5]: **DEF-89** (FILED-NOT-STAFFED, filed after extraction began): `handlers_chat_v2.go:460-473` builds `WebChatTopic` with no `ConversationID`, so `CreateTopic`'s atomic dual-write branch — which is correct, transactional and tested — is unreachable from its only caller. Every new topic is therefore created with no conversation row. The consequence is that the one-time backfill is a decaying artifact: coverage falls as new topics accumulate, and flipping the read switch would 409 exactly the newest, most-used threads. Scoping decision (whether new topics get a conversation unconditionally) is with ptone.

[^6]: **DEF-90** (OPEN, filed after extraction began): scion-gteam has port 22 open to `0.0.0.0/0` with no fail2ban, no iptables rule and no GCP firewall restriction. Observed: 36 concurrent inbound SSH connections from external scanning ranges, saturating sshd's default `MaxStartups 10:30:100` and causing the intermittent connection failures that have been slowing every VM operation. Reachability is established by the inbound traffic itself, not by an outbound probe. **Rescoped 2026-08-31 (journal §5jk): this is project-level, not instance-level.** The exposing rule is `default-allow-ssh` — `0.0.0.0/0` on tcp:22, priority 65534, **with no target tags** — so it applies to every instance on the `default` network: at least fifteen scion hubs plus the GKE node pool. gteam is not special; it is merely the box we had a journal open on. Fix in progress with ptone is deliberately gteam-only for now (a tag-scoped allow/deny pair at priorities 900/1000, where adding the tag is the switch and removing it is the rollback), because one instance is the right size to prove a deny rule on. The fleet-wide decision — tag everything, or delete `default-allow-ssh` outright since `default-allow-internal` and `allow-iap-ssh` already cover the legitimate paths — is open and needs input the firewall listing cannot give: whether anyone reaches these boxes over SSH from a laptop or CI runner today. Note also that IAP is already permitted by firewall for tag `scion-hub`; the `4033` we hit was IAM, not firewall.

**gteam remediated 2026-08-31 ~13:00Z (journal §5jp), verified three ways:** tags read back as `https-server,scion-hub,ssh-no-internet` with nothing dropped; external SSH from ptone's workstation times out; IAP still returns the hostname. Measured effect from a container on scion-community: 40 attempts at 24 clean / 16 `Exceeded MaxStartups` before, 100 of 100 clean after. **The row stays OPEN because the fleet is not fixed** — `default-allow-ssh` still exposes tcp:22 to `0.0.0.0/0` on every other instance in the `default` network.

### Additional observations

- **DEF-55** (OPEN): The journal records it as "Filed, not staffed" at line 20106, which maps literally to the FILED-NOT-STAFFED status. However, it appears in tracked defect lists alongside actively-worked items at line 22073, suggesting ongoing awareness. Classified OPEN as the more conservative status.

- **DEF-12** (CLOSED — reclassified by ca-msg-arch after extraction): This was an extraction error, and an instructive one. The journal states plainly at line 25177, "**DEF-12 and DEF-81 are closed.**" The extraction picked up DEF-81 from that sentence but not DEF-12, because a *later* mention (line 26055) discusses DEF-12 in the present tense as part of the activation sequence. The summary text was then taken from line 20471, which predates closure by five thousand lines. **Last mention is not last status.** Any future refresh of this ledger should search for an explicit closure statement before falling back to the most recent mention.

- **Anchor precision**: Anchors represent the first substantive mention found in the journal. For defects first listed in status tables (e.g., Section 3 or §5de), the anchor may point to the table row rather than a full prose definition.

[^7]: **DEF-91** (OPEN, filed 2026-08-31, journal §5js): `gcloud compute ssh` generates a keypair on first use and pushes the public half into **project-level** GCE metadata. Every agent container that has ever run it has left one behind, all under the same local username `scion`, and nothing prunes them. instance-investigator counted **40+ `scion:ssh-rsa` entries**. Project metadata keys apply to every instance in the project that does not set `block-project-ssh-keys`, and the resulting login lands as uid 1002 in group `google-sudoers` — passwordless root on every box. The private halves live in containers that have since been deleted, but nothing guarantees they were only there. There is no inventory, no expiry (gcloud sets none unless `--ssh-key-expire-after` is passed), and no rotation. Not exploited as far as we know; the defect is that we could not tell if it had been. Related to but distinct from DEF-87 (`github_pat_` in contrib-repo remote URL): same class — a credential accumulating silently as a side effect of ordinary tooling.

**Downgraded 2026-08-31 by ptone's judgment, and I agree I overstated it (journal §5jv).** ptone: *"I'm not too worried about the ssh key debris in deleted investigator agent containers."* He is right. The private halves live in container filesystems that no longer exist; for a key to matter someone needs the private half, and "we cannot prove a negative about deleted disks" is true of nearly everything and is not a finding by itself. The security framing is withdrawn.

**What survives is an availability item with a slow fuse.** The metadata entries only grow: every new agent container that runs `gcloud compute ssh` adds one, nothing removes them, and project metadata has a hard size limit. The realistic failure is that some future agent's key push fails and SSH stops working for new containers — at a moment when nobody connects the outage to key debris. Cheap fix whenever convenient: `--ssh-key-expire-after` in whatever provisions the agents, so keys age out rather than accumulate. Not urgent.

[^8]: **DEF-92** (FILED-NOT-STAFFED, filed 2026-08-31 during the ca-msg-h2 review, journal §5kc): `OperationalSettings.ConversationReadSwitch()` and `ConversationWriteDenySwitch()` (`pkg/hub/operational_settings.go`, ~lines 1175-1215) both do `if err := json.Unmarshal(state.Value, &ms); err != nil { return false }` with **no log line**. The behaviour is correct — fail-closed, per the standing rule that an unreadable setting must never enable a behaviour — and h2 added tests that pin it. The defect is that it is fail-*silent*: a malformed `messaging` row makes `GET /api/v1/admin/messaging` report both switches OFF, which is indistinguishable from the switches genuinely being OFF, and nothing anywhere says the row could not be parsed. Severity is low because the only sanctioned write path is `Update`, which validates against the opsettings registry, so a malformed doc can realistically only arrive by direct DB edit or a future schema change. Deliberately **not** folded into h2: these getters are pre-existing on `tranche-g`, h2 did not touch them, and they are called per-request, so any logging needs to be rate-limited or moved to `Refresh` rather than dropped in the hot path. The fix belongs at parse-time in `Refresh`, not at read-time in the getter.

    **2026-09-02 — promoted from low-severity to a Phase 9a blocker (journal §5lc, design §4.6.2).** OQ-2b required the consolidated messaging switch to default **ON** when absent. Measurement showed all three switch getters (`ConversationReadSwitch` :1172, `ConversationWriteDenySwitch` :1195, `ProjectDefaultScratchpad` :1149) share one shape in which **section-absent, malformed-JSON and field-omitted all collapse to a single return of the compiled default**. Flipping that default to `true` therefore also flips the malformed-JSON branch, making an unreadable settings document *enable* a behaviour — a direct violation of the standing rule. Default-ON and fail-closed are not simultaneously expressible in the current shape, so DEF-92's fix is no longer optional polish: it is the mechanism that makes the cutover legal. Fix as scoped — parse once at `Refresh`, record `Malformed` on `sectionState`, log once per refresh — which also delivers the original diagnosability goal for free and applies to every section, not just `messaging`. Note the severity re-rating did not come from new evidence about DEF-92 itself; the defect is unchanged. A later requirement made an existing shape load-bearing.

[^9]: **DEF-93** (OPEN, filed 2026-08-31, journal §5kc; **mechanism corrected §5ke**): the full `pkg/hub` package suite, compiled *with* SQLite, takes **~346s**. Measured twice independently and in agreement on the direction: ca-msg-h2 got base `6f6228f6` 344.8s / branch `6ac1a50e` 351.8s on a quiet host; I got branch 346.5s / base 394.9s from separate worktrees on a host that was concurrently building for two other agents. My base figure is the noisy one and I am not treating the 48s spread as signal — the point both runs agree on is that **the suite is ~345-395s, both sides PASS, and the branch is not slower. The branch is exonerated.**

**As first filed this row said "at/near the 300s suite-level timeout." That was wrong and the correction matters.** There is no 300s gate anywhere — not in the Makefile, not in any workflow, and Go's own default is 600s. The 300s was self-imposed by each agent's own invocation, and *both* agents then read the resulting kill as a test failure and named whichever test the axe happened to land on (h2 blamed `TestQuotaConcurrency_100Creates_Limit10`, h1 blamed a `TelegramLinkService` goroutine; neither was at fault). CI does not hit this at all, because the blocking job runs `make test-fast` = `go test -tags no_sqlite ./...`, which compiles out the slow half.

So the defect is not "we are about to breach a cap." It is that **a 346s package suite exceeds the default timeout of the tools people actually drive it with, and the resulting failure is indistinguishable from a real one.** Two agents lost time to exactly this within an hour. Fix is to find and bound the slow tests. **Raising a cap remains prohibited** — and note that here there was no cap to raise, which is precisely why nobody noticed the suite had grown.
[^10]: **DEF-94** (OPEN, filed 2026-08-31, journal §5ke): `.github/workflows/ci.yml` has two test jobs. The blocking one, `build-and-test`, runs `make test-fast` = `go test -tags no_sqlite ./...`, which **compiles out** every file behind `//go:build !no_sqlite`. The one that does run the SQLite half, `test-full-suite`, is named *"Full Test Suite (reporting only)"* and carries **`continue-on-error: true`**. Its output is uploaded as an artifact with 14-day retention and has no other effect. The nightly `race-detection.yml` does not cover the gap either — it runs `pkg/hub` with `-tags no_sqlite` as well, and says so explicitly in its own step summary. Measured scope: **3074 of 4187 test functions in `pkg/hub` (73%) live in files carrying `!no_sqlite`**, including `authorize_test.go`, `authz_bypass_agents_test.go`, `authz_project_owner_test.go`, `authz_candelegate_test.go`, `audit_authz_test.go` and `delegation_ceiling_test.go` — that is, the entire behavioural authorization suite. A regression in any of them produces a green PR and a red artifact nobody opens. **Important scoping note, because I nearly filed this two sizes too large:** the tests are not *unrun*, and they are not currently *failing* — I pulled the artifact and every `pkg/hub` package line reads `ok`. The defect is structural and latent: the gate exists, reports, and cannot block. Note also that the *grep*-based gates on the prohibition list (`check-authz-guards.sh`, `compat-literals`) ARE blocking steps in `build-and-test` and are unaffected; it is the behavioural tests that cannot fail the build. Recommended fix is two-step and cannot be done in one move: **first** clear DEF-95, **then** flip `continue-on-error` to `false`. Runtime is not the obstacle — the job's own timeout is 30m and the whole suite is well inside it.

[^11]: **DEF-95** (OPEN, filed 2026-08-31, journal §5ke): `internal/fixturegen.TestFixtureCoverage` has been failing on `main` for at least the last three CI runs (`33395284766`, `33385514197`, `33384065369`, all with overall conclusion **success**). Cause is unambiguous and small: a domain table `access_constraints` was added without a corresponding fixture row, so the test sees 59 tables where it asserts exactly 58, and reports `every domain table must have at least one fixture row; missing: [access_constraints]`. Nothing to do with messaging — routed to `ci-fix-lead` under the standing CI-routing directive. **The interesting part is not the failure, it is that it was invisible.** It only surfaced because I went looking for something else, and I only found it by downloading the artifact — the run list, the job summary and the PR checks all show green. This is the concrete instance that makes DEF-94 worth fixing rather than merely noting: a non-blocking gate does not degrade gracefully into a warning, it degrades into silence. DEF-95 blocks DEF-94's second step.

[^12]: **DEF-96** (OPEN, filed 2026-08-31, journal §5ke): `PromoteDM` — in both stores, plus the handler at `handlers_chat_v2.go:2542` — builds a `WebChatTopic` with no `ConversationID`, so a promoted DM ends up with no conversation row. Structurally identical to DEF-89, and ca-msg-h1 correctly identified it as "the same defect." **It is not the same fix, and I told h1 not to touch it.** `CreateTopic` mints a conversation where none existed. `PromoteDM` promotes a DM that **already has** a `direct` conversation whose `external_ref` is the DM key — and the DM key IS the access-control basis. Minting a second `group` conversation with `external_ref=''` alongside it raises questions that must not be answered by pattern-matching: what becomes of the existing direct conversation; whether the promoted topic inherits the DM's participants or derives them from project membership; and consequently who can read the promoted history. **INVARIANT D-1** holds that a direct conversation's participant set is immutable for its lifetime, and the standing rule is that a DM `Conversation` must never become the authority for participant membership. A promotion is therefore a *conversation-kind transition with an ACL consequence*, and that is on my escalation list. Needs a design, not a three-line repeat. Explicitly blocked from being fixed in `scion/ca-msg-h1` or any branch until that design exists.

[^13]: **DEF-97** (CLOSED same day, filed 2026-08-31, journal §5ke): I dispatched `ca-msg-h1` and `ca-msg-h2` in parallel and assumed each had its own working tree. Both were `SCION_WORKSPACE_MODE=shared-plain` on `/scion-volumes/contrib-repo` — **the same tree**. When caught, that tree was checked out on h2's branch with h1's uncommitted Postgres fix sitting in it: one `git checkout` from h2 and the work was gone, silently. It also fully explains h1's earlier "before h2 was stripped" report, which I had flagged as a confused belief about branch state — h1 was reading the shared tree's HEAD and reporting it accurately. **The signal arrived as a nonsense sentence in a status report, and the right response to a nonsense sentence is to chase it rather than correct it.** Contained with no loss: h2 frozen on all git operations and acknowledged; h1 exported its work to `/tmp/h1-pg-fix.patch` (a patch file, deliberately **not** `git stash`, since the stash is shared too), then moved to a private `git worktree` at `/tmp/h1-tree` where HEAD and index are its own. Note the residual: worktrees isolate HEAD and index but **share refs**, so the never-force-push and never-touch-another-agent's-branch rules still apply. Closed as an incident; the standing correction is Rule 855.

[^14]: **DEF-98** (OPEN, filed 2026-08-31, journal §5ke): PR #1432 (`scion/tranche-g` → `main`, on GoogleCloudPlatform/scion) fails `golangci-lint` with `srv.createProjectMembersGroupAndPolicy undefined (typecheck)` at `handlers_broker_inbound_test.go:659`. **Not our change.** `main` renamed `createProjectMembersGroupAndPolicy` to `createProjectMembersGroup` (`handlers_projects_core.go:788`); `tranche-g` still calls the old name in several `!no_sqlite` test files. Each branch compiles on its own — which is why my local `go build` / `go vet` / `gofmt` gates on the branch were all clean and told me nothing. CI lints the **merge commit**, and only the merged state is broken. Rule 856. Fix is a rebase of `tranche-g` onto `main`, and it is **deliberately deferred**: rebasing now moves the SHA out from under the binary gteam is about to be tested on. Keep `tranche-g` frozen through QA, rebase before merge. Blocks merging, does not block testing. When the rebase is dispatched it needs the standing caution attached — a rename conflict is exactly where an agent "resolves" by reverting main's rename.

[^15]: **DEF-99** (OPEN, filed 2026-08-31, journal §5kf): `pkg/hub/webchannel_store_postgres.go` is **1700 lines implementing 55 methods of `pgWebChatStore`, and nothing tests any of it.** Verified by exclusion rather than assumed: no `testcontainers`, no `dockertest`, no `sqlmock`, no `postgres` build tag anywhere in the tree, and zero `_test.go` files referencing `pgWebChatStore`. (`events_postgres_test.go` exists but covers a different component.) `NewWebChatStore` dispatches on driver name at `webchannel_store.go:361`, so the two implementations are wholly separate code — a test against the SQLite store says nothing whatever about the Postgres one. Surfaced when ca-msg-h1 answered "no test asserts the Postgres legacy path" during the DEF-89 review; the true statement is considerably broader than the one it was asked for. **Consequence for Tranche G specifically:** DEF-89's Postgres fix ships unverified, and so does the pre-existing dual-write branch it makes reachable — every test we have written and every gate we have run for this work exercised the SQLite store. Severity depends on a fact I do not have and should not guess: `deploy/helm/scion-hub/values.yaml:447` defaults `driver: sqlite` and postgres reads as opt-in for multi-replica, so it is **not** established that postgres is the common deployment. Question queued for ptone rather than assumed — **I had already asserted "Postgres is the production driver" to h1 without checking, and retracted it.** Not to be fixed inside a DEF-89 branch: standing up a postgres test harness is a project, not a hunk.

[^16]: **DEF-100** (OPEN, filed 2026-09-01, journal §5kk): **The first defect found by real QA on gteam, and the most consequential so far.** The write side and the read side of thread conversations use different keys and have never agreed. Write: `CreateTopic` inserts `INSERT INTO conversations (... external_ref, parent_ref ...) VALUES (..., '', '', ...)` — a hardcoded empty `external_ref`. All **8** group-conversation INSERT sites do this, `backfillTopicConversations` included, on both the SQLite and Postgres stores. Read: with `ConversationReadSwitch` ON, `ResolveThreadConversationForRead` (`pkg/messaging/conversation.go:282`) calls `DeriveConversationKey({ThreadID, ProjectID})`, which for a non-DM thread returns `thread:<projectID>:<threadID>` (`derive_key.go` case 2), then does `GetConversationByExternalRef(ctx, "native", extRef)`. An empty stored ref cannot match a derived one, so `convResult` is nil and `handlers_chat_v2.go:1886` returns 409 `conversation_not_resolved`. **The conversation row exists and is correctly linked from `webchat_topic.conversation_id`; it is simply unreachable by the only lookup the read path performs.** Because `backfillTopicConversations` writes `''` too, this is predicted to affect every pre-existing thread, not only newly created ones — i.e. the read switch is non-functional for group conversations in general. Second observed symptom is the same root cause: an outbound message persists with channel+thread and no `conversation_id`, the read filter is now `conversation_id`-only, so the message is delivered to the agent but never renders in the web UI; agent delivery is unaffected because that path never consults `conversations`. **Review failure, mine.** h1's DEF-89 diff carried the comment *"The external_ref derivation (empty string) matches backfillTopicConversations"* and I accepted it as consistency with the backfill. Consistency with the backfill was the wrong test: it guaranteed the new rows were exactly as unresolvable as the old ones. The question I failed to ask is whether the read path can find what the write path stores. **Rule 867: when a write-side comment justifies a value by consistency with another writer, that is not a justification — check it against the reader.** Test gap that let it through: no test exercises create-topic → read-with-switch-ON end to end; read-switch tests seed conversations directly with well-formed external_refs, so they never observe what `CreateTopic` actually writes. **Rule 868: a test that seeds its own fixtures cannot detect a producer/consumer key mismatch; at least one test must obtain its fixture from the production writer.** **Diagnosis corrected after live measurement (§5kl).** Measured on gteam: group rows split 40 empty / 2 non-empty; ptone's topic `815deddb` links to conversation `dbd35ed2` with `external_ref=''`; the oldest backfilled topic (2026-08-17) is identical — confirming new and backfilled rows behave the same. The 2 non-empty refs read `thread:<projectID>:<threadID>`, exactly the derived format, and tracing them changed the conclusion. They are written by `ResolveOrCreateConversationByKey` (`derive_key.go:131`), which contains an explicit **topic-lookup intercept**: for `kind=="group"` with a `thread:` prefix it resolves through `GetTopicConversationIDIncludingDeleted` and returns the topic's `conversation_id` *without* minting an external_ref-keyed row — described in-tree as "the sink-level guard that prevents all paths from minting shadow conversations for native topics that already have a conversation." The 2 exceptions are its fall-through case: non-native surfaces (one is a Discord snowflake), which are supposed to carry a real external_ref. **Therefore the 8 writers are conforming to the design and the read path is the defect.** For native topics the `webchat_topic` row is deliberately the authority and the empty `external_ref` is intentional, not an oversight; `ResolveThreadConversationForRead` simply never implemented the matching intercept. **The earlier proposal to write derived refs at all 8 sites plus a migration is WITHDRAWN** — it would contradict an explicit design rule and buy a migration for nothing. **Recommended fix:** move the topic-lookup intercept into `ResolveThreadConversationForRead` itself so read and write share one resolution rule. That repairs both affected read sites at once, including `handlers_messages.go:306`, which performs no topic lookup at all today and so cannot be fixed at its call site. No migration, no DM-path change — the DM branch must remain untouched because there external_ref IS the ACL (DEF-29). Awaiting ptone's go-ahead to staff.

[^17]: **DEF-101** (FILED-NOT-STAFFED, filed 2026-09-01, journal §5km): Not a bug — unlanded work, filed here because this ledger is the tracker and the gap is otherwise invisible. Raised by ptone during QA asking whether he should expect a conversation id in the agent-facing message metadata. He should not, today. Two envelope formats exist. Production delivers the legacy `deliveryMessage` (`pkg/messages/format.go`): `timestamp, sender, recipients, msg, type, urgent, broadcasted, attachments, channel, thread_id, metadata` — no conversation field by construction. The replacement is `DeliveryEnvelope` (`pkg/messaging/delivery.go:37`), which carries not a conversation id but a conversation **object** — `conversation: {id, kind, surface, name, participants}` — plus `from`, `to[]`, `kind`, `intent`, `event`, `visibility`, `attachments`, `reply_to`. A transition adapter `FormatLegacyAsNewDelivery` (`delivery_compat.go:28`) already maps legacy `StructuredMessage` into it, including a `synthesizeConversationInfo` stub for messages predating the conversation model. **MEASURED: `FormatLegacyAsNewDelivery` has zero non-test callers, and `FormatNewDelivery`'s only caller is that adapter.** Production delivery still calls `messages.FormatForDelivery` at three sites: `cmd/server_dispatcher.go:261`, `cmd/server_dispatcher.go:271`, `pkg/runtimebroker/handlers.go:1711`. So the receive-side interface — the second of the two interfaces in the original brief, `scion message` for sending and structured messages for receiving — is written, tested and dark. Wiring it is a tranche of its own and a visible behaviour change for every agent on the hub, so it must be sequenced deliberately; it is explicitly NOT part of DEF-100 and the read-switch fix will not alter the envelope. Open question for that tranche: whether the cutover is a hub-wide flip, a per-agent opt-in, or driven off harness config, and what happens to agents whose prompts parse the legacy field names.

[^18]: **DEF-102** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ky): `synthesizeConversationInfo` (`pkg/messaging/delivery_compat.go:67`) **fabricates a `conversation.id` that is not a conversation id.** When a legacy `StructuredMessage` is adapted into a `DeliveryEnvelope` and no real conversation context is available, it sets `conv.ID = msg.Channel + "/" + msg.ThreadID` (or whichever of the two is present), and derives `conv.Kind` from a heuristic — `"group"` if `msg.Broadcasted || msg.Recipients != ""`, else `"direct"`. The envelope then presents that value to the agent under the key `conversation.id`, in a field the whole refactor exists to make authoritative. An agent that echoes it back, logs it, or uses it to address a reply is handling a string that matches no row in `conversations`. **The `kind` heuristic is the more dangerous half**: labelling a message `direct` because it happens to have one recipient asserts a conversation kind that carries ACL meaning elsewhere in this design, on the basis of a recipient count. Severity today is nil because the adapter has zero non-test callers (DEF-101) — this is pre-positioned wrongness, not live wrongness, and that is exactly the window in which it is cheap to fix. **Recommended resolution, to be settled in the Phase 9 design rather than patched here:** omit `conversation` entirely when there is no real conversation, rather than synthesising one. A missing field is honest; a fabricated identifier is not, and the standing rule that a wrong key is worse than no key applies with more force here than in the DM case, because unlike a DM key this one is handed to a model that will treat it as fact. Do not fix in isolation — the correct behaviour depends on whether Phase 9 keeps the legacy adapter at all, which is the same question as whether the cutover is hard (ptone confirmed hard, 2026-09-02) and therefore whether `FormatLegacyAsNewDelivery` should exist after Phase 13.

[^19]: **DEF-103** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5kz; resolution designed in `PHASE9-DESIGN.md` §4.5): **`MapLegacyEnvelope` puts a thread ID in `reply_to`.** At `pkg/messaging/envelope_compat.go:171-175` the conversion does `if old.ThreadID != "" { tid := old.ThreadID; replyToID = &tid }`, and `DeliveryEnvelope.ReplyTo` is declared `*string \`json:"reply_to,omitempty"\`` with the comment `// msg ID`. A thread identifier is not a message identifier, so an agent that follows `reply_to` follows a reference that resolves to nothing — and unlike an obviously-absent field, it reads as valid. Same class as DEF-102 but worse in kind: DEF-102 fabricates an identifier in a descriptive field, whereas this fabricates one in a field whose entire purpose is to point at another object. Found in the same read: the adapter also synthesises the message ID as `fmt.Sprintf("legacy-%s", old.Timestamp)` (`:167`), which is not the persisted row's ID and collides for two messages in the same second — so the compat path fabricates **three** identifiers in total (`conversation.id`, the message ID, and `reply_to`). **None of these are bugs in the adapter's logic.** They are the adapter accurately reporting that a `StructuredMessage` contains no message ID, no conversation ID and no reply target; the derivations are invented because the input is lossy. The correct fix is therefore not better derivation but a different input: render the envelope from the **persisted message row**, which by B11/B13 exists before publication and carries all three values for real. That is the §4.5 decision. Live severity is nil today — the adapter has zero non-test callers (DEF-101) — which per Rule 887 is the argument for fixing it now rather than later. **Also recorded, because it was mis-stated in earlier notes:** `MapLegacyEnvelope` never reads `old.Urgent` or `old.Broadcasted`, so both are silently discarded by the conversion. `Visibility` comes from `old.Visibility`, not from `Broadcasted`, and `TextIntent` is a closed three-value enum (`inform`/`request`/`question`) with no urgency slot. Losing `urgent` is a behaviour change, not a schema change, and nothing fails when it happens — tracked as OQ-1b in the Phase 9 design.

[^20]: **DEF-104** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5lf): **Postgres-only, multi-replica-only, and silent.** `markMigrationCompleted` (`pkg/hub/webchannel_store_postgres.go:1160`) is a bare `INSERT INTO webchat_migrations (name, completed_at) VALUES ($1, NOW())` against a table whose `name` column is `TEXT PRIMARY KEY` (`:117-120`). There is no `ON CONFLICT DO NOTHING`. The migration sequence is check → work → mark with no lock held: `pgWebChatStore.Init()` (`:39`) takes **no advisory lock**, and it is invoked from `cmd/server_foreground.go:597` during event-bus spoke setup, well after and outside the `pg_advisory_lock(LockSchemaMigration)` that guards Ent `AutoMigrate` (`server_foreground.go:1243`). So two replicas cold-starting together can both observe `migrationCompleted(name) == false`, both perform the work, and both attempt the marker insert. **One receives a primary-key unique violation.** That error is returned unwrapped up through the migration function and `runMigrations`, causing `Init()` to fail. The consequence is at `server_foreground.go:597-600`: `if err := webStore.Init(); err != nil { log.Printf("Warning: failed to initialize webchat store: %v", err) }` — and the `else` branch is where the web spoke is registered, `hubSrv.SetWebChatStore(webStore)` is called, and the attachment store is initialised. **A replica that loses this race runs for its entire lifetime with web chat disabled**, having emitted one unstructured `log.Printf` warning that no structured-log query will surface. `Init()` is called once at startup; there is no retry. The data is never corrupted — each topic is backfilled in its own transaction with a `WHERE conversation_id IS NULL` guard, so the loser's work simply rolls back — which is why the survey that found this classified the double-mark as benign. It is benign for the *data* and not for the *outcome*: the marker is not merely a performance optimisation, because failing to write it is fatal to `Init()`. Not reproducible on gteam (SQLite, single node), which is why it has never been seen. Two independent fixes, either sufficient, both cheap: make the insert `ON CONFLICT (name) DO NOTHING`, and/or run `runMigrations` under the existing `runWithAdvisoryLock` helper (`server_foreground.go:1280`) with a new key in the `pkg/store/concurrency.go:43` block — the primitive already exists and is used for three other startup tasks. Recommend both: the conflict clause fixes the marker, the lock stops N replicas duplicating the scan. **Separately and more importantly, this is a blocker for the migration-auto-run tranche**, which was about to adopt this pattern for migrations that touch DM keys. Swallowing a migration failure into a warning and continuing is tolerable for a chat rail and is not tolerable for an ACL migration; the auto-run design must choose its failure posture explicitly rather than inherit this one.
[^21]: Verified at tranche-g `45c440bd`. `pkg/hub/handlers_broker_inbound.go`: dispatch via `dispatchWithBrokerRetry` at ~:293, `s.store.CreateMessage` at :418 with the error branch commented "Non-fatal: the dispatch already succeeded, so the agent got the message." NOT a B11/B13 violation — `PublishUserMessage` sits at :424 inside the success branch, so event-bus publish still requires persistence. The invariant governs publishing, not agent dispatch.

[^22]: **DEF-106** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ln; fix directed into Phase 9c): **`Message.Validate()` has no production callers, so a check added to it enforces nothing.** Found while reviewing `ca-msg-9c`'s commit `9631014b`. Removing the fabricated `legacy-<ts>` message ID (DEF-103) meant `Message.ID` can now legitimately be empty on the pre-persistence path, so `validateMessageContent` could no longer delegate to `msg.Validate()` — which opens with `if m.ID == "" { return fmt.Errorf("message id is required") }` (`pkg/messaging/envelope.go:293`). The developer's response was to inline a copy of `Validate()`'s body minus that one check. The copy is faithful — verified line by line against `45c440bd`, no other check dropped — but it leaves **two copies of the same structural validation differing by one line**, and the original now has **zero non-test callers**: at that HEAD the only `.Validate()` invocations in `pkg/messaging` are `msg.Event.Validate()`, a different type. The string `"message id is required"` survives at exactly one site, inside the dead function, with no test asserting it. **Two consequences, and the second is the reason this is filed rather than just corrected.** First, the ID requirement is now unenforced and untested — low live risk, since a persisted row's ID is non-empty by construction. Second and durable: a future contributor who adds a structural check to `Message.Validate()` will believe they have hardened the type, and the check will never execute on any production path. The failure is silent, looks like work, and survives review because the function it was added to is the obvious place to add it. Directed fix (issued to `ca-msg-9c`, to land before its commit 2): extract `validateStructural()` holding one copy of the shared checks; `Validate()` becomes the ID check plus a call to it; `validateMessageContent` calls it directly and adds its body-size limits. `Validate()` is retained as the type's public contract with a comment recording that it is not currently on a production path. **Note the shape of the original defect, which is the more interesting finding:** the ID check had been passing on this path only because `MapLegacyEnvelope` fabricated a value for it. The gate was not validating the message, it was validating that the fabricator had run — so deleting the fabrication did not weaken a real check, it revealed that the check had been hollow since the compat path was written. A validation satisfied exclusively by synthesized input is indistinguishable from an absent validation, and only becomes visible when the synthesis is removed.

[^23]: **DEF-107** (STAFFED to `ca-msg-9e`, filed 2026-09-02, journal §5lp): Found by `cr-msg-9b` reviewing Phase 9b. `pkg/hub/handlers_agent_messaging.go:1013-1016`, the branch that honours a caller-supplied `conversation_id` after authorization, constructs `&messaging.ConversationResult{ConversationID: ..., ExternalRef: conv.ExternalRef}` — two of the type's five fields. The other three (`Kind`, `Surface`, `DisplayName`) are all present on `conv`, already loaded by `GetConversation` a few lines above, so the omission costs nothing to repair and buys nothing today. **The gap is pre-existing; what is new is that Phase 9b made those fields observable.** Before 9b nothing read them. Now `RenderDeliveryText` projects `ConversationResult` into `ConversationInfo`, and `ConversationInfo.Kind` and `.Surface` carry **no `omitempty`** (deliberately — they are non-optional in the envelope contract), so the agent receives `"conversation":{"id":"conv-xxx","kind":"","surface":""}`. This is the same class as §5lo's empty conversation object and arguably worse: there the whole object was blank and could be read as "no context"; here a genuine, resolvable conversation id sits next to two empty strings, which reads as *positive assertion* that the conversation has no kind and no surface. Every other `ConversationResult` construction site was enriched during 9b; this one was missed because it lives in `pkg/hub` rather than `pkg/messaging`, i.e. outside the file set the enrichment work was framed around. **Rule 950: when a change makes a previously-unread field observable, the audit is every site that constructs the type, not every site inside the package being changed.** Three-line fix, no extra query.

[^24]: **DEF-108** (STAFFED to `ca-msg-9e`, filed 2026-09-02, journal §5lp): Found by `cr-msg-9b`. `handleGroupMessage` (`pkg/hub/handlers_agent_messaging.go:1292`) — the `group[a,b,…]` fan-out handler, a sibling of `handleAgentMessage` rather than a path through it — dispatches at `:1424` via `dispatchWithBrokerRetry` and **never stamps `DeliveryText`**. Phase 9b converted nine send paths; this is the tenth and it was not in the brief's enumeration. With the envelope switch ON the fallback is safe in the narrow sense — `resolveDeliveryText` falls through to `messages.FormatForDelivery`, so nothing breaks — but the *outcome* is that agents receiving `group[]` messages get the **legacy** envelope while agents receiving every other kind of message get the new one. A split delivery format is precisely the hybrid state ptone has twice ruled out, and because the whole point of the gteam exercise is to test the true end state, this **gates the deploy and not merely the merge**. Two things worth recording beyond the fix. First, everything required to convert is already live inside the per-recipient loop: authorization at `:1338`, `convResult` declared `:1371` and populated `:1375` by `ResolveOrCreateDMConversation`, and the persisted `storeMsg` with its real ID and `CreatedAt` — so this is the *cheapest* of the paths to convert, not the most expensive. Second, the reviewer presented it as "convert now or defer", which implies a cost to weigh; there is none, and a deferral option offered without a cost estimate invites deferral on the strength of the framing. **Rule 951: an unconverted path reported with a deferral option must come with what conversion would actually cost, because otherwise the reviewer's framing, not the cost, decides.** Directed fix gates the stamp on persistence success (the `processMentions` pattern, `switch && persisted`) rather than the looser `broadcastDirect` pattern, since B11/B13 reasoning is already applied in that block.

[^25]: **DEF-109** (STAFFED to `ca-msg-9f`, filed 2026-09-02, journal §5lr): Found by sweeping every `FormatForDelivery`/`DeliveryText` site at `c98a2b521` rather than trusting the phase brief's enumeration — the same method that produced DEF-108, applied exhaustively. Ten stamp sites now exist and `FormatForDelivery` survives only as the switch-OFF fallback at `cmd/server_dispatcher.go:289` and `pkg/runtimebroker/handlers.go:1723`, which is correct. Two paths remain unstamped: the scheduler's message delivery (`pkg/hub/server.go:~2965`, building a system message via `messages.NewSystemMessage("scheduler", …)` with `SystemCategoryScheduler` and dispatching straight through `dispatchWithBrokerRetry`) and notification dispatch to a subscribing agent (`pkg/hub/notifications.go:~391`, same shape, carrying `Status`). Both therefore deliver the **legacy** envelope with the switch ON. `cr-msg-9b` observed both and classified them "system/operational paths, not user messaging paths," which is the reason they were not in 9b's scope. **That classification is rejected, and the rejection is the substance of this entry.** It is a statement about where the code path originates, not about what the recipient experiences: from the receiving agent's side there is exactly one inbox, and agent state-change notifications are frequent enough on gteam that a mixed stream would have been among the first things QA encountered — and would have been filed as a defect, correctly. Two paths that terminate in the same inbox cannot have different envelope formats regardless of what the sending path is called. **The fix is wiring, not design**, which is the other reason not to defer it: `pkg/messaging/envelope_compat.go` already maps `messages.TypeSystem → KindEvent` through `mapSystemCategory`, which carries an explicit `SystemCategoryScheduler` case, and maps `TypeAgentStateChanged → EventBody{Type: EventAgentStateChanged}`. The envelope machinery was built for precisely these message types and was simply never connected to them. Distinct from every 9e stamp in one respect: neither path has a persisted message row and neither resolves a conversation, so both pass `MessageID: ""` and `ConvResult: nil` — omit, never substitute, per the standing rule that an absent field is honest and a fabricated one is not. The wire consequence was checked rather than assumed: **`DeliveryEnvelope` declares no message-id field at all**, so an empty `MessageID` never appears in delivered JSON and reaches only `Addressee.MessageID`, which is not serialised. Holds the gteam deploy — not because the defect is severe, but because deploying without it would put a known format split in front of the end-state testing the deploy exists to enable.

[^26]: **DEF-110** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ls): Result of the post-9f sweep, run to establish that DEF-109 was the last of its kind rather than assuming it. Two categories remain, and they are deliberately **not** being fixed before the gteam deploy. **(a) The DELIVERY_FAILED error notifications** — `pkg/hub/messagebroker.go:892` and `pkg/hub/handlers_agent_messaging.go:1901`, both `dispatcher.DispatchAgentMessage(ctx, senderAgent, failMsg, false, structuredMsg)` — deliver the legacy envelope with the switch ON. Structurally identical to DEF-109 (`mapSystemCategory` already has a `SystemCategoryDeliveryFailed` case, so it is the same wiring gap) and differing only in frequency: these fire solely when a dispatch has already failed. Under the standing directive that tracked drift is acceptable where it does not dead-end the end state, a rare error-path format split is exactly the kind of residual worth recording and not worth a third deploy hold. **(b) `pkg/hub/reconcile.go:328`** is a different animal: `DispatchAgentMessage(ctx, agent, m.Msg, m.Urgent, nil)` passes a **nil** `StructuredMessage`, so the transport takes its pre-rendered-text fallback (`reqBody["message"] = message`, §3.3) and the agent receives bare text. This path never had an envelope, legacy or new, and is **unchanged by the cutover** — verified nil at the `45c440bd` base as well, so it is not a Phase 9 regression. Recorded because it will read as one to anyone who greps for unstamped dispatch sites after the fact, and because a redelivery path that formats differently from the original delivery is a genuine inconsistency on its own merits, predating this work. **Note the asymmetry in how these two were classified**, since it is the reusable part: (a) is a Phase 9 omission and will be finished, (b) is a pre-existing design gap that Phase 9 merely made visible. Conflating them would either inflate this tranche's scope or let a real omission hide inside a "pre-existing" label.

[^27]: **Cleanup, not a defect** (noted 2026-09-02, journal §5ls): the two Phase 9f stamps each parse `structuredMsg.Timestamp` with `time.Parse(time.RFC3339, …)` to populate `RenderDeliveryInput.CreatedAt`. `MapLegacyEnvelope` already parses that same field from that same struct at `envelope_compat.go:158`, falling back to `time.Now().UTC()` at `:160`, so the six lines produce exactly what omitting `CreatedAt` would have produced. Harmless and left in place. Recorded because of what it implies rather than what it costs: `CreatedAt` exists for callers holding a **persisted row**, where the DB timestamp is authoritative and strictly better than anything derivable from a formatted string. These two paths have no such source — they are re-parsing the mapper's own input. **A parameter whose meaning is "I have better data than you" should not be passed by a caller who does not**, because the next reader infers an authoritative timestamp source that is not there and builds on it.

[^28]: **DEF-111** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5lu): Found while designing migration auto-run, by comparing what the boot-time warning *counts* against what the command it recommends actually *processes* — not by reading either in isolation, which is why it survived both being written. `CountUnbackfilledMessages(ctx, "")` (`pkg/store/entadapter/message_store.go:511`) counts every message with a NULL `conversation_id` hub-wide. `scion server backfill` enumerates projects via `ListProjects` and runs `BackfillService` once per project (`cmd/server_backfill.go:96-133`), because `Run` hard-errors on an empty `ProjectID` (`pkg/messaging/backfill.go:99`). Any unattributed message outside that enumeration is therefore counted by the warning and unreachable by the remedy: the operator runs the advertised command, it exits successfully, and the identical warning returns on the next boot, permanently. **Measured on gteam rather than left hypothetical**: every unattributed message has a `project_id`, but 5,637 of the 24,700 point at a `project_id` with no row in `projects`. The `projects` table has no `deleted_at` column — it hard-deletes — so this is not a soft-delete artefact recoverable by widening the listing filter; the parent rows are gone and the orphans are permanent. **Pre-existing, but auto-run changes its character**, which is the reason to file rather than merely note it: today a human runs the backfill occasionally and may notice the number never moves, whereas auto-run makes the hub assert on every boot, in its own logs, that work remains which it has already proven it cannot do. The design's response is deliberately *not* to close the gap — the messages reference projects that no longer exist and there is no defensible conversation to attribute them to — but to split the report into a reachable bucket (WARN, actionable, moves toward zero) and an unreachable bucket (INFO, stable, expected). Reporting an unfixable condition at the same severity as a fixable one is what turns a warning into noise, and a permanently-noisy warning is worse than no warning, because it also hides the real one. Whether to purge the 5,637 is a separate and irreversible decision, raised as OQ-6 and explicitly reserved to ptone.

[^29]: **DEF-112** (FILED-LATENT, filed 2026-09-02, journal §5lu): The counter tests `message.ConversationIDIsNil()`; the backfill's idempotence guard tests `msg.ConversationID != ""` (`pkg/messaging/backfill.go:135`). These are different questions, and they coincide only so long as no write path ever stores an empty string where the schema permits NULL. **Measured before being asserted**: gteam holds 0 rows with `conversation_id = ''` against 24,700 NULL, so the predicates agree today and this is latent rather than live. Filed anyway, and the reason is the interaction rather than the divergence itself. In isolation, a future write path storing `''` would desynchronise the migration from its own progress counter — the backfill would skip the row as already attributed while the counter kept reporting it outstanding. The symptom would be a warning count that never reaches zero. But DEF-111 has just made "warning count never reaches zero" the *expected* steady state on any hub with orphaned messages, so the one observable signal this defect would produce is now indistinguishable from normal. That is what moves it from a tidiness item to something worth a ledger entry: its detectability was destroyed by an unrelated defect, and neither entry is readable without the other. Fix is one predicate shared by both call sites; scheduled last (phase M7) and explicitly droppable.

[^30]: **DEF-113** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5lv): Found by disbelieving the survey. `MIGRATION-SURVEY.md` states empty-ref rows are left untouched per B14, but the function is named `stepMergeOrRekeyEmptyRef` and `DMMigrationResult` declares `EmptyRefMerged` and `EmptyRefRekeyed` alongside `EmptyRefSkipped` — a name and two counters describing behaviour the prose said does not happen. Both could not be true, so I read the body: `pkg/messaging/dm_migration.go:108` takes a context, a conversation and a dry-run flag, binds all three to `_`, and does nothing but `result.EmptyRefSkipped++`. The comment states the B14 ruling correctly and completely. **The behaviour is right; the naming is a trap.** The two merge/rekey counters are unreachable — no code path can make them non-zero — so any report rendering them is showing a field that structurally cannot fire, and any reader of the result struct will conclude that empty-ref rows are sometimes merged or re-keyed. Filed as security-relevant rather than cosmetic because of the specific edit it invites: a future contributor tidying up "the unimplemented branch" would implement exactly the ACL fabrication B14 exists to prevent — deriving a direct conversation's key from its participant index, inverting the direction of authority so the listing index becomes the source of membership rather than the consequence of it. The only thing currently preventing that is a comment, and comments survive refactors less reliably than names do. **Auto-run raises the stakes rather than creating them**: today the dead counters appear in a CLI report a human reads once, after which they are gone; wired to boot they become a log line emitted on every startup forever, which is how a field that can never fire acquires the appearance of a field that simply has not fired yet. Fix is a rename to `stepSkipEmptyRef` plus deletion of the two dead fields, no behaviour change, and it is marked not-droppable precisely because its cost is trivial and its failure mode is not. Note the ordering that produced this: the survey was right about what the code does and wrong about nothing, yet reading only the survey would have carried the misleading names straight into the design's own vocabulary.

[^31]: **DEF-114** (FILED, scheduled M0, filed 2026-09-02, journal §5lw): Surfaced by a timing measurement, which is the part worth noting — I asked how long the backfill takes, and the answer (37s, fine) was the least important thing the run produced. The same run exited non-zero twice with **11,593 errors against 19,082 messages processed**, 61% of everything it touched. `DeriveConversationKey` (`pkg/messaging/derive_key.go:47`) distinguishes four failure causes and wraps each with a distinct message: a `dm:`-prefixed ThreadID that fails `ParseDMKey`; one that parses but is non-canonical; a thread key with an empty projectID; and failure to derive from the principal pair. Every one of those is then destroyed twice over — `groupForMessage` returns bare `nil` and drops `deriveErr` on the floor, and the caller at `pkg/messaging/backfill.go:140` appends the constant string `fmt.Sprintf("message %s: key derivation failed", msg.ID)`. The operator gets 11,593 identical lines carrying a message ID and no reason. **The consequence is not cosmetic: it makes the dominant population in the migration undecidable.** Whether those rows are permanently un-attributable legacy data or a fixable bug determines whether the backfill's real ceiling is 26% or something much higher, and nothing in the current output can tell you which. A second casualty of the same shape: `hazardA` is set *after* a successful derive, but non-UUID principals are exactly what makes case 3 fail — so the hazard-A counter structurally cannot count the population it exists to measure, and appeared to report 4 against a suspected legacy-principal population three orders of magnitude larger. **Correction, 2026-09-02 (§5ly): it reported nothing at all.** `HazardAEmailCount` is not printed by `printBackfillReport` and never has been; the 4 is `r.Inferred`, a different field, emitted under the label `"Inferred (hazard-a)"`. So the counter's population was not merely mis-measured, it was unobserved — and I asserted a value for it, in this footnote, while writing up a defect about a counter that cannot see what it exists to count. The reporting fault is now DEF-120. The underlying hazard-A logic fault described here stands; only the claim that we had seen its output is withdrawn. A classifier that returns nil before classifying is not a classifier. Scheduled as **M0**, ahead of everything else in the tranche, because INVARIANT M-1′ requires distinguishing run-level failure from row-level refusal and that distinction is currently unobservable. Fix is small: propagate the error.

[^32]: **DEF-115** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5lw): Found in the four execute-only errors that the dry-run did not produce. `groupForMessage` takes the conversation key from `msg.ThreadID` when one is present (derive cases 1 and 2) but *always* builds the participant set from the message's own `Sender`/`Recipient`. **Nothing checks that those principals are named in that key.** On real data this produced `adding participant user:7581ea89-… to 93ef83f1-…: invalid input: participant (user, 7581ea89-…) not named in direct conversation key` — `CheckDMParticipantKey`, the B1/D-1 guard, refusing to write a participant into a DM whose key does not name them. **The guard worked and the result is an under-grant, the recoverable direction.** It is filed anyway for two reasons. First, the backfill *attempted* an ACL-widening write on production-shaped data, and "the guard caught it" describes the last line of defence holding, not the inference being sound — a wrong key is worse than no key precisely because the key is the ACL, and the same reasoning applies to a wrong participant. Second, and this is the part that makes it a defect rather than a near-miss: `CheckDMParticipantKey` only applies to `direct` conversations, so the identical unchecked inference on a `thread:`-keyed **group** conversation is written with nothing objecting. For group rows the participant table is a listing index rather than the ACL, which caps the severity — but the code does not know that, and it is the conversation kind, not any deliberate check, that decides whether the mistake is caught. Sizing needs DEF-114's classification first: four visible rejections may be the whole population or may be the small direct-conversation tail of a much larger group-conversation one that produces no error at all.

[^33]: **DEF-116** (FILED, filed 2026-09-02, journal §5lx): Found reviewing M2. `MarkMigrationComplete(ctx, s, name string, residuals int)` dispatches through `setMarker`, a `switch name` over two literal cases with **no `default`**. Pass `"dm_key_migraton"` — one transposed letter — and the switch matches nothing, the document is persisted unchanged, and the function returns `nil`. The caller has no way to learn that the marker it just asked for does not exist. On the next boot `IsMigrationComplete` reports false and the migration runs again; it will do this on every boot forever, at roughly 37 seconds each, making no progress and logging no error. **This is INVARIANT M-1 — the livelock I had to remove from my own design last week — arriving through a different door.** The instructive part is that the shape recurred in code written by someone who had read the corrected invariant: M-1′ tells you when it is legitimate to write a marker, and says nothing about whether the write you asked for actually happened. An invariant about *policy* does not protect you from a *mechanism* that silently no-ops. The agent's `TestMigrationMarker_UnknownMigrationName` covers only `IsMigrationComplete`, the read path; the write path is the one that can lie. Fix requested: make the name a typed constant so a typo is a compile error, with an error return from `setMarker` as a backstop.

[^34]: **DEF-117** (FILED, filed 2026-09-02, journal §5lx): `loadMigrationsDoc` unmarshals the `_migrations` section into `migrationsDoc`, a struct with exactly two fields, and `persistMigrationsDoc` re-marshals **from that struct** and upserts the whole section unconditionally (`expectedRevision = -1`). Anything in the stored document that the struct has no field for is discarded on the round trip. So a newer binary that writes a third marker, followed by an older binary that boots and marks any one migration complete, loses the third marker with no error and no log line. **This is not a hypothetical rollback for us**: the beta exercise has `scion.known-good-85f25c1a` and `scion.known-good-e132380f` staged on the gteam VM precisely so we can downgrade under supervision, and ptone has said he may restore from a DB snapshot if it goes badly. A downgrade path that quietly erases migration state is the worst possible companion to that plan — the migration would silently re-run on data it had already processed, and the operator's evidence that it had completed would be gone. Compounding it, `TestMigrationMarker_MalformedDocOverwritten` currently reads as a blessing for the overwrite: it proves a garbage *scalar* can be replaced, which is correct and desirable, but it says nothing about a valid sibling key being destroyed, and its name invites the reader to conclude the overwrite semantics were considered and approved. Fix requested: decode into `map[string]json.RawMessage`, touch only owned keys, write the remainder back untouched — plus a test that a sibling key the struct has never heard of survives a write.

[^35]: **DEF-118** (FILED, filed 2026-09-02, journal §5lx): `TestAdvisoryLockKeys_AllUnique` hand-enumerates 22 keys, then guards against list drift with `const expectedTotal = 22; if len(all) != expectedTotal { t.Fatalf(... "update this test when adding a key") }`. **Both operands are literals in the test file.** Adding a key to `concurrency.go` changes neither, so the check can only fail if someone edits the test to make it fail. Its comment states plainly that it catches "a newly added key that is not added to this list"; it does not. The duplicate-detection half of the test is real and worth keeping — a second `AdvisoryLockKey` const with a colliding value is legal Go, and the collision would present as "the migration sometimes doesn't run", which is close to undiagnosable. The enumeration half is theatre. Worth recording as a defect and not merely a review nit because it is the same failure mode as DEF-114 and DEF-116 in a third costume: **a mechanism that reports on itself rather than on the thing it claims to watch.** I confirmed the current 22 values are in fact unique by extracting them from `concurrency.go` with a shell one-liner, which is also the fix — have the test read the source file from its own package directory and assert every declared key appears in the list.

[^36]: **DEF-119** (FILED, filed 2026-09-02, journal §5ly): DEF-114 added `BackfillResult.DeriveFailures`, a per-cause map, and it works — six tests cover it and they pass. `cmd/server_backfill.go` invokes `Run` once per project across 39 projects and combines the results in `mergeBackfillResults` (line ~225), which merges `Attributed`, `Inferred`, `Skipped`, `ConversationsCreated`, both hazard counters, the checkpoint and the error slice — and not `DeriveFailures`. `printBackfillReport` never mentions it. Every per-project map is therefore constructed, asserted against in unit tests, and dropped on the floor. Our investigator produced the classification table I asked for by **string-parsing the cause back out of the error list**, which only worked because DEF-114 had put it there. That is the tell: the data survived to the operator by accident, through a channel intended for humans, while the structured channel built for exactly this purpose was disconnected at the far end. The lesson for me is a scoping one — I briefed DEF-114 against `pkg/messaging` and reviewed `pkg/messaging`, and never asked whether the new field reached anyone. **A diagnostic that is not plumbed to an operator is not a diagnostic.** Same family as DEF-118: a mechanism whose self-consistency is verified while its connection to the outside world is not.

[^37]: **DEF-120** (FILED, filed 2026-09-02, journal §5ly): `printBackfillReport` emits `fmt.Fprintf(out, "  Inferred (hazard-a): %d\n", r.Inferred)`. It prints `r.Inferred`. `r.HazardAEmailCount` is summed at line 228 of the same file and then output by nothing — there is no line for it anywhere. Hazard-b, by contrast, gets a correctly named line of its own, which is what makes the omission read as an oversight rather than a convention. **Consequences, all of which actually happened rather than being hypothesised:** the number 4 was reported as hazard-a in two separate production-shaped runs; the investigator built a careful structural argument in §4 of its report explaining why 4 might be correct, reasoning about the wrong variable; and I recorded "duly reported 4" into DEF-114's footnote, in a defect whose entire subject is a counter that cannot observe its own population. Three independent readers, one label, none of us checked what the format string actually referenced. The fix is trivial. The reason it is filed rather than swept in as a nit is that a mislabelled counter is worse than a missing one — a missing number prompts someone to go find it, whereas a wrong number under a plausible name gets analysed, written down, and cited.

[^38]: **DEF-121** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ly): The lone `dm_key_parse` refusal in the entire 24,700-message corpus. Message `eac1dd71` holds a `dm:agent:<uuid>:user:<uuid>` key in which one UUID contains an embedded literal newline — `b53249ea-b8ce-4e75-99f1-883ec0f5e96\n7`, 37 characters. The key is otherwise new-format and well-shaped, so this row would attribute correctly if the stored value were repaired. As a single row it is a curiosity. **What makes it worth filing is the investigator's unprompted extension of it:** if the ingestion path could inject a stray byte into a stored UUID once, corruptions of a *length-preserving* kind would not announce themselves. A UUID truncated or mangled to exactly 36 valid characters passes `isValidUUID`, derives a key without complaint, and names the wrong principal — and because the key IS the ACL for a direct conversation, that is a silently wrong access-control decision rather than a refusal. Refusals are safe; this class is not, and it is invisible precisely because it does not fail. Wants a targeted scan for DM keys whose embedded UUIDs are well-formed but resolve to no existing user or agent. That is a read-only query, not a migration change, and it should not be folded into the backfill.

[^39]: **DEF-122** (FILED, filed 2026-09-02, journal §5lz): The repair for my own broken invariant reproduced the invariant's flaw. `backfill.go` appends to `result.Errors` at five sites; ca-mig-2 paired four of them with a counter and reported that this "makes the invariant exact." The fifth, at line ~307 inside `resolveGroup`, handles the `else` branch of `NormalizeAgentRef` — the case where resolution fails with something that is neither `ErrNotFound` nor `ErrInvalidInput`, i.e. a store or database error. It appends an error and classifies nothing. So `sum(DeriveFailures) + WriteFailures` silently undercounts `len(Errors)` on exactly the paths nobody has exercised. **This is the third generation of one mistake**: I wrote `sum == len(Errors)` and it was false by 4; the agent added `WriteFailures` and it is false again on a rarer path; and in each case every test passes, because the fixtures and the gteam corpus both happen to avoid the uncovered branch. The pattern is that the invariant is maintained *by discipline at each call site* rather than *by construction*, and discipline degrades every time someone adds a sixth site. Fix requested is structural rather than a fourth increment: make a classifying method the only way to record an error — `addDeriveFailure`/`addWriteFailure`/`addOtherFailure` — so that appending without classifying stops being expressible. Also note the third bucket is not merely bookkeeping: an agent-ref resolution failure against the store is an operator-visible fault that currently disappears into an unclassified error string.

[^40]: **DEF-123** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ma): The DEF-122 fix is real and I verified it by mutation rather than by reading — I added a compiling `r.Errors = append(...)` bypass on the `resolveGroup` path and two tests failed (`TestBackfill_ResolutionFailure_CountedSeparately`, `TestBackfill_MixedFailures_AllThreeBuckets`). So the invariant holds on all three paths that exist today, and it holds against someone *editing* one of them. **What it does not hold against is a seventh path being added.** `Errors` remains an exported field on an exported struct; the three classifying helpers are a convention that the current tests happen to pin, not a constraint the compiler enforces. The honest statement of the current state is not "true by construction" — which is what the agent reported and what I would have accepted a generation ago — but "true, and now defended at the three doors that exist." DEF-118's fix is the template for closing this properly: have a test read `backfill.go` from its own package directory and assert that every `Errors = append` occurrence lies inside one of the named helpers. That is the same technique, against the same failure mode, in the same tranche. Filed rather than round-tripped because this is a diagnostics path and not an ACL, the principal's standing directive is to keep forward progress, and the residual is drift-over-time rather than a live fault. Recording the reasoning so that whoever adds the seventh site inherits it.

[^41]: **DEF-124** (FILED, filed 2026-09-02, journal §5mf): The M4 boot hook's first live run on gteam scanned 8 direct conversations and re-keyed zero. The sole old-format row, `f003ad87`, was refused with `step3b: ambiguous kind resolution ... (found in neither or both tables)`. **The refusal is almost certainly correct — the defect is that the message cannot tell me why.** "Neither" means the principals were deleted: permanent, correct, and we stop thinking about it. "Both" means a UUID is simultaneously an agent and a user, which is an integrity fault worth chasing on its own. One string covers both, and the counter is called `Ambiguous`, which names the *code's* state of mind rather than the data's condition. The two IDs are reported jointly as well, so the case where one principal resolves and the other does not is invisible. `unparseable=1` has the same shape: a count with no row attached, on a corpus of 8, where naming the row would have cost nothing. **This is a constraint I had already written down and did not enforce** — the Tranche G evidence rules require `unresolvable` to be a separate bucket from `disagree` and from non-UUID principals, and I applied that to the evidence report while leaving the migration's own refusal path bucketed exactly the way I had prohibited. Same family as DEF-114, DEF-116, DEF-118 and DEF-119: a mechanism that reports on itself rather than on the thing it claims to watch. Cheap fix — split the branch, name the row, report per-principal.

[^42]: **DEF-125** (FILED, filed 2026-09-02, journal §5mg): The live boot reported `unparseable=1` across 8 conversations and the investigator could not determine which row it was — correctly reporting the ambiguity and two candidate hypotheses rather than choosing one. **Both hypotheses were wrong, and the reason the search could not converge is the defect.** Row dispatch is a `switch`, one branch per row, so `f003ad87` reached `Ambiguous` only and is not double-counted; and all six new-format keys are structurally valid. The actual site is `stepRebuildParticipants` at `:193`: `if !s.verifyPrincipal(ctx, kindA, idA) || !s.verifyPrincipal(ctx, kindB, idB) { result.Unparseable++; return }`. The key parsed perfectly. What failed is that a principal named by a well-formed key does not exist in its claimed table — a *referential integrity* condition, filed under a counter whose name asserts a *syntactic* one. Unlike its sibling at `:186`, this branch appends **no error**, so the row is counted and then made unnameable; there is no log line to grep. `Unparseable` now covers at least three unrelated conditions across five sites: unknown classification, genuine parse failure, and nonexistent principal. **This is Rule 971 with the volume turned up** — a mislabelled number is worse than a missing one, and this one is mislabelled *and* silent, so it cost an investigator a full pass to fail to identify a row in a corpus of eight. Note the refusal behaviour itself is correct and fail-closed: participants are not rebuilt for a conversation naming a principal that is gone. Only the accounting is wrong. Fix: split the counter by condition and name the row, same shape as DEF-124.

[^43]: **DEF-126** (STAFFED — `ca-msg-fix1`, filed 2026-09-02, journal §5nf): `scion message user:preston` delivered to the wrong account. The CLI sends the literal token; the hub resolves it at `handlers_agent_messaging.go:155` with `ListUsers(Search: "preston", Limit: 1)` and accepts the result when `len(result.Items) == 1`. **The cardinality check is tested against a result set that has already been truncated to satisfy it.** Seven layers cooperate: the caller passes `Limit: 1`; the store sets `limit = 1` (`user_store.go:290`); SQL asks for `LIMIT limit+1` = 2 (`:323`); two rows come back; `result.Items = items` (`:336`); the store truncates `items[:1]` (`:339-340`); the caller sees exactly one and concludes the name was unambiguous. Two accounts match `preston` — `7581ea89` (gmail) and `b53249ea` (google.com), both ptone's. The store even computes the honest cardinality: `TotalCount` at `:337` comes from a pre-`LIMIT` count at `:285` and is discarded by the caller. The match is a case-insensitive **substring** over both `email` and `display_name`, and `display_name` has no uniqueness constraint of any kind. Clone at `handleGroupMessage:1494-1504`. Pre-existing since `18034299` (Apr 7) — **not** a tranche-g regression, so the atomic cut-over property is unaffected. **Two corrections I had to make to my own account of it.** First, I framed it as non-determinism on the strength of an investigator's no-`ORDER BY` finding; that came from a hand-written query, and the real one carries `ORDER BY created DESC` (`sql.go:411-425`, no dialect switch). It is fully deterministic, which is *worse* — a stable wrong answer produces no intermittent symptom to notice (Rule 1036). Second, I assumed which of the two accounts was ptone's and read his confirmation through that label; he was operating as google.com and the message landed on gmail. **The resolver picked a different principal than the sender named, and only the coincidence that both accounts belong to the same human prevented disclosure** (Rules 1033, 1034). Fix is P1/P2/P3 of `ADDRESSING-SPEC.md`: take cardinality from `TotalCount` before truncation, resolve `user:<token>` by UUID or exact email only, and refuse with a typed `ADDR_UNKNOWN`/`ADDR_AMBIGUOUS` rather than guessing.

[^44]: **DEF-127** (STAFFED — `ca-msg-fix2`, filed 2026-09-02, journal §5ng): Opening a DM conversation that has never been used renders a user-facing error. `handleConversationHistory` authorizes the key, fails to resolve a conversation row, and returns **409 `ErrCodeConversationNotResolved`** — "the read-switch is ON but no matching conversation record exists" (`handlers_chat_v2.go:1911-1916`), with sibling sites at `handlers_messages.go:85-89` and `:321-335`. An empty inbox is indistinguishable from data corruption, and the message points the reader at the switch. The design decision is empty-200 plus a counter, and it is safe for a specific reason worth recording: authorization on that path is **key-based and runs before resolution** — `validDMKey(key)` then `isDMParticipant(key, user.ID())` at `:1823-1833` — so the caller is already proven to be one of the two principals the key names, with no conversation row consulted. Returning empty involves no authorization change and no ACL inference. Two things stay forbidden: the read must not mint the conversation row (a GET must not create a record whose `external_ref` is an access-control basis), and the client must not be taught to treat 409 as empty, which would suppress genuine drift at the one layer nobody inspects. **`handlers_messages.go:316-319` keeps its 409** — that is the `thread_id` path, threads are created by the topic system, and absence there really is drift. I looked for a cheap discriminator between "never existed" and "existed and drifted" and there is none: no index covers `(channel, thread_id)`. That is why the observability moves into a counter instead of staying a user-facing error.

[^45]: **DEF-127a** (STAFFED — `ca-msg-fix2`, filed 2026-09-02, journal §5ng): `ResolveDMConversationForRead` collapses `store.ErrNotFound` and every other error into `nil` (`conversation.go:205-209`), logging the real one at **Debug** (`:207`). A database outage therefore renders to the user as DEF-127's "no matching conversation record exists" — an availability fault wearing the costume of data corruption, at precisely the moment an operator is trying to work out what is wrong. **Rule 1037**: an error path that collapses "not found" into "failed" is a misdiagnosis generator. Absence and failure have different remedies and must not share a status code or a message. Absence is 200-empty; failure is 500, logged at error.

[^46]: **DEF-128** (STAFFED — `ca-msg-fix2`, filed 2026-09-02, journal §5nh): The most serious defect of this pass, and it exists only because DEF-127 supplies the trigger. Chain: open an agent detail page for an agent you have never messaged → hub returns 409 → `agent-message-viewer.ts` `fetchMessages` checks `hubRes.ok` (`:417-418`) and, on any non-OK, silently falls through (`:428`) to a Cloud Logging fallback → `handleAgentMessageLogs` (`handlers_logs.go:281`) builds its filter at `logquery.go:179-185` as `logName=scion-messages` AND `(recipient_id OR sender_id = agentID)` AND `project_id`, **with no participant constraint** → the viewer renders every user's messages with that agent. The hub-store path applies `filter.ParticipantID = user.ID()` for non-manage users (`handlers_messages.go:258-260`); the fallback does not. Same page, same component: your own messages when the hub succeeds, everyone's when it fails. **Rule 1038** — the security question about a fallback is never "is it authorized" but "is it authorized *identically*". The fallback's own checks are individually sound (`checkAgentReadScope`, project isolation, RBAC `ActionRead`); it is one missing dimension, not a missing check. Note the second defect inside the first: a 4xx is the hub **answering**, and treating an answer as a failure is what turns a cosmetic error into a privacy fault. **Fix order is deliberate and must not be reversed** — stop the 4xx fallback, then participant-scope the log filter, then fix DEF-127 last. Fixing the trigger first makes the exposure invisible while leaving it in place, which is how a finding gets closed without being resolved. Exposure denominator was not resolvable from source alone: only `Role() == "admin"` bypasses project membership, so it is 1 + admin-role holders + any of the 12 in `a3083e98`'s member set.

[^47]: **DEF-129** (**RETRACTED** 2026-09-02, journal §5ni): Filed as "direct messages misfiled under group conversations, presently readable by any project member", escalated to the principal as a live disclosure, and **wrong**. All 8 flagged messages carry `thread_id` equal to their conversation's topic key (`f1b450c9` general, `815deddb` new-thread). They are channel messages — a user typed in a webchat topic, an agent replied in the same topic — correctly filed through the intended path. The discriminator cuts both ways: none of the 13 correctly-filed DMs in the same project carries a topic key as `thread_id`, and all 8 of the "misfiled" ones do. Confirmed independently by measurement and by source. **The error was mine and it is narrow:** messages sitting in a group conversation between two principals who also DM each other are equally consistent with misplaced DMs and with ordinary channel traffic, and the field that separates them is one I did not ask for until I was chasing something else. **Rule 1043** — before escalating that data is in the wrong place, state what it would look like in the right place and confirm the observation distinguishes them; "this looks misfiled" is a hypothesis about intent, and intent is not in the row. Retained rather than deleted because the retraction was cheap for a reason worth preserving: `ca-msg-misfile` was dispatched to *falsify*, was deliberately not shown the shadow-check evidence that pointed the same way (Rule 1035), and instance-investigator was under standing orders not to remediate — so no rows were moved and no clean before-state was spent. **Rule 1045** — the value of a falsification dispatch is not that it finds bugs, it is that it makes being wrong recoverable. What survives unaffected, because it came from source rather than from these rows: the read filter really is `Channel:"web"` + `ConversationID` and nothing else (`handlers_chat_v2.go:1900-1908` → `message_store.go:339-344`), nothing cross-checks a message's own principals against the conversation it claims, and `ValidateAttributed` (`validate.go:84`) is a non-empty check on the write path only. **Rule 1040 stands, correctly re-sized from realised to latent.**

[^48]: **DEF-130** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5ni): The residual real defect extracted from DEF-129's retraction. `CheckConversationConsistency` (`divergence.go:336-437`) pairs a message with actual stored prior messages for the same sender/recipient pair and compares their stored `conversation_id` against the freshly resolved one — **without regard to the surface the message was sent on**. Any two principals who both DM each other and talk in a shared channel will therefore trip it on every channel message, permanently. 16 WARNs a day on gteam, all false positives. **Rule 1044** — an alarm that cannot distinguish two legitimate surfaces is not evidence about either; establish what a check *ignores* before treating what it reports as a finding. Filed with unusual emphasis on the cost: this check consumed three agents and roughly a day, and the next person to look at its output will start from the same place unless the retraction is findable from the warning. Note it is a genuinely separate mechanism from `LogDivergence` (`:199-222`), the actual shadow comparison, which reported 17 match / 0 divergence / 0 fallback over the same 24 hours. Two checks, similar names, similar fields, opposite meanings — and I dismissed the second as noise from the first (Rule 1039). Fix is to include the message's surface in the pairing key, or to exclude messages carrying a topic key as `thread_id`. Not staffed: it is an observability fault with no user-visible effect, and the fix ordering behind DEF-126/127/128 is deliberate.

[^49]: **DEF-131** (FILED, filed 2026-09-02, journal §5nj): The new agent-facing delivery envelope is gated behind `s.writeDenyEnabled()` (`pkg/hub/server.go:2162-2164`), a function whose body is `return ops != nil && ops.ConversationEnvelopeSwitch()`. **The name and the setting are unrelated.** Ten hub call sites depend on it, including the external-channel inbound path at `handlers_broker_inbound.go:305`, whose own comment says "when the envelope switch is ON" while the code reads a function named for write-denial. Behaviour today is correct; the hazard is what the name invites. There is no genuine write-deny predicate on `Server`, so the name is unclaimed and looks available — a future contributor implementing real write-denial will find an existing, plausibly-named, correctly-shaped helper and wire to it, at which point enabling write-deny silently flips the delivery envelope for every agent on the hub, and the setting that appears to control the envelope stops controlling it. **This is the first defect in this project where the failure mode is a name being attractive rather than a name being wrong.** I had it recorded as a truth-in-labelling one-liner alongside three cosmetic renames; that classification was wrong and is corrected here. Fix is a rename to `conversationEnvelopeEnabled` plus a test asserting the gate reads `ConversationEnvelopeSwitch` — trivial, and it must not be dropped as a nit. Discovered because ptone reported Discord messages arriving in the old format after a correct rebuild and restart; the switch was the answer, and it took a source read to find because the name gives no clue.

[^50]: **DEF-101 STALENESS CORRECTION** (2026-09-02, journal §5nj): DEF-101 [^17] records that the new envelope is "written, tested and dark — `FormatLegacyAsNewDelivery` has zero non-test callers", and that production delivery still calls `messages.FormatForDelivery` at three sites. **That was measured on 2026-09-01 and is now false.** At `97c3462ab` the wiring has landed: `RenderDeliveryText` is called from ten hub sites (`notifications.go:397`, `messagebroker.go:732`, `handlers_chat_v2.go:1261` and `:1370`, `server.go:2972`, `handlers_broker_inbound.go:305`, `handlers_agent_messaging.go:1130`, `:1435`, `:1850`, `:2019`), and `runtimebroker/handlers.go:1715-1723` implements a four-step precedence that prefers a hub-rendered `DeliveryText` and treats `FormatForDelivery` as the legacy fallback. The receive-side interface is wired and switch-gated, not dark. **The correction matters less than how close I came to not making it.** Asked why Discord messages looked unchanged, I had a filed, detailed, confidently-worded ledger entry that answered the question — "the work is unlanded" — and it was wrong. Acting on it would have sent a developer to build something that already existed, and the note's specificity is exactly what would have made that feel safe. **Rule 1046** — a defect ledger is a record of what was true when it was written, not a description of the tree. Any entry asserting "no callers", "not wired" or "does not exist" has a shelf life measured in days on an active branch, and must be re-measured before it is reasoned from. Entries of that shape should carry the commit they were measured at; DEF-101 did not, and neither did most of this file.

[^51]: **NOT-A-DEFECT — envelope "old format" report, resolved to a measurement disagreement** (2026-09-02, journal §5nk): Recorded because the reasoning failure is worth more than the outcome. ptone reported Discord messages arriving in the old envelope after correctly updating and restarting the integration. I found the envelope gated behind `s.writeDenyEnabled()` → `ConversationEnvelopeSwitch()` (DEF-131), and **told him the switch was off. I had not read the switch.** I reasoned backwards from a symptom to a plausible gate and reported the inference as a measured fact, in the same message where I criticised a stale ledger entry for exactly that class of error. The switch is ON — the key is absent from the settings doc and the compiled default is ON (`operational_settings.go:1225-1248`, covered by `TestConversationEnvelopeSwitch_AC97_KeyOmitted`). Subsequent measurement of the actual delivered text to agent `smith` for the 19:49:34 message returned key names `timestamp, from, to, kind, intent, msg, visibility` with `sender`, `type`, `channel` and `thread_id` **absent** — unambiguously the new envelope. `conversation` is absent because the Discord plugin sets no Surface/ExternalRef, so `preDispatchConvResult` is nil and §4.3 omits the key honestly rather than fabricating it; that is correct behaviour and was briefly mistaken by the receiving agent for a rendering fault. The gate passed, the render ran, `delivery_text` carried to the broker. **The chain I read from source was right at every link, and I distrusted it because a human report contradicted it.** Two lessons. **Rule 1047** — a plausible gate discovered while searching for a cause is a hypothesis, not a finding; naming a switch as the culprit without reading its value is the same error as trusting a stale ledger, and being able to point at code makes it more persuasive rather than more true. **Rule 1048** — when a measurement and a user report disagree, the unexamined variable is usually *which system the user was looking at*. I am on `community.projects.scion-ai.dev` and receive old-format envelopes correctly, because community is not on tranche-g; gteam is. Resolution is pending one question to ptone — which hub, and which direction — asked rather than guessed, explicitly because the DEF-129 retraction cost a day to a classification I accepted without confirming what the innocent case looks like. Note the asymmetry that made this cheap: nothing was flipped, no settings were changed, and the only expenditure was reading.

[^52]: **DEF-132** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5nl): `buildPrincipalRef` (`pkg/messaging/envelope_compat.go:224-226`) derives a principal kind from a recipient string by inspecting its prefix. Given a colonless recipient — `@target-agent`, or any bare token the grammar does not cover — it cannot derive a kind, and **it does not refuse. It fabricates one**, returning `PrincipalRef("system:" + name)`. An address the system could not parse silently becomes a *system* principal, the most privileged-sounding kind in the grammar. Today this is inert: addressee validation at `envelope.go:363` rejects any kind that is neither user nor agent, which is how the condition surfaced at all — `principal_kind must be user or agent, got "system"`, measured on base at `97c3462ab`. **The safety is downstream, incidental, and not local to the decision.** Nothing at the derivation site objects to inventing a kind; the only thing standing between an unparseable address and a system principal is a validator in a different file that happens to enumerate an allowlist. The day any path legitimately accepts `system:` principals — a service message, an internal notification, an audit or lifecycle event, all of which are plausible and none of which are far-fetched — every unparseable address in the system becomes one, and the change that enables it will look local and safe to whoever makes it. **A kind derivation that cannot determine a kind must refuse; guessing is wrong and guessing upward in privilege is the worst available default.** Same family as DEF-131 (correct behaviour, dangerous construction) and the same shape as the standing rule that parse failure denies, always, everywhere on the derivation path — a rule written for DM keys and evidently not generalised to principal refs. **Rule 1049** — a defaulted value in a security-relevant classifier is a decision, not a fallback; if the safe form of that decision is "refuse", then any default other than refusal is a latent grant awaiting a downstream change. Found by `ca-msg-fix1` while measuring an unrelated claim, which is the second time today that requiring a measurement instead of accepting "pre-existing" has produced a better defect than the one being chased.

[^53]: **ADDRESSING-SPEC form-3 correction** (2026-09-02, journal §5nl): The nine-form table asserted `@<agent>` was "working, exact". `ca-msg-fix1` measured it failing on base and concluded the row was wrong; I checked before correcting, and **both the row and that conclusion were wrong in different directions.** `@<agent>` works through the CLI — `cmd/message.go:150` calls `messaging.ParseReference` before any legacy handling, `resolve.go:76-85` returns `RefAgent`, and the `:154` guard rejects only `RefConversation` and `RefThread`, so the form never reaches the legacy resolver. It fails only on the legacy envelope path, via DEF-132. **The table's fault was structural rather than factual: it has no path column.** Every row describes a form as though it has one behaviour, when a form has one behaviour per entry point, and all nine rows were written from the hub-side resolver on the tacit assumption that the CLI is a transparent pass-through. That assumption is now known false for row 3 and **unverified for the other eight**. The row is annotated rather than deleted, because deleting it would tell the next reader `@agent` does not work — a second wrong answer replacing the first. **Rule 1050** — a claim that a form works or fails is incomplete without the entry point it was measured at; "form X is broken" generalised from one path is a scope claim, and scope claims need measuring too. Recording this against myself: the table is my deliverable, downstream phases resolve against it, and it shipped with an assumption I never stated and never tested.

[^54]: **DEF-133** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5nn): **The delivery envelope was migrated to the new vocabulary; the REST API DTO was not.** `GET /api/v1/agents/<id>/messages` on gteam at `43be4baad` returns per-message keys `agentId, channel, conversationId, createdAt, dispatchState, groveId, id, msg, projectId, read, recipient, recipientId, sender, senderId, type, visibility` — measured, not inferred. The agent-facing envelope for the same message returns `timestamp, from, to, kind, intent, msg`. **`sender`/`recipient`/`type`/`channel` versus `from`/`to`/`kind` is the exact old-vs-new discriminator this project defined**, and both are live on the same hub for the same row. The web UI agent detail page renders the DTO, so a user watching that page after a Discord send sees the old field names and correctly reports "old envelope form" — which is what ptone did, twice, and which I twice scoped away as a delivery-path question. **The report was accurate and my scoping was wrong both times.** The project brief is "two agent-level messaging interfaces with one clear, crisp, coherent semantic contract"; a hub whose API says `sender` while its envelope says `from` does not satisfy that, regardless of which surface is the agent-facing one. Filing rather than waving off, because the alternative is shipping a refactor whose stated goal is vocabulary coherence with the most user-visible surface still speaking the old vocabulary. Not yet scoped: the DTO is consumed by `web/src/` and by any external API client, so this is a compatibility decision (dual-emit then drop, versus hard rename in line with the Q2 hard-cutover ruling) and not a mechanical rename. **The general lesson is Rule 1048 charged twice: when a user reports the old format and a measurement says new, the unexamined variable was which surface, and I had already been told the answer once.**

[^55]: **DEF-134** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5nn): **One human with two accounts holds two separate DM conversations with the same agent, and each is invisible from the other login.** Measured on gteam for agent `c9c1123b` (= `smith`): `f92dc5cf` is the DM with ptone's gmail identity (10 messages), `b2fd01b6` is the DM with his google.com identity (7 messages, holding all the Discord traffic). Seven conversations, 45 messages total for that agent. The web UI shows only the conversation whose participant matches the authenticated login, with **no indication the others exist** — so the agent detail page can, and did, show a conversation that does not contain the messages the user just sent, while reporting no error. This is the display-side consequence of the same root cause as DEF-126: the system has no notion that two principals are one person, and nothing in the model wants one. **Note what makes it worse than a missing-messages bug: the page is not empty and does not fail.** It shows a plausible, populated, correct-looking conversation. There is no signal to distinguish "your messages are elsewhere" from "your messages were lost", which is precisely the ambiguity that consumed a debugging cycle here. Any fix must be handled with the standing constraint that under-granting is recoverable and over-granting is not — merging conversations, or showing one identity's DMs to another session, is a **disclosure**, not a UX improvement, and DEF-32's `(issuer, subject) → user_id` link table is the only sanctioned path to establishing that two principals are the same person. The minimum safe increment is therefore not a merge but a *disclosure-free* signal: the UI knowing that other conversations exist for this agent under principals it cannot show. Caveat on the measurement: instance-investigator ran M3 as the gmail identity while ptone stated he was logged in as google.com. The two-conversation split is identity-independent and holds regardless; which specific conversation his browser rendered is indicative pending confirmation. **Rule 1033/1034 recurring for the third time — assuming which of two identities is in play has now cost something on every occasion it has happened, and it keeps happening because both identities belong to the same person and the transcript rarely says which one.**

[^56]: **DEF-134 RETRACTED — ruled not-a-defect by ptone** (2026-09-02, journal §5no): Filed hours earlier [^55] as "one human with two accounts holds two separate DM conversations with the same agent, and each is invisible from the other login." ptone's ruling, verbatim: *"i don't expect scion to support two email based logins as a single 'user'. i can have two independent user accounts on the system."* **Two independent accounts is the intended model, so a conversation per account is correct behaviour and the UI showing only the authenticated principal's conversation is correct too.** The measurement stands and remains useful — `f92dc5cf` (gmail, 10 msgs) and `b2fd01b6` (google.com, 7 msgs, holding the Discord traffic) do exist and are disjoint — but as a debugging note explaining why my measurement and his screen disagreed, not as a defect. This does **not** disturb DEF-126 (a genuine ambiguity in *resolution*, where one address matched two rows) or DEF-32 (federated identity must not be resolved by email); those concern the system guessing that two principals are the same, which remains prohibited. What is retracted is only the inference that the system *ought* to relate them. **The filing error was importing an assumption about the product model into a defect report and never marking it as an assumption.** I described the split as "the display-side consequence of the same root cause as DEF-126" — root-causing a behaviour to a defect requires knowing the behaviour is unintended, and I did not know that; the person who did was one question away. **Rule 1052** — before filing "X and Y should be related", state the product assumption that makes the relation desirable and mark it as an assumption; a defect report that silently encodes a design opinion will be read as a measurement, and mine was written persuasively enough that it would have been.

[^57]: **DEF-135** (FILED, BLOCKING for gteam acceptance, filed 2026-09-02, journal §5no): **The delivery envelope omits `conversation` for external-channel inbound messages that do have a conversation.** This is what ptone was actually reporting for three rounds when he said Discord messages arrived in "the old envelope form" — he was not describing field *names*, he was describing a field that **is not there**: *"on the discord envelope. i was expecting a conversation id."* Measured both halves: the envelope delivered to `smith` carries `timestamp, from, to, kind, intent, msg` with no `conversation` key, and the same messages are persisted in conversation `b2fd01b6`. The system knows the id; the envelope does not carry it. **The cause is ordering, not omission.** `handlers_broker_inbound.go:243` populates `preDispatchConvResult` only when the caller supplies **both** `Surface` and `ExternalRef`; the Discord plugin supplies neither, so the value is nil when `RenderDeliveryText` runs at `:305`, and the DM conversation is resolved further downstream, after the envelope is already built. The renderer then correctly omits a key it has no value for. **The renderer's honesty is right and is not the bug — it is what made the bug legible instead of fabricating an id.** I had this recorded in [^51] as correct behaviour, on the reasoning that omitting beats fabricating. That reasoning is sound and the conclusion was still wrong, because it answered "should the renderer invent a value?" when the live question was "why does the renderer not have one?" **An envelope that cannot name its own conversation cannot be replied to by id, which is most of the purpose of having conversation ids in the contract at all** — so this is a hole in the deliverable, not a cosmetic gap, and it is marked blocking for gteam acceptance. Two candidate fixes and they are **not** equivalent: (a) move the render after conversation resolution, which changes ordering only; (b) have the Discord plugin declare `Surface`/`ExternalRef`, which would resolve a **group** conversation pre-dispatch and thereby change *which conversation the message lands in* — a semantic change to persisted data, not a rendering fix. Design owed by me before any developer picks. **Rule 1053** — when a user reports a wrong output three times and each measurement says the output is right, stop re-measuring the thing you think they mean and get them to point at the exact token; I checked field names three times against a complaint that was about a missing field, and the vocabulary I had just finished renaming is precisely what made "envelope" feel unambiguous to me.

[^58]: **DEF-136** (FILED-NOT-STAFFED, filed 2026-09-02, journal §5nr): **"Manage implies read" is an unverified assumption load-bearing at two authorization sites, with no test anywhere asserting it.** `handlers_messages.go:237-243` and now `handlers_logs.go:313-321` both gate as *allow if `ActionManage` OR `ActionRead`*, each carrying the comment "manage implies read". I searched `pkg/hub` for a hierarchy: `authz_delegation_ceiling.go:75` groups `ActionManage` with other privileged actions, and `permissions/registry.go` enumerates capabilities, but **nothing implements or asserts an implication from manage to read.** If a role can hold manage without read, both handlers grant access that a plain `s.authorize(..., ActionRead)` would refuse. Caught while reviewing `ca-msg-fix2`, which replaced `if !s.authorize(w, r, agentResource(agent), ActionRead)` at `handlers_logs.go` with the manage-then-read pair. **I nearly rejected that change as a widening, and the reason I did not is worth recording: the pattern it adopts already exists verbatim at the sibling handler, comment and all.** fix2 mirrored established practice rather than inventing an assumption, so the change is consistent with the codebase — but *consistent with* is not *verified*, and the assumption is now load-bearing in two places instead of one. That is the actual defect: an unproven authorization premise spreading by imitation, where each new copy makes it look better-established than the last. Fix is a test asserting, for every role that can hold `ActionManage` on an agent resource, that `ActionRead` is also allowed — cheap, and it converts an assumption into an invariant. Filed rather than blocked because it is pre-existing and this merge does not change its truth value; **but note that "pre-existing" is exactly the justification that let it reach two sites.**

[^59]: **DEF-137** (FILED-NOT-STAFFED, trivial, filed 2026-09-02, journal §5nr): `handlers_messages.go:243` denies with a bare `writeError(w, http.StatusForbidden, ErrCodeForbidden, "Access denied", nil)` and **no `logAuthzDenial` call** — the identical gap I sent `ca-msg-fix2` back to fix at `handlers_logs.go`. The denial is silent in the audit trail. Found immediately after requiring the fix one file over, which is the useful part: **the defect I was reviewing for turned out to have a twin two hundred lines away, and I only saw it because I went to check whether fix2's pattern matched its neighbour.** Reviewing a fix against its sibling is worth more than reviewing it against the diff. Fix is one line; the standing helper `authorizeWithMessage` (`authorize.go:78-106`) already does denial logging correctly and both sites should use it where the two-level manage/read check permits.

[^60]: **DEF-135 residual coverage gap — ACCEPTED, not a defect to fix** (2026-09-02, journal §5nr): The F2 fix (validate `effectiveConv`, the value actually persisted, rather than `convFromPhase5`) is **correct and untestable**. I mutated it — swapped the validated value for a non-empty sentinel — and the mutation compiles and passes the whole suite. `ca-msg-fix3` reported this themselves, unprompted, with the correct reasoning: `ValidateAttributed` rejects only the empty string, and no live path produces a Phase 11 result with an empty `ConversationID`, so the gate cannot be exercised without a synthetic store fabricating empty ids. **Accepted as an untested one-line gate rather than demanding the fake store**, because the machinery would exceed the risk and would itself need to be trusted. Recorded so the gap is known rather than assumed covered — and recorded approvingly: an agent reporting its own uncaught mutation is the behaviour the mutation discipline exists to produce, and is the opposite of the "it should catch it" claims that made the discipline necessary.

[^61]: **DEF-131 SUPERSEDED by the switch collapse — close, do not work** (2026-09-02, `SWITCH-COLLAPSE-DESIGN.md` §4.2): DEF-131 asked to rename `writeDenyEnabled` → `conversationEnvelopeEnabled` and add a test. The switch-collapse design **deletes** that predicate and both closures injected into `NotificationDispatcher` (`server.go:2500`) and `MessageBrokerProxy` (`server.go:2564`). Renaming a symbol scheduled for deletion costs two dispatches and guarantees a merge conflict between them. Closed as superseded rather than fixed. Recorded because the ledger otherwise reads as an outstanding defect that a future agent would helpfully pick up — Rule 1046, an entry is only true as of when it was written.

[^62]: **No Discord-side envelope switch exists — correcting a premise I let stand** (2026-09-02, `SWITCH-COLLAPSE-DESIGN.md` §0): ptone's directive said "so for discord this is yet another switch." Measured at `0cff20a2b`, `grep` for all three switch names across `extras/` returns nothing. The two things that resemble a Discord switch are `mention_routing` — a pre-existing @-mention feature flag, present identically in the **Teams** broker, predating this refactor — and the plugin-local `conversation_context` table, which is an outbound routing cache keyed `(discord_user_id, project_id, agent_slug) → last_channel_id`, sharing a word with `conversations` and nothing else. Consequence: the collapse does not touch `extras/` at all, and does not need a coordinated plugin+hub release. Filed as a defect in **my** reporting, not in the code: I accepted "yet another switch" as a measurement for a full segment without checking it, and a premise the principal is reasoning from has to be corrected wherever it has already influenced a decision (Rule 1051). AC-10 makes the correction enforceable — any diff touching `extras/` means §0 was wrong.

[^63]: **DEF-135 AC-9 PASS (inbound leg) — and a false premise in my own topology notes** (2026-09-03): gteam capture, message `3e718a5a`, target `smith` (`c9c1123b`), sender principal `b53249ea` (user, google.com account). Envelope delivered to the agent carries `timestamp, conversation, from, to, kind, intent, msg, visibility` with `conversation: {id, kind, surface}` — and **no** `sender`/`type`/`channel`/`thread_id`. Envelope `conversation.id` == persisted `conversation_id` == `0c57b491`, both derived from the **same message id** (envelope from the agent's container log at 00:06:43, row from `SELECT … WHERE id = '3e718a5a'`), so this is a matched pair rather than two lookups that happened to agree. **AC-9 as written passes.** AC-9 as written was also *incomplete* — it checked only the inbound leg, when the entire purpose of the id is that the agent can reply against it and land in the same conversation. Reply-leg verification requested separately; that gap is mine, not the investigator's.

[^64]: **"b2fd01b6 holds the Discord traffic" was WRONG — Discord has no DM surface** (2026-09-03): ptone, verbatim: *"messaged via channel. there is no DM option via the discord integration."* Discord messages therefore always carry a channel id as `thread_id`, always take the Phase 5 **thread** branch, and have **never** landed in a `direct` conversation on this instance. My gteam topology note describing `b2fd01b6` (`dm:agent:c9c1123b:user:b53249ea`) as "holds the Discord traffic" is false; it is most likely web-chat traffic (provenance confirmation outstanding with instance-investigator). **Consequence:** the Alternative B rejection in `DEF-135-DESIGN.md` rested on this premise and has been corrected in place — the conclusion survives, but the mechanism is group-to-group history splitting (`discord:chan:X` vs `thread:<project>:X` keying), not DM-to-group. **Consequence for the fix itself: none.** The chosen design did not change where Discord messages are stored; pre-fix the same channel message also resolved to a thread conversation, it merely resolved too late for the envelope to carry it. G3 ("no change to which conversation a message is persisted into") holds. Recorded because a rejected alternative reads as settled ground to a future implementer, and one foreclosed on a bad premise is worse than one never considered (Rules 1046, 1051).

[^65]: **OPEN — group thread conversations on gteam have ZERO participant rows** (2026-09-03, raised not concluded): conversation `0c57b491` (`kind: group`, `surface: native`, `external_ref: thread:a3083e98:1532505776013312133`) has **0 rows in `conversation_participants`**, yet ptone demonstrably reads it — he received smith's reply. Since Discord has no DM surface ([^64]), **group conversations are the only conversation kind Discord traffic can produce**, so the ACL story for group conversations is the ACL story for all Discord traffic on this instance. Two states are consistent with the observation and are opposites in security terms: (a) group reads are gated on **project membership** and zero participant rows is the normal, by-design state; (b) group reads are gated on **participant rows** and a read just succeeded against an empty ACL. **A working UI cannot distinguish them** — "it works" is evidence for (a) and for (b) equally, which is precisely why this is not being waved through on the strength of the AC-9 pass. Measurement requested from instance-investigator: the authorising function at `file:line`, the behaviour of any participant check on zero rows, and the count of group conversations with 0 vs >0 participant rows. Read-only; nothing to be changed on gteam.

[^66]: **DEF-138 — Discord round trip splits across two conversations. OPEN, significant, NOT a regression** (2026-09-03): Measured on gteam. Inbound message `3e718a5a` persisted to `0c57b491` (`kind: group`, `external_ref: thread:a3083e98:1532505776013312133`) and the envelope correctly carried that id. Smith's reply `1f3f0e39` persisted to `b2fd01b6` (`kind: direct`, `external_ref: dm:agent:c9c1123b:user:b53249ea`). **(d) ≠ (b) == (c).** Inbound resolves by `thread_id`; outbound re-derives a DM key from sender+recipient; neither consults the other. Sharpest statement of the bug: the reply was **delivered to the Discord channel but persisted as a DM**, so delivery surface and storage disagree. **Severity:** this inverts the refactor's core promise — an agent told "you are in conversation X" replies into conversation Y, making the envelope id cosmetically correct and functionally useless. An id you cannot reply into is worse than no id. **Not caused by the DEF-135 fix:** that fix touched only `handlers_broker_inbound.go`; the outbound path is untouched and the split predates it. Pre-fix the envelope carried no id, so nothing contradicted the outbound choice — **the fix did not break the round trip, it made the pre-existing break observable.** This is the argument for putting the id in the envelope even before it was actionable. Does **not** dead-end the end state: it is a resolution-strategy mismatch, not a data problem. Code investigation dispatched to locate where outbound drops the conversation; design to follow. **Consequence for the plan: gteam acceptance is NOT complete while the round trip is broken.**

[^67]: **My [^64] correction was ITSELF wrong — a UI-affordance claim converted into a storage claim** (2026-09-03): [^64] asserted, on the strength of ptone's *"there is no DM option via the discord integration"*, that Discord messages "have **never** landed in a `direct` conversation on this instance." **Measured: false.** gteam holds **32 Discord messages in direct conversations** and 9 in group conversations; `b2fd01b6` alone has channel breakdown web=3, **discord=1**, empty/null=4. The resolution: ptone's statement is about the **inbound user-facing surface** (a user cannot DM the bot), which is true; Discord messages nonetheless reach direct conversations on the **outbound** leg, which is exactly the DEF-138 split. I collapsed a statement about what a UI offers into a claim about what the database contains. **This is the second error in the same area within thirty minutes, and I made it while correcting the first** — the correction was drafted at the speed of the insight rather than the speed of a measurement. The load-bearing part of [^64] survives (Alternative B's rejection mechanism is group-to-group key divergence, not DM-to-group); the over-general sentence does not. → **Rule 1054.**

[^68]: **RESOLVED [^65] — `conversation_participants` is never consulted; conversation is a FILTER, not an ACL** (2026-09-03): Measured answer to the zero-participant question. The read authorization chain is: (1) agent-level authz, `CheckAccess(ctx, user, res, ActionManage)` falling back to `ActionRead`, deny → 403, at `pkg/hub/handlers_messages.go:241-249`; (2) for non-manage callers, a **message-level** participant filter `filter.ParticipantID = user.ID()` scoping to rows where the caller is sender or recipient, at `handlers_messages.go:254-258`; (3) `ResolveThreadConversationForRead()` at `pkg/messaging/conversation.go:310-381`, which derives the key and looks up by external_ref with **no participant check at any point**. **42 of 46 group conversations on gteam have zero participant rows; 4 have some.** So zero-participant is the normal by-design state for group conversations, and **the agent-resource `ActionRead` check is the gate** — conversation membership narrows results, it does not authorise them. This is consistent with the standing rules, which scope "the key IS the ACL" to **direct** conversations specifically; it does not generalise to group. **Not a defect. Filed instead as a latent risk:** if any future change makes conversation membership the authorisation basis for group conversations, those 42 zero-participant rows become either universally invisible or universally readable depending on the check's polarity, and both failure modes would ship silently because the current UI exercises neither. Any such change must first backfill participants or explicitly declare group conversations project-scoped.

[^69]: **DEF-138 root cause — NOT Discord-specific; all four channel surfaces split** (2026-09-03, design at `DEF-138-DESIGN.md`): Every channel plugin sets `ThreadID` inbound and none sets `Surface`/`ExternalRef` — Discord `broker.go:1471-1489`, Slack `events.go:403`, Teams `activities.go:85`, web chat `handlers_chat_v2.go:1159` — so all four take the Phase 5 thread branch to `thread:{projectID}:{threadID}`, kind `group`. Outbound (`messagebroker.go:466`) branches on exactly one field: non-empty `ThreadID` → thread conversation, else → DM. **An agent's reply carries empty `ThreadID`, so empty ⇒ DM, always.** No surface, channel or plugin is consulted. **The asymmetry:** an affinity mechanism already exists and works — `webchat_conversation_context` (`webchannel_store_postgres.go:51-58`) stores `last_channel`, written inbound at `handlers_broker_inbound.go:476-481` and read outbound at `handlers_agent_messaging.go:221-229` to populate `req.Channel`. It stores **no thread id and no conversation id**. That single omission is the entire defect: delivery has affinity, persistence does not, which is exactly why the reply reached the right Discord channel while being stored as a DM. Verified independently by me at three points (the tautology, the absent `ConversationID` field, the `ThreadID`-only branch) rather than accepted on report.

[^70]: **DEF-139 — the divergence detector cannot fail, and its docstring claims otherwise** (2026-09-03): `ComputeDivergenceMatch` (`pkg/messaging/divergence.go:286-323`) documents itself as *"non-tautological: actualExternalRef comes from the database, not from reconstructing inputs."* **In the DM branch this is false.** `oldPair` = sorted{`senderID`, `recipientID`}; `newPair` = the ids at `parts[2]`/`parts[4]` of an external_ref that `ResolveOrCreateDMConversation` (`conversation.go:92-116`) built from those same two ids and upserted moments earlier. Same derivation on both sides — it **cannot** return anything but `dm-routing-agreement`. The "comes from the database" defence is true and irrelevant: the row was written from those inputs on this request, so the claim holds only for rows created under a *different* rule, which resolve-or-**create** guarantees against. Compounding it, the check that could have caught the split — `CheckConversationConsistency` (`divergence.go:381-460`), called at `messagebroker.go:520` — has **its return value discarded at both call sites**, and per DEF-130 already emits ~16 false warnings a day, so its log line is buried even when correct. **This is how a green signal accompanied a broken round trip for the entire life of the switch.** A detector that cannot fail is worse than no detector, because it is read as evidence. Fix scoped as SC-5 with a mandatory mutation test (AC-7): the current check *passes* an inconsistent-conversation scenario, so any test that does not fail against unfixed code proves nothing.

[^71]: **Double conversation resolution on the outbound path — one result silently discarded** (2026-09-03, scoped as DEF-138 SC-2): `handleAgentOutboundMessage` resolves a conversation at `handlers_agent_messaging.go:307-314`, assigns it at `:343`, emits a divergence log line at `:355-369` — and then **discards the entire `storeMsg` when a broker is present** (`:398-407`), because `structuredMsg` carries no `ConversationID` and `store.CreateMessage` runs only on the non-broker branch at `:409`. The surviving row is written by `deliverToUser`, which resolves from scratch again (`messagebroker.go:481-486`, assigned `:501`, persisted `:522`). So one reply produces **two resolutions and two divergence log lines with different message ids**, and one result is dead. They agree today only because they consume identical inputs. Not cosmetic: the dead path is precisely the code a future change "fixes" without effect, and the duplicate log lines make the instrumentation ambiguous about which id is authoritative — the investigator had to disambiguate the two lines by matching `message_id` against the persisted row. AC-6 requires exactly one line after the fix.

[^72]: **DEF-139 WIDENED — both branches are tautological, seven call sites discard the real check, and the codebase already contains the correct diagnosis** (2026-09-03, verified by me directly at `0cff20a2b`): (1) **The thread branch is tautological too.** `ComputeDivergenceMatch` (`divergence.go:325-340`) compares `oldThreadID` — which `OldRoutingFromMessage` produced as `"thread:" + msg.ThreadID` — against the threadID portion of `thread:{projectID}:{threadID}`, a ref built from that same `msg.ThreadID`. Same input on both sides, exactly as in the DM branch. (2) **The mismatch branches are effectively unreachable.** `OldRoutingFromMessage` selects thread-vs-DM on `threadID != ""`, and the resolver selects thread-vs-DM on the identical `threadID != ""` test (`messagebroker.go:466`), so old and new can never disagree on routing *type* and `routing-type-mismatch` cannot fire from this path. (3) **`CheckConversationConsistency`'s return value is discarded at SEVEN call sites**, not the two I first reported: `messagebroker.go:520,720`, `handlers_broker_inbound.go:452`, `handlers_agent_messaging.go:371,1127,1433,1615`. (4) **The codebase states the correct diagnosis itself.** `CheckConversationConsistency`'s docstring (`divergence.go:368-372`) reads: *"Unlike ComputeDivergenceMatch (**which compares routing keys derived from the same input fields**), this function queries actual persisted messages… providing a truly independent source of truth."* That is precisely right, and it sits directly above the function whose output is thrown away — while `ComputeDivergenceMatch`'s own docstring insists *"The comparison is non-tautological."* **Two docstrings in one file contradict each other, and the correct one belongs to the ignored function.**

[^73]: **PRECISION CORRECTION to my own DEF-139 claim** (2026-09-03): I told ptone `ComputeDivergenceMatch` "cannot return anything but agreement." **That is overclaimed.** It can return `no-new-routing` when `convID == ""` (`divergence.go:288-290`), which is a genuine and useful signal that resolution failed. The exact claim is narrower and still damning: **when it reports *agreement*, that verdict carries no information**, because both sides of the comparison derive from the same input fields; and its *mismatch* verdicts are effectively unreachable because old and new routing branch on the identical `threadID != ""` predicate. The one residual path to a real mismatch is `ResolveOrCreateThreadConversation` with `WithTopicLookup` remapping a thread onto a pre-existing conversation whose ref differs — narrow, and not what the counter is read as measuring. Recorded because this is the third premise error in one session and the pattern is consistent: I state the vivid form of a finding before I have bounded it. The vivid form was *nearly* right, which is what makes it dangerous — a reviewer checking it would find `no-new-routing` and could reasonably conclude the whole finding was overstated.

[^74]: **The divergence board's headline number is not evidence — and this bears on how we judge the refactor** (2026-09-03): `GET /api/v1/admin/messaging/divergence` (`admin_messaging_divergence.go:39-80`) reports `matches`, `mismatches`, `comparisons`, `fallbacks` with a genuinely scrupulous caveat block: counters are per-replica and reset on restart; `mismatches` conflates two unrelated signals (already DEF-124/125); `CheckConversationConsistency` **fails open** on query errors, empty conversation ids and insufficient data; it is blind to pre-dual-write rows with empty `ConversationID`; and it samples 50 rows by thread or 25 per direction for DMs, so *"the reported mismatch count is a lower bound on a sample, not a measurement of the population."* **What the caveats do not say is the thing that matters most:** that the other contributing detector, `ComputeDivergenceMatch`, produces agreement verdicts that are uninformative by construction ([^72], [^73]). So `matches: N` is not N confirmations — it is N tautologies, and `mismatches: 0` is a fail-open sampled check with a documented blind spot. **Consequence for the plan:** this board cannot be used as acceptance evidence for gteam or for the fresh cutover. Whatever we use to judge "is the conversation model covering traffic correctly" must be built or fixed first. That is a scope addition to the acceptance work, not merely a defect to file, and it is the strongest argument yet for AC-7's mutation test — DEF-138 was invisible to every counter on this board while being trivially visible to a single round-trip test.

[^75]: **DEF-139 RESOLVED** (2026-09-03, merged to `scion/tranche-g` as `f7687e2dc` from `scion/def-139-detector` @ `222775ee`): The fix deliberately **does not** make `ComputeDivergenceMatch` non-tautological. The developer's reasoning, which I accepted: the function is pure, and threading a store through it to give it an independent source would widen the signature across seven call sites in order to duplicate a check the codebase already has. Instead the tautology is **documented accurately** — the replacement docstring states that both sides derive from the same input fields within the same request, that its match verdicts carry no independent signal, and that the one residual mismatch path is a `WithTopicLookup` thread remap — and the genuinely independent check, `CheckConversationConsistency`, is surfaced: it gains its own `consistencyChecks`/`consistencyMismatches` counters and **all seven call sites now consume its return** with a descriptive WARN (instrumentation only; no site fails a request). The admin board gains `consistency_checks`/`consistency_mismatches` plus a `routing_key_tautology` caveat stating that *"a match count of N means N tautological comparisons, not N confirmed agreements."* **Unplanned side benefit:** because the consistency path moved from `Inc(false)` to `IncConsistency(false)`, `DivergenceMetrics.Inc` is now reached only from `LogDivergence`, so `mismatches` no longer conflates the two signals — that is DEF-124/125's de-conflation arriving as a by-product. **Two bounded gaps recorded rather than fixed:** the AC-8 structural guard scans `pkg/hub` only (`hubDir := "."`), so a call site added in another package escapes it; and its line-oriented regex would miss a gofmt-split call, though the call-count assertion partly covers that. If the guard is ever green while the defect returns, those are the first two places to look. **Verification was mine, not the developer's** — see [^76].

[^76]: **The AC-7 evidence was a build failure, and the distinction it obscures is worth keeping** (2026-09-03): The developer satisfied AC-7 ("the test must fail against unfixed code") by pasting `ConsistencyMismatches undefined … [build failed]`. That is not a caught violation; it shows only that the test references an API that did not yet exist. **The reason it mattered here specifically:** `CheckConversationConsistency` *already returned false on mismatch* before the change — the defect was that its return was **discarded**, not that detection was broken. So the test's substantive assertion (`consistent == false`) would have passed against unfixed code, and the entire red came from a missing accessor method. On that evidence the criterion was satisfied by an API addition. I ran the real mutation myself — `IncConsistency(false); return false` → `IncConsistency(true); return true`, build green — and got `divergence_test.go:409: expected consistency check to detect the mismatch, but it reported consistent`. A semantic failure, restored clean, passes unmutated. I separately mutated call site `messagebroker.go:520` to `_ =` and confirmed the AC-8 guard reports `assigned to _ (return value discarded)`. **The conclusion to carry forward: the soundness of a test and the soundness of the evidence offered for it are separable properties, and only the second one was defective here.** Conflating them would have meant either rejecting a good test or — far more likely, and the real hazard — accepting a worthless one on identical-looking evidence in some future review where the test *was* only coupled to an API surface.

[^77]: **DEF-138 RESOLVED — explicit conversation routing, merged to `scion/tranche-g` as `c316b08de`** (2026-09-03, design at `DEF-138-DESIGN.md` v2, three commits `ee75cc47a` → `3edf96d78` → `a026a3b7a`): The fix completes the address rather than improving the guess. `OutboundMessageRequest` gains `conversation_id` (server DTO and the `pkg/hubclient` mirror); the CLI `conv:<uuid>` and `#<thread>` gates are opened; a caller-asserted conversation is **authorized before use**, mirroring the DEF-49 block with docstrings rewritten for the sender direction — in the sibling handler `agent` is the *recipient*, so identical-looking code asserts a different claim, and that is a copy-paste hazard now called out in the design. The handler's resolution reaches the writer, collapsing [^71]'s double resolution. **The load-bearing review finding (BLOCKER 1):** the first revision built a minimal `ConversationResult` with an empty `ExternalRef` and fed it to `ComputeDivergenceMatch`, so **every correctly routed explicit message logged `routing-type-mismatch`** — a fix that would have made the board scream precisely when it was working. Confirmed empirically with a throwaway probe test before I said a word about it. The repair is not a better comparison but the recognition that **comparison is meaningless when the caller stated the answer**: explicit routes bypass the matcher entirely via `LogExplicitRouting` and a separate `explicit_routes` counter, surfaced on the admin board with a caveat stating it measures **adoption, not correctness** ([^74] applies unchanged). **BLOCKER 2:** AC-6 was declared "out of scope" when it was explicitly in the brief; the structural tell was the AC-8 guard still counting 7 call sites when the collapse should have made it 6. **What made this reviewable at all was DEF-139 landing first** — [^74] establishes the board cannot be acceptance evidence, so the only currency was mutation, and both blockers were found by running mutations against production code with the build green rather than by reading reports. Verification independent at every point: numstat re-run against a **freshly fetched** `refs/rt/tg` (a stale local ref had shown 13 files instead of 6 and I nearly reasoned from it), call sites grepped, gate run, environmental failures compared against the base commit. **Not yet closed:** AC-12 — that an agent reply carrying `conv:<uuid>` lands in the inbound conversation — is only verifiable on gteam after deploy, and the gteam reproduction (`0c57b491` / `b2fd01b6`) is preserved untouched for exactly that test.

[^78]: **DEF-140 — every conversation created via the thread path is stamped `surface: "native"`, whatever channel it came from. All four surfaces affected; NOT a DEF-138 regression** (2026-09-03, found by ptone on gteam re-test of `c316b08de`): `ResolveOrCreateConversationByKey` defaults `cfg.surface = "native"` (`derive_key.go:160`) and `WithSurface` overrides it — but `WithSurface` is reachable from exactly two production call sites (`handlers_broker_inbound.go:245`, `handlers_agent_messaging.go:868`), **both gated on `req.Surface != "" && req.ExternalRef != ""`**. No channel plugin sets either field ([^69]), so every inbound message instead takes `resolvePhase5Conversation`'s thread branch → `ResolveOrCreateThreadConversation`, whose only option is `WithTopicLookup`; there is **no surface parameter on the thread path at all**, and `resolvePhase5Conversation` is not even passed the information. The row is therefore written wrong at creation — this is persisted data, not a rendering fault, and `render_delivery.go:166` faithfully reports what is stored. **The hub already knows:** `req.Message.Channel` holds `"discord"` in the same request and is used at `:418` and `:479-481` for the channel-affinity record. The defect is a plumbing gap, not missing information. **Severity — deliberately not inflated:** surface participates in **no routing and no authorization decision** (grepped: the only comparisons are the two request-validation gates, an audit default, and store-level query filters), so nothing is mis-delivered or wrongly exposed. The costs are a false field in the envelope contract, and `ListConversations(filter.Surface=...)` / `conversation_store.go:356-362` returning nothing for any external surface. **What raises it above cosmetic is recoverability:** a thread conversation's `external_ref` is `thread:{projectID}:{threadID}`, which does not encode the channel, so surface **cannot be reconstructed from the conversation row**. Repair would require joining to messages and inferring from `message.channel`, which is ambiguous for mixed-channel conversations — `b2fd01b6` already holds web=3, discord=1. Wrong rows accrete with traffic and get harder to fix, which is the argument for fixing it before more gteam testing rather than after. **Design ruling (mine, flagged to ptone for objection):** surface means *where the conversation was born* — stamped once at creation, never rewritten on later messages; per-message `channel` continues to carry per-message truth. First-writer-wins, because a conversation legitimately receives traffic from several channels and a last-writer rule would make the field flap. **My own miss:** [^65] records `0c57b491` as `kind: group, surface: native, external_ref: thread:...` — I wrote that value down myself while investigating participant rows and did not see it, because I was looking at a different column. ptone found it by reading the envelope the refactor exists to produce. **A field you transcribe while looking for something else is a field you have not checked.**

[^79]: **DEF-140 SHARPENED, and my R1 ruling WITHDRAWN — the schema already encodes the model I ruled against, and my evidence for ruling was wreckage from DEF-138** (2026-09-03): I ruled that surface means "where a conversation was born", stamped once, on the stated ground that *"a conversation legitimately receives traffic from several channels"*. ptone overturned the premise, verbatim: *"a conversation is a distinct routing construct - where a conversation does NOT span across channels."* **He is right and the code already said so:** `pkg/ent/schema/conversation.go:89-92` declares `index.Fields("surface", "external_ref").Unique()` with `IndexWhere("external_ref <> '' AND deleted_at IS NULL")`. Conversation identity is the **pair**, not `external_ref` alone. I ruled on the semantics of a field without reading the uniqueness constraint that defines it — one grep away, in the file the ruling was about. **The sharpened defect:** because every row carries the constant `"native"`, surface contributes zero discriminating power to that index, which silently degrades to uniqueness on `external_ref` alone. The guarantee that prevents conversations spanning channels **is present in the schema and inert**. DEF-140 is therefore not a label fault; the fix reactivates a dormant constraint. **The evidentiary failure is the part worth keeping.** My sole evidence for multi-channel conversations was `b2fd01b6` holding web=3 and discord=1 ([^67]). That row is the DEF-138 reproduction, and the discord message in it is almost certainly `1f3f0e39` — smith's misrouted reply, i.e. **the defect I had just spent three review rounds fixing**. I took the corpse of a known bug as evidence for a design principle, in the same conversation I had personally established as corrupt. The correct move on seeing a row that violates a model is to ask why the row is malformed, never to widen the model to admit it. **This is the third time in this project that I have generalised from gteam data without asking what produced it** ([^64], [^67], now this) — and unlike the first two, this one would have shipped into a design ruling that a developer was already building against. Marked inference, not measurement: I have not re-queried `b2fd01b6`'s channel breakdown since the fix, and said so to ptone rather than presenting it as settled. **Consequence now flagged to both ptone and the developer:** once surface carries a real value, `(discord, thread:P:T)` no longer matches an existing `(native, thread:P:T)` row, so live conversations FORK — history stays on the old row, new traffic starts a new one. Correct under the model and acceptable before a fresh cutover, but it must be a deliberate, tested outcome rather than a surprise, so the developer now owes a test asserting the split happens and the old row is untouched. → **Rule 1060.**

[^80]: **DEF-140 RESOLVED — merged to `scion/tranche-g` as `eb59ea98f`** (2026-09-03, four revisions `6fc8204f8` → `63d20e231` → `77cc53e52` → `94fc5eee7`): `ChannelToSurface` is now the single channel→surface mapping point, called from all three thread-resolution sites; valid surfaces pass through, `"web"` maps to `native` explicitly, unknown falls back to `native` with a WARN, nil logger guarded. **Review blocker worth remembering:** the first revision forwarded the raw `msg.Channel` into an enum-validated column guarded only by `channel != ""`. `"web"` is set at eight sites in `handlers_chat_v2.go`, including one that also carries a `ThreadID`, and is **not** a member of the six-value surface enum — so the fix would have converted a working message path into a **denied write** wherever `writeDenyEnabled` is on. The developer had reported *"web is NOT a valid surface enum value"* and *"passes WithThreadSurface(msg.Channel) when non-empty"* in the same report; the defect was visible only by reading those two sentences against each other. **The second blocker is the structural one:** the test double accepted any surface string, so the suite was constitutionally incapable of detecting the first — which is precisely why a change that could deny writes produced a green gate. **A test double more permissive than production is not a weak test, it is a blind spot with a passing tick.** **Three attempts were needed at the drift guard**, and the sequence is the lesson: `validSurfaces` + a test-side `validSurfaceEnum` (two hand copies joined by the comment *"Kept in sync manually"*), then a cross-check that enumerated the ent constants **by hand** into `entSurfaces` — under which adding a value to the real schema left Direction 1, Direction 2 and the cardinality check all green, because every assertion compared a copy against a copy. Resolved by scanning the schema declaration at `pkg/ent/schema/conversation.go:52-53`, **fail-closed** on unreadable file, unmatched pattern or zero extracted values. → **Rule 1062.** **Deliberately NOT fixed:** existing rows are not backfilled (R2) — mutating them is irreversible and ptone's call. **Consequence accepted and tested, not merely noted:** because identity is `(surface, external_ref)`, an existing `(native, thread:P:T)` row and a new `(discord, thread:P:T)` are different conversations, so live histories **fork** on deploy; `TestDEF140_DiscordThreadCreatesSeparateConversation` asserts the split occurs and the old row is untouched. **Verification was mine at every point** — numstat re-run, the `whatsapp`-into-schema mutation executed myself (the direction the two earlier guards could never fail on; semantic failure, build green, `dirty_after_restore=0`), and the full gate: 64 ok / 2 fail, exactly the seven known-environmental, `pkg/hub`, `pkg/messaging`, `pkg/store` green.

[^81]: **DEF-141 — `LogExplicitRouting` fires on hub-DERIVED conversations, so `explicit_routes` counts adoption that did not happen, and `ComputeDivergenceMatch` is skipped for the entire outbound agent→user path** (2026-09-04, found while diagnosing the AC-12 failure): `handleAgentOutboundMessage` has two branches. The explicit branch honours `req.ConversationID` after authorization; the `else` branch **derives** a key via `DeriveConversationKey` and resolves it (`handlers_agent_messaging.go:422-470`). Both then fall through to the same unconditional statement at `:485-487` — `if convResult != nil { structuredMsg.ConversationID = convResult.ConversationID }` — which is DEF-138's P-3 propagation and is correct in itself. The fault is downstream: the broker tests **only** `if msg.ConversationID != ""` (`messagebroker.go:473`) and, on that single bit, concludes the value was caller-asserted. Its own comment states the false premise verbatim: *"the handler already authorized the assertion (P-2) and stamped structuredMsg before publishing to the broker."* True in one branch, false in the other, and the broker cannot tell them apart because **the discriminator was never propagated**. Two consequences, both measurement-integrity rather than delivery faults: (1) `LogExplicitRouting` + the `explicit_routes` counter fire on derivations, and that counter is surfaced on the admin board with a caveat saying it measures **adoption** ([^77]) — so the one number we added to tell us whether agents had adopted explicit routing reports a floor of "every outbound agent→user message", i.e. it measures nothing. (2) The `else` arm holding `ComputeDivergenceMatch` and `LogDivergence` becomes **unreachable for every derived outbound agent→user message**, so those messages contribute no `comparisons` at all. The gteam board reading `comparisons: 1, explicit_routes: 1` for a two-message round trip is exactly this: the inbound was compared, the outbound was silently reclassified. Note the compounding with [^74] — the board was already unable to serve as acceptance evidence; this narrows its coverage further **without any caveat saying so**, which is the worse failure mode, because a shrinking denominator looks like a clean record. **Fix direction (not yet designed):** the propagated value must carry its provenance — an explicit boolean or a two-field carry (`ConversationID` + `ConversationAsserted`) set true only in the authorized branch — and the broker must branch on provenance, never on non-emptiness. Do **not** fix this by reverting P-3; the propagation is what closed [^71]'s double resolution.

[^82]: **DEF-142 — the CLI posts every conversation reference to `/api/v1/conversations/resolve`, and no such route is registered on the hub** (2026-09-04, static evidence complete, empirical probe in flight): `cmd/message.go:746` routes **all four** reference forms — `conv:<uuid>`, `#<thread>`, `@<agent-slug>`, `@<email>` — through `hubCtx.Client.Messages().ResolveConversation`, which POSTs `/api/v1/conversations/resolve` (`pkg/hubclient/messages.go:225`). Enumerating every `"/api/v1/…"` literal in the tree outside `pkg/hubclient` returns no such registration; the sole conversation prefix is `/api/v1/chat/conversations/` (`server.go:3952`), which does not match, and all eleven `handleConversation*` funcs sit behind it. `web.go:678`'s `Handle("/api/v1/", …)` is a proxy to the same hub mux, not a dynamic dispatcher. **The only other occurrence of the path anywhere in the repository is a mock server in `cmd/message_convref_test.go:60`** — so the CLI reference tests pass against an endpoint that exists nowhere but the tests. This is the [^80] blind-spot shape at a coarser grain: there, a test double was more permissive than production; here, a test double **supplies a production surface that production does not have**, and no test in the tree crosses the boundary to notice. If confirmed, the blast radius is that `scion message conv:<uuid> …` — the affordance DEF-138 exists to provide, and the mechanism ptone's explicit-routing position depends on — **cannot succeed against a real hub**, and the failure is a transport error at `wrapHubError`, so the message is not sent at all rather than mis-routed. That also means DEF-138's AC-12 was never testable by the route it assumes. **Deliberately recorded as unconfirmed at time of writing:** the static case is strong but a missing-route claim deserves a measurement, so a probe against a real `pkg/hub` mux with a known-registered control route is running; a 404 without a working control proves nothing. **If falsified, the registration site goes here and the CLI-side diagnosis of AC-12 is reopened.**

  **CONFIRMED** (2026-09-04, throwaway probe against a real `pkg/hub` server built by `handlers_test.go:41`'s `testServer`, issued through `srv.Handler()` so middleware and auth are in the path; probe file deleted, worktree removed, nothing committed). The control discriminates exactly as required and is self-authenticating: `GET /api/v1/messages` → **200** with a real body `{"items":[]}`, and `POST /api/v1/messages` → **405** carrying the hub's own structured `method_not_allowed` envelope. Against that, `POST` and `GET /api/v1/conversations/resolve` both → **404** with Go's default plain-text `"404 page not found\n"`. The body is the tell: a registered pattern rejecting a verb produces the hub's JSON envelope, while an unmatched pattern produces `http.NotFound`'s plain text. Method is irrelevant — the failure is at mux dispatch, before any verb check. Two independent methods now agree (my static enumeration of every `"/api/v1/…"` literal, and this probe), which is why I am recording it as settled.

  **The probe found more than it was asked for, and it changes the fix.** `POST /api/v1/chat/conversations/resolve` — the "obviously correct" URL, i.e. the hypothesis that this is a client-side path typo — **also 404s**, and for a second, independent reason: `handleChatConversationRoutes` (`handlers_chat_v2.go:218`) requires two path segments (`:225-228`) and its action switch (`:254-271`) admits only `messages`, `read`, `typing`, `interagent`, `mute`, `pin`, `promote` before falling through to `http.NotFound` at `:270`. A single-segment `resolve` cannot match under either prefix. **So there is no resolve endpoint anywhere, under any prefix: the server-side handler was never written.** This is not a registration line to add — it is a feature to design, with its own authorization surface, and DEF-142 is therefore substantially larger than filed. Ruled out as a flag artifact: `nativeChatEnabled` defaults true (`handlers_public_settings.go:33-38`), so the chat block at `server.go:3952` *was* registered during the probe.

  **Consequence for the record:** every reference form fails at `cmd/message.go:751` with `failed to resolve conversation reference %q`, and the message is never sent. This is not new with DEF-138 — the `@<agent>` and `@<email>` forms predate it and route through the same call, so **the entire S4 reference grammar has been dead against a real hub for its whole life**, while its CLI tests passed green against `cmd/message_convref_test.go:60`'s mock. A test double that supplies a production surface production lacks does not merely fail to catch the defect; it manufactures the evidence that the feature works. **This is the second time in two weeks that a green CLI suite certified a path no real server serves** ([^80] is the permissive-double variant), and the shared root is that nothing in the tree exercises the CLI against a real mux. That gap is worth closing on its own account, separately from DEF-142.

[^83]: **DEF-143 — the envelope presents `conversation` as inert metadata, requiring the agent to supply four inference steps it was never given** (2026-09-04, measured on gteam, root cause of the AC-12 failure jointly with [^84]): The delivery envelope smith received renders the conversation as a nested object and nothing else — `"conversation": {"id": "0c57b491-…", "kind": "group", "surface": "native"}`. There is **no `reply_to`, no `reply_ref`, no instruction text, and nothing marking the field as a routing directive rather than context**. For the field to change behaviour, the agent must independently know (1) that this is a directive and not decoration, (2) that a `conv:<uuid>` CLI form exists, (3) that it belongs in the *recipient* position, and (4) that it differs from `user:<id>` addressing. All four live in a separate document. **The field the entire refactor exists to deliver is, at the point of delivery, indistinguishable from decoration.** Smith saw it, had no instruction, and used the only addressing form it knew. **Fix direction:** carry the reply reference in the envelope as a literal ready-to-use string (`"reply_ref": "conv:0c57b491-…"`), collapsing all four steps into a copy. Deliberately *not* proposing prose inside a structured envelope as the primary mechanism — the structured field is the contract, and DEF-133's vocabulary work should absorb it rather than have prose bolted alongside. **This is also the correct place for the affordance on the merits, independent of the staleness problem in [^84]:** ptone's stated preference is explicit routing with no external affinity memory, and an envelope that carries its own reply address is exactly that — the information needed to route the reply travels with the message being replied to, so nothing has to be remembered between messages.

[^84]: **DEF-144 — agent containers carry a skill snapshot frozen at creation, so no skill edit ever reaches a running agent** (2026-09-04, measured on gteam; ownership probably platform, not this project, but it blocks our acceptance testing so it is filed here): Smith's `scion-messaging` skill at `/home/scion/.claude/skills/scion-messaging/SKILL.md` is dated **Aug 12 12:48**, its container creation time. It predates *both* revisions of the conversation-routing paragraph — the Aug 29 transitional note and the Sep 3 operative instruction from `ee75cc47a`. The delivery mechanism is not broken and the agent is not negligent: the skill is on-demand, and **smith invoked it twice, immediately before messaging.** It loaded a document that had no conversation-routing text to load. **The consequence is general and it invalidates a class of plan:** any strategy whose success depends on agent behaviour changing after we edit a document is inert for every agent already running, which on gteam is all of them. My own recap proposed exactly such a plan ("draft the skill update") one message before this was measured. **Immediate testing consequences:** AC-12 cannot be exercised with smith as it stands; updating the gteam *hub* does not update agent *containers*; and any future acceptance criterion that depends on agent-side guidance must state which agents were created after the guidance landed. → **Rule 1065.**

[^85]: **DEF-145 — DEF-142 is one instance of a class: seven client call sites target HTTP paths the server does not serve, and the tree contains no test that crosses the client/server boundary** (2026-09-04, audit run after [^82] confirmed; method: all 165 `s.mux.HandleFunc` patterns replayed through a real `http.ServeMux` so Go's prefix semantics are honoured rather than approximated, then every prefix-match hand-checked against its sub-router's accepted action set, then probed). **Ownership: NOT this project.** Recorded here because the class was found here and because the shared root cause is one I am already fixing for the messaging path; routing the individual defects is ptone's call and I have not absorbed them.
    **Live callers, probe-confirmed:** (1) `scion reset-auth` — `agents.go:468` posts the project-scoped path, but `handleProjectAgentAction`'s switch has no `reset-auth` case (`handlers_projects_core.go:2477-2491`); the action exists only on the hub-scoped route. `404 "Action not found"`. **The entire CLI command is dead.** (2) `POST /api/v1/agent/secrets` (`pkg/sciontool/hub/client.go:1849`) — no such route; called at container init by `fetchSecretOverrides` (`cmd/sciontool/commands/init.go:2112`), which **logs and swallows** the error, so agent secret overrides have silently never resolved. (3) the whole Slack identity-linking flow (`extras/scion-slack/.../register.go:228,252`) — `pkg/hub` contains **zero** slack routes; static, but there is nothing to miss. (4) Teams broker callback → `/api/v1/broker/callback`; only `broker/inbound` and `broker/projects` are registered, and the plural `/api/v1/brokers/` prefix does not cover the singular. **Latent, no production caller:** (5) `auth/ws-ticket`; (6) hub-scoped `SubmitEnv` — reachable only via `Client.Agents()` while the sole caller uses `ProjectAgents()`. **Web:** (7) the UI posts `{action:"set_message_mode"}` to `.../actions`, but the hub expects the action **in the path** — a dead write path for message-mode and cascade changes.
    **Three of these are mock-backed and therefore have passing tests that certify a route the server lacks** — `secret_fetch_test.go:113` asserts the exact missing path, as does the Teams `hubclient_test.go:133`, alongside [^82]'s `message_convref_test.go:60`. **The unifying fact is not that mocks are bad; it is that no test in this repository exercises a client path against a real server mux, so a mock is the only thing any of these paths has ever met.** A mock cannot disagree with the server about what exists, because it never meets it.
    **Recommended structural fix, and this part IS mine to design:** a route-contract gate that replays every client-side path literal through a mux loaded with the server's real patterns, and — for paths landing on a prefix — checks the sub-router's accepted action set. The audit performed exactly this procedure manually, which is the evidence it is buildable; it would have caught all seven. Its harder half is enumeration, not matching: dynamically-built paths resist static extraction (120 of 261 web paths carry a `${…}` segment; ~14 vary in the first segment and are statically unresolvable), which argues for shared path constants referenced by both sides over regex scraping. **Scope bounds stated rather than implied** ([^1057]): web sub-routers were hand-checked for seven prefixes only; `web/dist`, and shell/doc clients under `hack/`, `scripts/`, `deploy/` were not searched. A negative result over those is not available.

[^86]: **DEF-146 — the deploy procedure upgrades the hub but not the `scion` CLI agents run, so no CLI-side change has ever reached a gteam agent** (2026-09-04, measured on gteam by `instance-investigator`, code read by me at the exact commit agents run): Three independently-versioned builds are live simultaneously. The hub is `eb59ea98` (today). The binary agents actually execute is `/opt/scion/bin/scion` at **`98543da9`** (Sep 2), bind-mounted from `/home/scion/scion/.build/container` via `SCION_DEV_BINARIES`. The image's own `/usr/local/bin/scion` is `7c12c09a` (Aug 2), baked into `scion-claude:latest` and shadowed by the sideload on `PATH`. **Nothing rebuilds the sideloaded binaries when the hub is upgraded** — the VM's git HEAD is already `eb59ea98`, so `make container-binaries` would close the gap, but the deploy does not run it. The three-way divergence is what establishes the policy: a matching pair could be coincidence, three distinct commits cannot. **The concrete cost, and it is not hypothetical:** `git log 98543da9..tranche-g -- cmd/` returns two commits, one of which is `ee75cc47a` — DEF-138, explicit conversation routing. At `98543da9`, `cmd/message.go:154` returns a hard error for `RefConversation` and `RefThread`: *"conversation reference %q is not yet supported in the CLI; use @<agent-name> to message an agent"*. **Every agent on gteam is running a CLI in which the affordance this entire refactor exists to deliver is wired to refuse.** Verified by reading the branch body at that commit, not the `Long` help string — the help text at `98543da9` says "(not yet supported — errors)" and agrees, but a help string is a claim about behaviour and the gate is the behaviour. **This corrects an attribution I made.** [^83] (DEF-143) diagnosed the AC-12 failure as the envelope presenting `conversation` as decoration. That remains true and worth fixing, but it was **not the binding constraint**: an agent that had been told exactly what to type would have hit this error anyway. DEF-143 was a *sufficient-looking* cause found before the actually-sufficient one, which is the more dangerous ordering, because a plausible root cause stops the search. **Same class as [^84]** (DEF-144, skill snapshots frozen at container creation) and the pair is the general statement: *the container is a version boundary, and every artefact that crosses it — skills, binaries, guidance — has its own staleness clock that hub deploys do not wind.* Any acceptance criterion depending on agent-side behaviour must state the build the agent was running, not the build the hub was running. **Forward consequence:** DEF-142 P5 changes `cmd/` too and would have been invisible in exactly the same way. **Remediation recommended, not executed** — `make container-binaries` on the VM is a live change and is ptone's call; escalated with that recommendation.

[^87]: **DEF-146 addendum — the remediation I first recommended was a regression, and the reason is a two-checkout split that is the actual root cause** (2026-09-04, measured after [^86] was filed): I recommended `make container-binaries` on the VM on the strength of "the VM's git HEAD is already `eb59ea98`". That was true of the wrong directory. **gteam has two checkouts.** The hub is built and run from `/home/scion/scion-tranche-g` (HEAD `eb59ea98`). Containers bind-mount their CLI from `/home/scion/scion/.build/container`, fed by a *separate* checkout at **`45c440bd`**. Different inodes (1909104 vs 2078128), confirmed rather than assumed. `CONTAINER_DIR := ./.build/container` is CWD-relative, so the Makefile silently targets whichever checkout you happen to be standing in. **Consequence: running the command in the obvious directory writes where no container reads; running it where containers do read builds `45c440bd`, which is OLDER than the `98543da9` binary agents run today.** The naive remediation ships a regression. **And the feeding directory is not even self-consistent:** `45c440bd` is dated Sep 2 02:36, the binary sitting in its `.build/container` is `98543da9` from Sep 2 11:38 — the checkout is nine hours *behind its own build output*, so it was moved backward after building. **A build directory whose contents cannot be predicted from its checkout is not a source of truth, and no version discipline layered on top of it can be.** Four commits are therefore live at once (`7c12c09a` image, `45c440bd` feeding checkout, `98543da9` actual CLI, `eb59ea98` hub), which strengthens rather than merely extends [^86]: the three-way divergence showed the rebuild is not automatic; the four-way shows there is no single place where the answer to "what will agents run" is written down. **Revised recommendation: repoint `SCION_DEV_BINARIES` at `scion-tranche-g/.build/container` so the checkout we deploy and test from is the one that feeds containers** — costs a hub restart, removes the split permanently. Copying binaries across is the no-restart alternative and re-drifts on the next deploy. **Mechanics measured:** the CLI is invoked per-command, so a replaced binary is picked up immediately with no restart; only `sciontool` as PID 1 would need new containers, which conversation routing does not require. **The generalisable error is mine and worth naming: I accepted "the VM's git HEAD is X" as a property of the VM.** It is a property of a directory, and the machine had two that mattered. A path-free statement about a filesystem is an inference, and I built a recommendation on it without asking which path it referred to. → **Rule 1066: when a report states a fact about "the machine", find out which path it is a fact about before acting on it — hosts have more than one of most things.**

[^88]: **DEF-146 root cause isolated — the rebuild action writes repo-relative while the bind mount is absolute, so a correct build is unreachable by every container** (2026-09-04, measured on gteam immediately after ptone triggered the admin-UI rebuild): The admin action did everything right. Journal: `"Resolved rebuild-container-binaries executor" repo_path="/home/scion/scion-tranche-g"`, build complete in ~23s, output binary at `eb59ea98`, sha256 `9f51c97a`. **And no container can see it.** `make container-binaries` uses `CONTAINER_DIR := ./.build/container`, relative to the repo it builds in, so the output landed in `/home/scion/scion-tranche-g/.build/container/`. `SCION_DEV_BINARIES` points at `/home/scion/scion/.build/container/`, which is untouched — same commit `98543da9`, same sha256 `8f31d5c5`, same mtime Sep 2 14:38. Verified at the surface that matters: `scion version` inside `chat-auth--agent-a` is still `98543da9`, and `scion message --help` still prints `conv:<uuid>  Send to a conversation by ID (not yet supported — errors)`. **So the defect is narrower and more mundane than the four-way version divergence in [^87] suggested: one relative path and one absolute path that were never reconciled.** The divergence was the symptom; this is the mechanism. **Correct fix, and it is small:** the rebuild action must write to the `SCION_DEV_BINARIES` path rather than repo-relative. **Immediate unblock is different from the durable fix, and conflating them would have cost us:** repointing `SCION_DEV_BINARIES` does *not* help the 28 running agents, because a bind mount is fixed at container creation — only new containers would pick it up, and the fix would appear to fail. The immediate move is an atomic replace (temp file in the same directory, then rename) of the binaries at the mounted path, which every running agent picks up on its next command because the CLI is invoked per-command. **Two of my own predictions were falsified here and both are worth keeping.** I predicted the rebuild would be a *regression* to `45c440bd`; it was a no-op, because the admin UI reads a different repo than the one I had reasoned about. And I *withdrew a correct recommendation* — the `SCION_DEV_BINARIES` repoint — roughly a minute before the measurement that would have confirmed it, because ptone asked a direct question and commit ancestry let me reason to a confident answer. → **Rule 1067: when a measurement you have already commissioned will settle a question, a confident inference is not a faster answer, it is an unreviewed one. Say "measurement in flight" and wait — the cost of a minute's delay is always lower than the cost of retracting advice the principal may already have acted on.**

[^89]: **DEF-146 addendum 2 — `/home/scion/scion` is a detached-HEAD snapshot, which retires the "keep the checkouts in sync" fix entirely** (2026-09-04, measured): `git status` there reports `HEAD detached at ptone/scion/tranche-g`, HEAD `45c440bd`. **No local branch tracks `scion/tranche-g`** — the only local branch is `main` tracking `origin/main` at `1a2c1b07`, and the tree has two remotes (`origin` → GoogleCloudPlatform, `ptone` → ptone/scion). A detached HEAD is a snapshot taken at checkout time; it does not advance. **So the `git pull` I recommended would not merely have been ineffective, it would have failed outright** — there is no tracking branch to pull. Advancing it requires `git fetch ptone && git checkout ptone/scion/tranche-g --detach`, which nothing runs. The practical upshot is that this checkout is **vestigial except as the bind-mount source**: the hub is built from `scion-tranche-g`, the admin rebuild action reads `scion-tranche-g`, and the only remaining job of `/home/scion/scion` is to hold a directory that `SCION_DEV_BINARIES` happens to name. **That settles the durable fix in favour of changing the write path rather than synchronising checkouts** — synchronising would mean adding machinery to keep a detached snapshot current so that a second copy of a build can exist, when the first copy is already correct and merely written somewhere nothing reads. Prefer deleting the divergence to automating it.

[^90]: **DEF-147 — `RebuildContainerBinariesExecutor` reads `SCION_DEV_BINARIES`, prints it into the build log, warns when it is unset, and then never uses it** (2026-09-04, read in `pkg/hub/maintenance_executors.go:436-464` at `eb59ea98`): The executor's entire placement logic is `cmd := exec.CommandContext(ctx, "make", "container-binaries"); cmd.Dir = e.repoPath`. The Makefile's `CONTAINER_DIR := ./.build/container` is CWD-relative, so output lands in `<repoPath>/.build/container` unconditionally. `SCION_DEV_BINARIES` — the variable that names where containers actually mount from — is read at `:444` for exactly two purposes: to `Fprintf` it into the operator-visible build log, and to emit `"WARNING: SCION_DEV_BINARIES is not set; built binaries will not be mounted into containers until it is configured."` when empty. **The warning is the tell.** It states the consequence of a mismatch correctly and then does nothing to prevent one, so the non-empty case — a *wrong* value, or a right value the build ignores — passes silently while the empty case is loudly diagnosed. **This is worse than not reading the variable at all.** A build log that opens with `SCION_DEV_BINARIES=/home/scion/scion/.build/container` and closes with `Container binaries rebuild complete` reads as end-to-end confirmation, and it is why the gteam diagnosis took four rounds: the operator-facing evidence actively asserted the thing that was false. → **Rule 1068: a program that logs a configuration value it does not act on is not being transparent, it is manufacturing false evidence. Before trusting any value in a log, find the line that consumes it — printing is not consuming.** **Config mechanism, for the record:** `MaintenanceConfig.RepoPath` resolves in `cmd/server_foreground.go:2909` from `vs.WorkspacePath`, overridden by `SCION_MAINTENANCE_REPO_PATH` (which supports a `path@branch` form). The same value drives `rebuild-hub`, `rebuild-web` and the update check, **so it is not a container-binaries-specific knob** and repointing it moves every maintenance operation. Note also that `RebuildContainerBinariesExecutor` carries no `branch` field, unlike the hub and web executors — it builds whatever is checked out, which is why the checkout's HEAD is load-bearing and why a detached snapshot ([^89]) was able to silently pin the output. **Fix:** the executor should place output at `SCION_DEV_BINARIES` when set — passing it to the Makefile as `CONTAINER_DIR`, so one variable names the destination and the build honours it — and should fail rather than warn when the value is set but unwritable. **Immediate operational fix is different and does not need this:** set `SCION_MAINTENANCE_REPO_PATH=/home/scion/scion` so `<repoPath>/.build/container` and the mount source coincide. Recommended to ptone with the caveat that it relocates *all* maintenance operations, which is being measured before the switch.

[^91]: **DEF-148 — the maintenance subsystem hardcodes the remote name `origin` and derives the branch from the working tree, so on any fork-based deployment it silently tracks the wrong repository** (2026-09-04, read in `pkg/hub/maintenance_executors.go` at `eb59ea98`; symptom reported by ptone from the gteam admin UI: "when I check for updates, it wants to pull updates from upstream main, instead of fork/tranche-g"): Three call sites, one literal. `CheckForUpdates` (`:1034`) runs `git fetch origin`, then `currentBranch := git rev-parse --abbrev-ref HEAD`, then `trackingRef := "origin/" + currentBranch`. `RebuildHubExecutor` (`:289`) runs `git fetch origin`, `git checkout <branch>`, `git reset --hard origin/<branch>`. `RebuildWebExecutor` (`:386`) runs `git fetch origin`. **Nothing consults `git config branch.<name>.remote`.** On gteam the maintenance repo's `origin` is GoogleCloudPlatform/scion and the fork is a second remote named `ptone`, so every maintenance operation reads upstream regardless of what the checkout is tracking. **The configured branch is not the escape hatch it looks like.** `SCION_MAINTENANCE_REPO_PATH` accepts a `path@branch` form and `MaintenanceConfig.RepoBranch` is threaded into `RebuildHubExecutor` — but `CheckForUpdates` takes only `repoPath` and never receives it, so the update check and the rebuild can disagree about which branch they are talking about. On gteam they did: configured branch `deploy/gteam`, checked-out branch something else, and the UI reporting against `origin/<checked-out>`. **Two independent bugs are stacked here and separating them matters.** (1) The remote is not configurable. (2) The branch is read from two different sources depending on which operation runs. Fixing only (1) leaves the update check able to report progress against a branch the rebuild will not build. **Operational workaround, and it is the whole fix on gteam** — make the literal true: rename the remotes in the maintenance repo so `origin` *is* the fork, and keep the local branch named identically to the fork's branch so `origin/<branch>` resolves. This is ptone's proposal and it is correct precisely because it removes the divergence rather than configuring around it. **Proper fix:** resolve the remote from the branch's own upstream (`git rev-parse --abbrev-ref --symbolic-full-name @{u}`) and fail loudly when there is none, rather than assuming a name; and pass `RepoBranch` to `CheckForUpdates` so one source of truth answers "which branch" for every operation. **Related hazard retired as a side effect:** the previous value `\/home\/scion\/scion-tranche-g@deploy\/gteam` pinned `RebuildHubExecutor` to `deploy/gteam`, which is 78 commits behind `scion/tranche-g` with zero commits of its own — so Rebuild Hub would have checked out Aug-31 code in the repo that builds the running hub. Dropping the `@branch` suffix removes it. **Note the shape this shares with DEF-147** ([^90]): in both, a configuration value exists, is visible to the operator, and is not the thing the code actually obeys. DEF-147 prints a path it does not write to; DEF-148 accepts a branch that one of three consumers ignores. → **Rule 1069: when a subsystem hardcodes the name of an external binding — a remote, a host, a default account — it has not chosen a default, it has assumed a topology. The assumption is invisible until someone deploys the other topology, and then it fails by pointing confidently at the wrong thing rather than by erroring.**

[^92]: **DEF-146 RESOLVED, and DEF-148 gains a third distinct bug that fully explains ptone's symptom** (2026-09-04, measured on gteam after ptone ran the maintenance tasks). **Resolution of DEF-146:** the maintenance repo was retargeted to `/home/scion/scion` (no `@branch` suffix) and its remotes renamed so `origin` *is* the fork. The rebuild then wrote to the same directory the containers mount. Verified at the surface that matters rather than at the build log: the artefact changed on all four attributes — sha256 `8f31d5c5`→`55e76dac`, mtime Sep 2 14:38→Sep 4 17:17, **inode 4514→34611**, commit `98543da9`→`eb59ea98`. The inode change is the load-bearing one: it proves the file was *replaced*, which is what makes the change visible through a directory bind mount. **Two live containers** (`chat-auth--agent-a`, `color-demo--smith`) both report `eb59ea98` and both print `conv:<uuid>        Send to a conversation by ID` with the `(not yet supported — errors)` parenthetical gone, **neither restarted**. Hub healthy on `eb59ea98`, repo HEAD unmoved, agents' binary unaffected by the hub restart. **Two containers rather than one was deliberate** — a single container cannot distinguish "the mount propagates" from "this container happens to be new". **Third bug in DEF-148, and it is the actual cause of the reported symptom.** The journal shows the pre-retarget update check ran against `/home/scion/scion-tranche-g` with `tracking_ref=origin/HEAD` and `branch=HEAD`, concluding `commits_behind=31`, `latest=ff938acb`. The mechanism: `CheckForUpdates` computes `trackingRef := "origin/" + strings.TrimSpace(rev-parse --abbrev-ref HEAD)`, and **in a detached HEAD that command emits the literal string `HEAD`**. So the code constructs `origin/HEAD`, which git happily resolves to the *default branch of the remote* — GoogleCloudPlatform's `main`. The check then reports a confident, precise, entirely wrong 31-commit deficit against a repository we do not ship from. Nothing validates that the abbrev-ref output is a branch. **This is a separate failure from the hardcoded remote and would survive fixing it**: with `origin` correctly pointed at the fork, a detached maintenance checkout would still resolve `origin/HEAD` to the fork's default branch and report drift against `main` instead of `scion/tranche-g`. **Fix must be both:** resolve the remote from the branch's upstream, *and* refuse to proceed when `rev-parse --abbrev-ref HEAD` returns `HEAD`, because a detached checkout has no meaningful "latest" to compare against and the honest answer is an error, not a number. → **Rule 1070: `git rev-parse --abbrev-ref HEAD` returns the string `HEAD` when detached, and string-concatenating it into a ref produces `origin/HEAD` — a ref that resolves successfully to something entirely unrelated to what was asked. The dangerous shape is not the sentinel value, it is that the sentinel composes into a valid-looking ref rather than an error.** **Not verified, low priority:** the post-retarget update check's *result* was not logged before ptone triggered the rebuild, so we have confirmed the check now reads the right repo path but not what it concluded. **Status: DEF-146 closed. DEF-147 (executor ignores `SCION_DEV_BINARIES`) remains open as a code fix — the operational workaround makes the two paths coincide, which removes the symptom without removing the trap for the next deployment.**

[^93]: **DEF-141 and DEF-142 ACCEPTED and merged to `scion/tranche-g`** (2026-09-04, fast-forward `eb59ea98f`→`8795a70df`, 11 commits, 0 behind). Merge-gate sweep before taking it: fast-forward possible (no divergence); prohibition-list symbols all zero except one `authenticatedSender` occurrence which I confirmed is an **addition** (`+		authKind, authID := authenticatedSender(ctx)`), not a removal; the only deleted line mentioning a guarded surface is a comment. Production deletions are exactly the two sanctioned ones — `cmd/message.go` −135 (P5 drops the two-step) and `pkg/hubclient/messages.go` −25 (`ResolveConversation`). `messagebroker.go` changes classification only; the P-3 honouring gate (`if msg.ConversationID != ""`) is untouched, so no authorization behaviour moved. **I verified the two security-critical controls by my own mutation rather than by reading the developer's report.** (1) AC-3 allowlist: flipping `default: return false`→`true` in `disclosableResolutionReason` turns all three AC-3 tests red, and the table test fails on exactly `not-found`, `not-a-participant`, `boundary-violation`, `some-future-reason`, `(empty)` while `ambiguous`/`no-shared-project` stay green. (2) G1 provenance: applying the **realistic** mutation — prefer a body-supplied `project_id` *when present*, fall back to `agent.ProjectID` when absent — makes `TestDEF142_G1_ResolveContext_ProjectFromAuth_NotBody` **the only failing test out of eleven**, at `handlers_outbound_def142_test.go:321`, expected 400 got 403. Clean restore both times. **That result is the justification for having sent the test back.** The developer's original mutation sourced `ProjectID` from JSON unconditionally, which made it *empty* (no test sent the field) and tripped an unrelated "requires a project context" path — so it was caught by an accident of the mutation's shape. Under the prefer-when-present form, every other DEF-142 test stays green. Without the strengthened test, the natural version of this change would have landed undetected. **A mutation caught only by an implausible variant of itself has not tested the property.** **Defence-in-depth finding worth keeping:** the mutation was caught at 403, not by the disclosure assertions — DEF-138's group authorization refused the cross-project conversation before the ambiguity/collapse path could leak anything. So a ProjectID provenance failure alone is not sufficient for disclosure; it would take that *and* a weakening of DEF-138's project check. Two faults, not one. **Unresolved known-unknown, recorded rather than closed:** the developer saw one `make test-fast` failure that did not recur across 4+ subsequent runs, and did not retain the name or output. It is scoped out of this work by construction — `test-fast` runs `-tags no_sqlite`, which excludes every DEF-142 test file — so it cannot be caused by this change, but it is a flake in the *blocking* gate and nobody has characterised it. → **Rule 1071: never pipe a measurement through `tail`/`head` before it reaches the log. Capture whole, filter after. This cost the developer the identity of a flake in the blocking gate, and within the same hour it cost me a nearly-issued false rejection — my mutation script truncated `go test` output before writing the log, so the grep I ran over that log showed one failure where there were three. The apparatus destroyed the evidence in both cases, and in neither case did anything look wrong.** **CI gate-scope finding, escalated to ptone, not ours to fix:** the blocking job is `make test-fast` (`-tags no_sqlite`) while the SQLite suite is `continue-on-error: true`, so **256 of 829 test files (31%) cannot block a merge** — including all of DEF-142 and `handlers_agent_messaging_test.go`. The structural gates (authz guards, upsert guard, security marker gates, compat literals) *do* block, so the prohibition-list invariants remain defended; it is the behavioural half that is not. Recommended to ptone that `ci-fix-lead` measure whether the full suite is green on main and flip `continue-on-error` if so.

[^94]: **DEF-149 — main and tranche-g independently allocated the same advisory lock value, and main's own uniqueness test passed on the collision** (2026-09-05, found by trial merge of main `2ac554cb0` into `scion/tranche-g` `8795a70df`; 41 main-only commits, 105 tranche-g-only, merge base `9f66cd551`). Exactly two files conflicted, and **neither was one I predicted.** From file-overlap analysis I had expected the route files (`route_metadata.go`, `route_classification_test.go`, `server.go`); those merged cleanly. → **file-overlap analysis predicts conflict *risk*, not conflict *location*; only a trial merge settles where the conflicts actually are.** **Conflict 1, `pkg/hub/errors.go`, benign:** ours adds 6 error codes, main adds 11, no name collisions and no string-value collisions — kept both, 27 total, verified unique on both axes after resolution. **Conflict 2, `pkg/store/concurrency.go`, not benign and not additive:** ours declares `LockDataMigrations AdvisoryLockKey = 0x5C100014`, main declares `LockWebchatMigration AdvisoryLockKey = 0x5C100014`. **My own standing DEF-118 ruling — "the correct resolution of a lock-key conflict is always to ADD the entry" — is necessary but not sufficient here, and following it literally would have shipped the bug.** Keeping both verbatim yields two semantically distinct advisory locks sharing one integer, which on Postgres multi-replica means the boot-time conversation-model migration and the webchat migration silently serialise against each other; on SQLite both are no-ops, so **no amount of local testing would surface it.** The ruling assumed collisions occur on *name*. This one is on *value*, where "keep both" is exactly the wrong move. **Resolution:** keep both, renumber **ours** to `0x5C100015` (next free — allocation is contiguous `0x5C100001`–`0x5C100014`, plus `0x5C100020`, `0x5C101001`, `0x5C101002`), because main's is already shipped. Renumbering is safe: advisory locks are ephemeral session locks, never persisted, and a no-op on SQLite. **The defence worked, and the interesting part is which one.** Main carries `TestAdvisoryLockKeys_NonOverlapping`, which hardcodes **9 of the 24** keys and covers none of the three in play — it **passed throughout**, before and after the collision, and passes today. Tranche-g carries `TestAdvisoryLockKeys_AllUnique`: Check 1 value-uniqueness over a hand-maintained registry, Check 2 a bidirectional source-drift regex over `concurrency.go`. Post-merge, Check 2 immediately named both of main's unregistered keys (`LockRecoveryAuthz`, `LockWebchatMigration` — **main added both without registering either**), and with the collision reintroduced as a mutation Check 1 reported `duplicate advisory lock key value 0x5C100014: LockWebchatMigration and LockDataMigrations`. **So the branch under review supplied the gate that caught the mainline's latent defect.** → **a test that samples a fixed subset of a growing set decays into decoration: it keeps passing, and its passing is read as coverage of the whole set. Bind the test to the artefact the set is declared in, not to a copy of it.** **Method note against myself:** my first run of the gate on main returned `ok` and I nearly recorded main as green-and-therefore-fine — but `-run TestAdvisoryLockKeys` matching *something* and matching *what I meant* are different facts, and only `-v` distinguishes them. Same family as [^93]'s Rule 1071: the apparatus reported success while measuring the wrong thing. **A separate ad-hoc regex of mine, `Lock[A-Za-z]+`, silently dropped `LockA2ABridgeSweep` (digit in the name) and undercounted 24 as 23** — caught only because the count disagreed with the listing. The production test's `(\w+)` is correct; my throwaway one was not, which is the usual direction of that error.

[^95]: **DEF-150 — `fmt-check` is a target of `make ci` but is not a job in CI, so gofmt drift accumulated unnoticed on tranche-g** (2026-09-05, measured during the main merge): `make fmt-check` fails on **14 files** at tranche-g tip `8795a70df` — *before* the merge, so this is not merge fallout — while `.github/workflows/ci.yml` contains no `fmt-check` invocation at all (grep for it returns nothing; the blocking job runs `make test-fast`). Main and tranche-g both declare `go 1.26.1` and my local toolchain is `go1.26.1`, so this is genuine drift and not version skew. **Every developer running `make ci` locally has seen a red gate that CI does not enforce, which is the precise condition for a gate being ignored.** Changes are comment and whitespace only — thirteen files are pure const-block comment alignment (`git diff -w --ignore-blank-lines` empty), and `cmd/boot_m9_test.go` additionally gains two `//` lines from gofmt's doc-comment reflow around an indented block, comment text only, no code tokens. Fixed in a **separate commit** from the merge so the merge stays reviewable. **Same shape as the CI gate-scope finding in [^93]** and worth stating as one class: this repository has several gates whose *existence* is not the same as their *enforcement* — `test-fast` excludes 31% of test files, the SQLite suite is `continue-on-error`, and `fmt-check` is not wired up at all. **A gate that is defined but unreachable from CI is indistinguishable from an absent gate, except that it produces false confidence in anyone who reads the Makefile.** Recommended to ptone as `ci-fix-lead` work alongside the `continue-on-error` question, not fixed here beyond restoring the formatting.

[^96]: **DEF-151 — the main merge left `pkg/hub` test code uncompilable, every gate I ran was green, and I pushed it** (2026-09-05, found ~22h after the push by asking what I could measure rather than continuing to wait on ptone). Main's authorization refactor `a61cddd63` (#1435) removed the policy concept and renamed `createProjectMembersGroupAndPolicy` → `createProjectMembersGroup`, updating every call site it could see. Tranche-g had three it could not see. Git merged both sides with **no conflict**, and `handlers_broker_inbound_test.go` came out of the merge carrying **four call sites on the new name and one on the old name in the same file**. Pure rename otherwise — identical signature `(ctx, project *store.Project, callerUserID ...string)`, same defining file, all three sites passing only `(ctx, project)` — so the fix was mechanical (`a29d75f77`). **The interesting part is not the break, it is that nothing I ran could have caught it.** `go build ./...` passes because only *test* code referenced the old name. The blocking CI gate is `make test-fast` (`-tags no_sqlite`), which excludes those files. The four structural gates and `fmt-check` are all orthogonal. **So I reported "gates green" on a merge that did not compile, and every word of that report was true.** **I filed the enabling fact myself in [^93] two days earlier** — 31% of test files cannot block a merge — and then used `test-fast` as my merge verification anyway. Knowing a gate's blind spot is worthless unless the knowledge is applied at the moment of choosing what to run; a recorded caveat is not a control. → **standing practice: `go vet ./...` before any merge push. It typechecks test files in every package regardless of build tags, costs about a minute, and is the cheapest thing that would have caught this.** Whole tree now vets clean, which also establishes those three call sites were the only breakage rather than the first one found. **Method that produced the find, worth keeping:** of the files changed on both sides since the merge base (16 Go files), **2 conflicted loudly and 14 auto-merged silently — the breakage was in the silent set.** A conflict is a merge telling you where it was unsure; the dangerous residue is where it was confident and wrong. Having computed that set, I checked its security-relevant members directly: `pkg/hub/permissions/registry.go` (45 permission strings on all three of main, tranche-g and the merge — none lost in either direction), and `server.go`'s `StartMessageBroker`/`StartNotificationDispatcher`/`SetWebChatStore` (all still take `s.mu.Lock()`, no `RLock` — the deadlock constraint holds). **Semantic compatibility of the two authorization systems now verified rather than assumed:** the full SQLite suite on the merged tree gives 67 ok / 2 failing packages / 7 failing tests, and those 7 are byte-identical to the 7 that fail on main in my container (Rule 1061 environmental set). `pkg/hub` passes in 395s, and a targeted run shows **all 37 DEF-138/141/142 tests green**, including `TestDEF142_G1_ResolveContext_ProjectFromAuth_NotBody` and `TestDEF142_AC3_DisclosableResolutionReason` — the two controls I mutation-verified at merge time. Main's only edit to `pkg/hub/authorize_message.go` was a **comment**, and tranche-g never touched that file, so the two authorization changes turn out not to intersect in code at all. **General statement, and it is the one to carry:** *a merge that compiles is not a merge that builds, when a third of the tree's tests are excluded from the thing you called a build.*
